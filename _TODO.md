# TODO (proposal/spec sync)

## OpenSpec specs drift

- `openspec/specs/yaml-dsl-editor-core/spec.md` 仍描述模板/outline 以 `output` 为核心,但仓库实际已升级为 `outputs`(需按代码现状同步 spec 文本与场景).
- `openspec/specs/testing-quality/spec.md` 中 demo 对拍引用路径在 `demo_big_data_report` 重构后已陈旧(需按当前实现同步).

## Low priority follow-ups

- `scalim-viz`: 近期引入了更多执行/输出模式,可能需要更新其“表达/示例/文档”,但不要求纳入对拍回归;可单独开 change 跟踪(优先级较低).
