[project]
name = "scalim-agent-fixture"
version = "0.0.0"
description = "prompt-eval agent fixture (stub CLI)"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
scalim-fixture-cli = "scalim_agent_fixture.cli:main"
