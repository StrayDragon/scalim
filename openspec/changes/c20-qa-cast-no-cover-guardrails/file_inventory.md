# 文件清单与建议（c20 cast/no-cover）

基线来源：

- `.tmp/artifacts/cast-usage.report.json`
- `.tmp/artifacts/no-cover.report.json`

说明：

- 本清单以“文件”为粒度标注用途与建议；用途基于路径/顶层符号推断（非最终定案）。
- allow 推荐默认仅指 **行级**（文件级需非常谨慎）。

字段：

- `cast types(top)`：该文件内 `cast(...)` 的目标类型 Top（用于判断是否是“结构化解析边界”）。
- `no-cover patterns`：粗分类（`plain`/`compat-py<`/`type-ignore`/`other`）。

## scripts


| 文件                                                 | 主要用途                                                                                                                | cast | cast types(top)                   | no-cover | no-cover patterns | cast 推荐                         | no-cover 推荐                          |
| -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | ---- | --------------------------------- | -------- | ----------------- | ------------------------------- | ------------------------------------ |
| `scripts/check-doc-governance.py`                  | Dev 检查脚本：`check-doc-governance`（符号: `_repo_root`, `_read_text`, `_check_claude_symlink`）                            | 0    |                                   | 2        | plain×2           |                                 | 允许：脚本罕见错误分支；行级 allow-no-cover + 触发条件 |
| `scripts/check-dynattr.py`                         | Dev 检查脚本：`check-dynattr`（符号: `_Hit`, `_CommentPolicy`, `_LocatedAstNode`）                                           | 5    | "_LocatedAstNode"×5               | 1        | plain×1           | 允许（低优先级）；若 gate 则仅行级 allow + 原因 | 允许：脚本罕见错误分支；行级 allow-no-cover + 触发条件 |
| `scripts/check-known-outer-paths.py`               | Dev 检查脚本：`check-known-outer-paths`（符号: `_Entry`, `_repo_root`, `_load_entries`）                                     | 0    |                                   | 1        | plain×1           |                                 | 允许：脚本罕见错误分支；行级 allow-no-cover + 触发条件 |
| `scripts/gen-agent-skill.py`                       | Dev 生成脚本：`gen-agent-skill`（符号: `parse_args`, `main`）                                                                | 0    |                                   | 1        | plain×1           |                                 | 允许：脚本罕见错误分支；行级 allow-no-cover + 触发条件 |
| `scripts/gen-marimo-coverage.py`                   | Dev 生成脚本：`gen-marimo-coverage`（符号: `_Row`, `_diff`, `_write_if_changed`）                                            | 0    |                                   | 1        | plain×1           |                                 | 允许：脚本罕见错误分支；行级 allow-no-cover + 触发条件 |
| `scripts/gen-viz-data.py`                          | Dev 生成脚本：`gen-viz-data`（符号: `_ensure_repo_root_on_syspath`, `_parse_args`, `_resolve_profile_root`）                 | 0    |                                   | 1        | plain×1           |                                 | 允许：脚本罕见错误分支；行级 allow-no-cover + 触发条件 |
| `scripts/gen-viz-workflow-bundle.py`               | Dev 生成脚本：`gen-viz-workflow-bundle`（符号: `_parse_args`, `_normalize_allowed_modules`, `main`）                         | 0    |                                   | 1        | plain×1           |                                 | 允许：脚本罕见错误分支；行级 allow-no-cover + 触发条件 |
| `scripts/gen-viz-workflow-demo-big-data-report.py` | Dev 生成脚本：`gen-viz-workflow-demo-big-data-report`（符号: `_parse_args`, `_normalize_allowed_modules`, `_to_posix_path`） | 0    |                                   | 1        | plain×1           |                                 | 允许：脚本罕见错误分支；行级 allow-no-cover + 触发条件 |
| `scripts/scan-downstream-yaml-dsl.py`              | Dev 扫描脚本：`scan-downstream-yaml-dsl`（符号: `_Entry`, `_FileResult`, `_DownstreamResult`）                               | 0    |                                   | 1        | plain×1           |                                 | 允许：脚本罕见错误分支；行级 allow-no-cover + 触发条件 |
| `scripts/upgrade-yaml-dsl-aggregate-fields.py`     | Dev 升级脚本：`upgrade-yaml-dsl-aggregate-fields`（符号: `_as_mapping`, `_as_list`, `_field_value`）                         | 3    | "Dict[str, Any]"×1; "List[Any]"×1 | 0        |                   | 允许（低优先级）；若 gate 则仅行级 allow + 原因 |                                      |


## src/scalim/_internal


| 文件                                 | 主要用途                                                                | cast | cast types(top)                  | no-cover | no-cover patterns | cast 推荐                        | no-cover 推荐 |
| ---------------------------------- | ------------------------------------------------------------------- | ---- | -------------------------------- | -------- | ----------------- | ------------------------------ | ----------- |
| `src/scalim/_internal/loggingx.py` | `scalim` 内部日志工具（符号: `_ensure_null_handler`, `get_logger`, `prefix`） | 5    | "object"×2; "Iterable[object]"×2 | 0        |                   | 不建议 allow；应通过收紧类型/运行时校验替代 cast |             |


## src/scalim/cli


| 文件                           | 主要用途                                                                             | cast | cast types(top)                    | no-cover | no-cover patterns | cast 推荐               | no-cover 推荐 |
| ---------------------------- | -------------------------------------------------------------------------------- | ---- | ---------------------------------- | -------- | ----------------- | --------------------- | ----------- |
| `src/scalim/cli/yaml_dsl.py` | CLI：`yaml_dsl`（符号: `ValidationPayload`, `WorkflowValidationPayload`, `register`） | 11   | "Dict[str, Any]"×7; "List[Path]"×2 | 0        |                   | 默认：重构优先；确属边界再行级 allow |             |


## src/scalim/dsl/by_yaml


| 文件                                                               | 主要用途                                                                                                                                                        | cast | cast types(top)                                     | no-cover | no-cover patterns                    | cast 推荐                               | no-cover 推荐                             |
| ---------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- | --------------------------------------------------- | -------- | ------------------------------------ | ------------------------------------- | --------------------------------------- |
| `src/scalim/dsl/by_yaml/config_parsing/call_by.py`               | YAML DSL 配置解析：`call_by`（符号: `CallByParseError`, `CallByValue`, `ParsedCallBy`）                                                                              | 0    |                                                     | 15       | plain×8; compat-py<×3; type-ignore×4 |                                       | 允许：兼容分支不可达；行级 allow-no-cover + 兼容原因     |
| `src/scalim/dsl/by_yaml/config_parsing/field_extract.py`         | YAML DSL 配置解析：`field_extract`（符号: `FieldExtractCompileError`, `compile_field_extract`, `derive_source_field_data_key`）                                      | 0    |                                                     | 3        | plain×3                              |                                       | 默认：补测试移除为主；确属不可达/防御分支则行级 allow-no-cover |
| `src/scalim/dsl/by_yaml/config_parsing/imports.py`               | YAML DSL 配置解析：`imports`（符号: `ImportSource`, `ImportTraceItem`, `YamlImportExpansionError`）                                                                  | 19   | "Dict[str, Any]"×12; "object"×4                     | 0        |                                      | 重构优先：用运行时契约/解析函数替代 cast；确属边界再行级 allow |                                         |
| `src/scalim/dsl/by_yaml/config_parsing/jsonschema_issues.py`     | YAML DSL 配置解析：`jsonschema_issues`（符号: `JsonSchemaCollectorError`, `_absolute_path`, `_format_error_path`）                                                   | 3    | "Iterable[Any]"×2; "Sequence[object]"×1             | 0        |                                      | 重构优先：用运行时契约/解析函数替代 cast；确属边界再行级 allow |                                         |
| `src/scalim/dsl/by_yaml/config_parsing/models/__init__.py`       | YAML DSL 配置解析：`__init_`_（符号: `ensure_mapping`, `RawDemand`, `FieldDef`）                                                                                     | 8    | "Dict[str, Any]"×7; "List[Any]"×1                   | 0        |                                      | 重构优先：用运行时契约/解析函数替代 cast；确属边界再行级 allow |                                         |
| `src/scalim/dsl/by_yaml/config_parsing/parsers/outputs.py`       | YAML DSL 配置解析（parsers）：`outputs`（符号: `_non_empty_str`, `_AggregateFieldDef`, `_AggregateAliasIndex`）                                                        | 16   | "Dict[str, Any]"×9; "Tuple[str, ...]"×5             | 1        | plain×1                              | 重构优先：用运行时契约/解析函数替代 cast；确属边界再行级 allow | 默认：补测试移除为主；确属不可达/防御分支则行级 allow-no-cover |
| `src/scalim/dsl/by_yaml/config_parsing/parsers/utils.py`         | YAML DSL 配置解析（parsers）：`utils`（符号: `str_or_none`, `mapping_or_none`, `list_or_none`）                                                                        | 2    | "Dict[str, Any]"×1; "List[Any]"×1                   | 0        |                                      | 重构优先：用运行时契约/解析函数替代 cast；确属边界再行级 allow |                                         |
| `src/scalim/dsl/by_yaml/config_parsing/project_config.py`        | YAML DSL 配置解析：`project_config`（符号: `YamlDslProjectConfig`, `_read_yaml_mapping`, `_resolve_dir`）                                                            | 4    | "Dict[str, Any]"×2; "Dict[object, object]"×1        | 0        |                                      | 重构优先：用运行时契约/解析函数替代 cast；确属边界再行级 allow |                                         |
| `src/scalim/dsl/by_yaml/config_parsing/security.py`              | YAML DSL 配置解析：`security`（符号: `_NameCollector`, `extract_dependencies_from_compute`, `is_constant_compute_expression`）                                       | 12   | "ast.Name"×1; "ast.BinOp"×1                         | 9        | plain×9                              | 重构优先：用运行时契约/解析函数替代 cast；确属边界再行级 allow | 默认：补测试移除为主；确属不可达/防御分支则行级 allow-no-cover |
| `src/scalim/dsl/by_yaml/config_parsing/template_precompile.py`   | YAML DSL 配置解析：`template_precompile`（符号: `_validate_template_sandbox`, `_scan_template_expr_sandbox_violation`, `_raise_template_vars_not_json_like`）        | 3    | "str"×1; "Sequence[object]"×1                       | 0        |                                      | 重构优先：用运行时契约/解析函数替代 cast；确属边界再行级 allow |                                         |
| `src/scalim/dsl/by_yaml/config_parsing/unknown_fields.py`        | YAML DSL 配置解析：`unknown_fields`（符号: `UnknownFieldIssue`, `_unescape_json_pointer`, `_resolve_json_pointer`）                                                  | 19   | "Dict[str, Any]"×12; "List[Any]"×5                  | 0        |                                      | 重构优先：用运行时契约/解析函数替代 cast；确属边界再行级 allow |                                         |
| `src/scalim/dsl/by_yaml/config_parsing/validator.py`             | YAML DSL 配置解析：`validator`（符号: `_field_def_path`, `_output_item_requires_unique_effective_display_names`, `_outputs_require_unique_effective_display_names`） | 17   | "Dict[str, Any]"×8; "List[Any]"×3                   | 2        | plain×2                              | 重构优先：用运行时契约/解析函数替代 cast；确属边界再行级 allow | 默认：补测试移除为主；确属不可达/防御分支则行级 allow-no-cover |
| `src/scalim/dsl/by_yaml/config_parsing/validators/base.py`       | YAML DSL 配置解析（validators）：`base`（符号: `ValidatorMixinBase`, `ValidatorFieldBaseMixin`）                                                                       | 1    | "Dict[str, Any]"×1                                  | 0        |                                      | 重构优先：用运行时契约/解析函数替代 cast；确属边界再行级 allow |                                         |
| `src/scalim/dsl/by_yaml/params_template.py`                      | YAML DSL：`params_template`（符号: `ParamsTemplateError`, `ParamsTemplateCompileError`, `ParamsTemplateRenderError`）                                            | 9    | "Dict[object, object]"×3; "Set[RuntimeValue]"×1     | 0        |                                      | 重构优先：用运行时契约/解析函数替代 cast；确属边界再行级 allow |                                         |
| `src/scalim/dsl/by_yaml/runtime/_internal/conversion_lookup.py`  | YAML DSL runtime 内部转换：`conversion_lookup`（符号: `cast_int`, `cast_str`, `cast_decimal`）                                                                       | 1    | "Sequence[object]"×1                                | 0        |                                      | 重构优先：用运行时契约/解析函数替代 cast；确属边界再行级 allow |                                         |
| `src/scalim/dsl/by_yaml/runtime/_internal/conversion_sources.py` | YAML DSL runtime 内部转换：`conversion_sources`（符号: `_ensure_field_value`, `_require_call_by_context`, `_resolve_call_by_ctx_attr`）                              | 7    | "Callable[..., object]"×3; "Mapping[str, object]"×2 | 4        | plain×4                              | 重构优先：用运行时契约/解析函数替代 cast；确属边界再行级 allow | 默认：补测试移除为主；确属不可达/防御分支则行级 allow-no-cover |
| `src/scalim/dsl/by_yaml/runtime/compiler.py`                     | YAML DSL runtime：`compiler`（符号: `_ensure_allowlist`, `validate_allowlist`, `_require_dict`）                                                                 | 10   | "Dict[str, Any]"×3; "List[object]"×2                | 0        |                                      | 重构优先：用运行时契约/解析函数替代 cast；确属边界再行级 allow |                                         |
| `src/scalim/dsl/by_yaml/runtime/contracts.py`                    | YAML DSL runtime：`contracts`（符号: `_UnsetType`, `RunOverrides`, `RunOptions`）                                                                                | 1    | "Any"×1                                             | 0        |                                      | 重构优先：用运行时契约/解析函数替代 cast；确属边界再行级 allow |                                         |
| `src/scalim/dsl/by_yaml/runtime/output_composition_yaml.py`      | YAML DSL runtime：`output_composition_yaml`（符号: `PathlessCsvOutputError`, `_is_pathless_container_path`, `_resolve_output_container_path_with_overrides`）    | 10   | "Dict[str, Any]"×4; "Tuple[str, ...]"×3             | 1        | plain×1                              | 重构优先：用运行时契约/解析函数替代 cast；确属边界再行级 allow | 默认：补测试移除为主；确属不可达/防御分支则行级 allow-no-cover |
| `src/scalim/dsl/by_yaml/runtime/output_path_resolve.py`          | YAML DSL runtime：`output_path_resolve`（符号: `resolve_output_container_path`）                                                                                 | 1    | "Dict[str, Any]"×1                                  | 0        |                                      | 重构优先：用运行时契约/解析函数替代 cast；确属边界再行级 allow |                                         |
| `src/scalim/dsl/by_yaml/runtime/references.py`                   | YAML DSL runtime：`references`（符号: `_has_wildcard`, `ReferenceParser`, `ResolverPolicy`）                                                                     | 0    |                                                     | 2        | plain×2                              |                                       | 默认：补测试移除为主；确属不可达/防御分支则行级 allow-no-cover |
| `src/scalim/dsl/by_yaml/schema_dsl/builder.py`                   | YAML DSL schema DSL：`builder`（符号: `_build_default_types_module`, `SchemaMeta`, `SchemaBuilder`）                                                             | 6    | "Dict[str, Any]"×3; "Tuple[Any, ...]"×2             | 0        |                                      | 重构优先：用运行时契约/解析函数替代 cast；确属边界再行级 allow |                                         |
| `src/scalim/dsl/by_yaml/schema_dsl/models/outputs.py`            | YAML DSL schema DSL（models）：`outputs`（符号: `OutputContainerConfig`, `OutputAggregateFieldConfig`, `OutputAggregateConfig`）                                   | 5    | "Dict[str, Any]"×3; "List[Dict[str, Any]]"×1        | 0        |                                      | 重构优先：用运行时契约/解析函数替代 cast；确属边界再行级 allow |                                         |
| `src/scalim/dsl/by_yaml/tools.py`                                | YAML DSL：`tools`（符号: `OutputConfigDict`, `load_output_config`, `derive_base_module_path`）                                                                   | 2    | "OutputConfigDict"×1; "object"×1                    | 0        |                                      | 重构优先：用运行时契约/解析函数替代 cast；确属边界再行级 allow |                                         |
| `src/scalim/dsl/by_yaml/workflow_compile.py`                     | YAML DSL：`workflow_compile`（符号: `_compile_workflow_resources`, `_reserved_xlsx_paths_from_resources`, `_build_demand_nodes_and_graph`）                      | 1    | "WorkflowConfig"×1                                  | 7        | plain×7                              | 重构优先：用运行时契约/解析函数替代 cast；确属边界再行级 allow | 默认：补测试移除为主；确属不可达/防御分支则行级 allow-no-cover |
| `src/scalim/dsl/by_yaml/workflow_config.py`                      | YAML DSL：`workflow_config`（符号: `WorkflowConfigError`, `_safe_load_yaml_no_duplicates`, `WorkflowWorkbookResource`）                                          | 32   | "Dict[str, Any]"×17; "Dict[Any, Any]"×6             | 5        | plain×5                              | 重构优先：用运行时契约/解析函数替代 cast；确属边界再行级 allow | 默认：补测试移除为主；确属不可达/防御分支则行级 allow-no-cover |
| `src/scalim/dsl/by_yaml/workflow_entrypoints.py`                 | YAML DSL：`workflow_entrypoints`（符号: `_extract_bundle_viz_base_config`, `run_workflow`）                                                                      | 10   | "Any"×9; "RunOverrides"×1                           | 3        | plain×3                              | 重构优先：用运行时契约/解析函数替代 cast；确属边界再行级 allow | 默认：补测试移除为主；确属不可达/防御分支则行级 allow-no-cover |


## src/scalim/execution


| 文件                                                                       | 主要用途                                                                                                                                         | cast | cast types(top)                                                                | no-cover | no-cover patterns | cast 推荐               | no-cover 推荐                             |
| ------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------- | ---- | ------------------------------------------------------------------------------ | -------- | ----------------- | --------------------- | --------------------------------------- |
| `src/scalim/execution/adaptive/_internal/loadref_scheduler_base.py`      | Execution adaptive（internal）：`loadref_scheduler_base`（符号: `AdaptiveLoadRefSchedulerBase`）                                                    | 0    |                                                                                | 4        | plain×4           |                       | 默认：补测试移除为主；确属不可达/防御分支则行级 allow-no-cover |
| `src/scalim/execution/adaptive/_internal/loadref_scheduler_execution.py` | Execution adaptive（internal）：`loadref_scheduler_execution`（符号: `AdaptiveLoadRefSchedulerExecutionMixin`）                                     | 2    | "LoadRefOperatorExecutor"×1; "Dict[Tuple[str, object], _AdaptiveTaskResult]"×1 | 0        |                   | 默认：重构优先；确属边界再行级 allow |                                         |
| `src/scalim/execution/adaptive/_internal/loadref_scheduler_planning.py`  | Execution adaptive（internal）：`loadref_scheduler_planning`（符号: `AdaptiveLoadRefSchedulerPlanningMixin`）                                       | 3    | "Hashable"×2; "Dict[Tuple[str, object], object]"×1                             | 0        |                   | 默认：重构优先；确属边界再行级 allow |                                         |
| `src/scalim/execution/adaptive/submission_unit.py`                       | Execution adaptive：`submission_unit`（符号: `PoolWaitStats`, `LayerScheduleStats`, `run_tasks_in_pool`）                                         | 0    |                                                                                | 1        | plain×1           |                       | 默认：补测试移除为主；确属不可达/防御分支则行级 allow-no-cover |
| `src/scalim/execution/adaptive/thread_loop_executor.py`                  | Execution adaptive：`thread_loop_executor`（符号: `_best_effort_all_tasks`, `ThreadLoopExecutor`）                                                | 1    | "Any"×1                                                                        | 8        | plain×8           | 默认：重构优先；确属边界再行级 allow | 默认：补测试移除为主；确属不可达/防御分支则行级 allow-no-cover |
| `src/scalim/execution/derived_outputs.py`                                | Execution：`derived_outputs`（符号: `_auto_str_normalize_derived_key_part`, `AggregationKeyLimitExceededError`, `DistinctKeyLimitExceededError`） | 1    | "NonNullFieldValue"×1                                                          | 5        | plain×5           | 默认：重构优先；确属边界再行级 allow | 默认：补测试移除为主；确属不可达/防御分支则行级 allow-no-cover |
| `src/scalim/execution/executor/batch/_internal/segments.py`              | Execution batch（internal）：`segments`（符号: `iter_operator_segments`）                                                                           | 1    | "LoadRefOp"×1                                                                  | 0        |                   | 默认：重构优先；确属边界再行级 allow |                                         |
| `src/scalim/execution/executor/batch/executor.py`                        | Execution batch：`executor`（符号: `BatchExecutor`）                                                                                              | 4    | "Sequence[SupportedOperatorIr]"×1; "SupportedOperatorIr"×1                     | 0        |                   | 默认：重构优先；确属边界再行级 allow |                                         |
| `src/scalim/execution/executor/helpers/field_access.py`                  | Execution helpers：`field_access`（符号: `extract_field`, `extract_field_segments`, `_extract_field_segment`）                                    | 7    | "FieldValue"×4; "Mapping[str, Any]"×1                                          | 0        |                   | 默认：重构优先；确属边界再行级 allow |                                         |
| `src/scalim/execution/executor/operators/compute/executor.py`            | Execution operators：`executor`（符号: `_execute_constant_compute`, `_execute_secure_compute`, `_execute_general_compute`）                       | 1    | "ComputeOperatorIr"×1                                                          | 0        |                   | 默认：重构优先；确属边界再行级 allow |                                         |
| `src/scalim/execution/executor/operators/load.py`                        | Execution operators：`load`（符号: `LoadOperatorExecutor`）                                                                                       | 2    | "object"×1; "Mapping[object, object]"×1                                        | 0        |                   | 默认：重构优先；确属边界再行级 allow |                                         |
| `src/scalim/execution/executor/operators/release.py`                     | Execution operators：`release`（符号: `ReleaseOperatorExecutor`）                                                                                 | 1    | "ReleaseOperatorIr"×1                                                          | 0        |                   | 默认：重构优先；确属边界再行级 allow |                                         |
| `src/scalim/execution/executor/operators/write.py`                       | Execution operators：`write`（符号: `WriteColumnOperatorExecutor`, `WriteRowOperatorExecutor`）                                                   | 2    | "WriteColumnOperatorIr"×1; "WriteRowOperatorIr"×1                              | 0        |                   | 默认：重构优先；确属边界再行级 allow |                                         |
| `src/scalim/execution/pipeline/base/_adaptive_pool.py`                   | Execution pipeline base：`_adaptive_pool`（符号: `maybe_create_adaptive_pool`）                                                                   | 0    |                                                                                | 3        | plain×3           |                       | 默认：补测试移除为主；确属不可达/防御分支则行级 allow-no-cover |
| `src/scalim/execution/pipeline/base/_row_emission.py`                    | Execution pipeline base：`_row_emission`（符号: `RowEmissionCoordinator`）                                                                        | 1    | "Callable[[Sequence[str], Sequence[FieldValue]], None]"×1                      | 0        |                   | 默认：重构优先；确属边界再行级 allow |                                         |
| `src/scalim/execution/pipeline/base/pipeline.py`                         | Execution pipeline base：`pipeline`（符号: `_ReverseSortValue`, `_make_noarg_loader_call`, `Pipeline`）                                           | 9    | "LoaderResultMapping"×2; "List[Hashable]"×2                                    | 0        |                   | 默认：重构优先；确属边界再行级 allow |                                         |
| `src/scalim/execution/run_ir.py`                                         | Execution：`run_ir`（符号: `ObservabilitySpec`, `ExecutionRequest`, `ExecutionResult`）                                                           | 1    | "Any"×1                                                                        | 0        |                   | 默认：重构优先；确属边界再行级 allow |                                         |
| `src/scalim/execution/workflow_cache_pool.py`                            | Execution：`workflow_cache_pool`（符号: `WorkflowCachePoolError`, `_ensure_json_like`, `_normalize_json_like`）                                   | 0    |                                                                                | 1        | plain×1           | ✅ 已重构移除 cast（通过 `TypeGuard` 做运行时窄化与 dict 归一化） | 默认：补测试移除为主；确属不可达/防御分支则行级 allow-no-cover |


## src/scalim/hooks


| 文件                                                    | 主要用途                                                                                          | cast | cast types(top)                                    | no-cover | no-cover patterns | cast 推荐               | no-cover 推荐                             |
| ----------------------------------------------------- | --------------------------------------------------------------------------------------------- | ---- | -------------------------------------------------- | -------- | ----------------- | --------------------- | --------------------------------------- |
| `src/scalim/hooks/_internal/common.py`                | Hooks（internal）：`common`（符号: `resolve_mro_attr`, `read_optional_attr`, `read_callable_attr`）  | 2    | "Callable[..., Any]"×1; "Set[str]"×1               | 0        |                   | 默认：重构优先；确属边界再行级 allow |                                         |
| `src/scalim/hooks/_internal/manager_base.py`          | Hooks（internal）：`manager_base`（符号: `ExecutionHookLike`, `HookManagerLike`, `HookManagerBase`） | 2    | "HookManagerLike"×1; "object"×1                    | 3        | plain×3           | 默认：重构优先；确属边界再行级 allow | 默认：补测试移除为主；确属不可达/防御分支则行级 allow-no-cover |
| `src/scalim/hooks/_internal/manager_state.py`         | Hooks（internal）：`manager_state`（符号: `HookManagerStateMixin`）                                  | 7    | "List[ExecutionHookLike]"×2; "Mapping[Any, Any]"×1 | 0        |                   | 默认：重构优先；确属边界再行级 allow |                                         |
| `src/scalim/hooks/_internal/manager_subscriptions.py` | Hooks（internal）：`manager_subscriptions`（符号: `HookManagerSubscriptionMixin`）                   | 2    | "HookOnEventHandler"×1; "HookTypedHandler"×1       | 0        |                   | 默认：重构优先；确属边界再行级 allow |                                         |


## src/scalim/ob


| 文件                                                | 主要用途                                                                                                                             | cast | cast types(top)                                 | no-cover | no-cover patterns | cast 推荐               | no-cover 推荐                             |
| ------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ---- | ----------------------------------------------- | -------- | ----------------- | --------------------- | --------------------------------------- |
| `src/scalim/ob/_internal/manager_capture.py`      | Observability（internal）：`manager_capture`（符号: `ObserverManagerCaptureMixin`）                                                     | 3    | "Deque[Event]"×2; "Any"×1                       | 0        |                   | 默认：重构优先；确属边界再行级 allow |                                         |
| `src/scalim/ob/_internal/manager_registry.py`     | Observability（internal）：`manager_registry`（符号: `ObserverManagerRegistryMixin`）                                                   | 1    | "Deque[Event]"×1                                | 0        |                   | 默认：重构优先；确属边界再行级 allow |                                         |
| `src/scalim/ob/_internal/manager_state.py`        | Observability（internal）：`manager_state`（符号: `ObserverManagerStateMixin`）                                                         | 5    | "MappingABC[Any, Any]"×1; "list[Any]"×1         | 0        |                   | 默认：重构优先；确属边界再行级 allow |                                         |
| `src/scalim/ob/presets/_internal/viz_handlers.py` | Observability presets（internal）：`viz_handlers`（符号: `_safe_len`, `_normalize_dict_keys`, `_sample_value`）                         | 7    | "Dict[Any, Any]"×4; "VizObserverConfig"×1       | 0        |                   | 默认：重构优先；确属边界再行级 allow |                                         |
| `src/scalim/ob/presets/_internal/viz_nodes.py`    | Observability presets（internal）：`viz_nodes`（符号: `VizObserverNodeMixin`）                                                          | 1    | "list[Dict[str, Any]]"×1                        | 0        |                   | 默认：重构优先；确属边界再行级 allow |                                         |
| `src/scalim/ob/presets/_internal/viz_output.py`   | Observability presets（internal）：`viz_output`（符号: `VizEventEmitter`, `VizObserverOutputMixin`）                                    | 3    | "Dict[str, Any]"×2; "VizObserverConfig"×1       | 0        |                   | 默认：重构优先；确属边界再行级 allow |                                         |
| `src/scalim/ob/presets/execution_trace.py`        | Observability presets：`execution_trace`（符号: `LoaderCallStep`, `FieldSlimStep`, `RowWriteStep`）                                   | 1    | "Iterable[Any]"×1                               | 0        |                   | 默认：重构优先；确属边界再行级 allow |                                         |
| `src/scalim/ob/presets/row_gap.py`                | Observability presets：`row_gap`（符号: `RowGapObserver`）                                                                            | 3    | "Dict[Hashable, Any]"×2; "Iterable[Hashable]"×1 | 0        |                   | 默认：重构优先；确属边界再行级 allow |                                         |
| `src/scalim/ob/presets/viz/output_composition.py` | Observability viz presets：`output_composition`（符号: `_get_snapshot_node_ids`, `_ensure_snapshot_list`, `_get_snapshot_edge_keys`） | 4    | "List[Dict[str, Any]]"×2; "Iterable[str]"×2     | 0        |                   | 默认：重构优先；确属边界再行级 allow |                                         |
| `src/scalim/ob/presets/viz/workflow.py`           | Observability viz presets：`workflow`（符号: `_as_node_id`, `_workflow_node_ref`, `_workflow_resource_ref`）                          | 3    | "Dict[str, Any]"×2; "List[Dict[str, Any]]"×1    | 1        | plain×1           | 默认：重构优先；确属边界再行级 allow | 默认：补测试移除为主；确属不可达/防御分支则行级 allow-no-cover |


## src/scalim/planning


| 文件                                    | 主要用途                                                                                       | cast | cast types(top)                      | no-cover | no-cover patterns | cast 推荐               | no-cover 推荐 |
| ------------------------------------- | ------------------------------------------------------------------------------------------ | ---- | ------------------------------------ | -------- | ----------------- | --------------------- | ----------- |
| `src/scalim/planning/viz_schedule.py` | Planning：`viz_schedule`（符号: `_build_ref_deps`, `_build_layers`, `build_viz_schedule_plan`） | 0    |                                     | 0        |                   | ✅ 已重构移除 cast（用 `TypedDict`/`isinstance` 收紧类型） |             |


## src/scalim/sinks


| 文件                               | 主要用途                                                                                                           | cast | cast types(top) | no-cover | no-cover patterns | cast 推荐 | no-cover 推荐                             |
| -------------------------------- | -------------------------------------------------------------------------------------------------------------- | ---- | --------------- | -------- | ----------------- | ------- | --------------------------------------- |
| `src/scalim/sinks/sink_excel.py` | Sink：`sink_excel`（符号: `_acquire_write_lock`, `_release_write_lock`, `_best_effort_close_write_only_worksheet`） | 0    |                 | 3        | plain×3           |         | 默认：补测试移除为主；确属不可达/防御分支则行级 allow-no-cover |


## src/scalim/spec


| 文件                                       | 主要用途                                                                                                         | cast | cast types(top)                                            | no-cover | no-cover patterns | cast 推荐               | no-cover 推荐 |
| ---------------------------------------- | ------------------------------------------------------------------------------------------------------------ | ---- | ---------------------------------------------------------- | -------- | ----------------- | --------------------- | ----------- |
| `src/scalim/spec/ir/binding/__init__.py` | Spec IR：`__init_`_（符号: `_stable_lookup_key_sort_key`, `build_stable_lookup_key_list`, `LoaderCallContextIr`） | 5    | "Tuple[object, ...]"×2; "Dict[object, object]"×1           | 0        |                   | 默认：重构优先；确属边界再行级 allow |             |
| `src/scalim/spec/ir/helpers.py`          | Spec IR：`helpers`（符号: `infer_lookup_steps`, `extract_from_fields`, `call_loader_with_binding`）               | 2    | "LoaderResultMapping"×2                                    | 0        |                   | 默认：重构优先；确属边界再行级 allow |             |
| `src/scalim/spec/ir/relations.py`        | Spec IR：`relations`（符号: `FieldRefIr`, `JoinConditionIr`, `RelationIr`）                                       | 1    | "LookupSourceRefIrBase"×1                                  | 0        |                   | 默认：重构优先；确属边界再行级 allow |             |
| `src/scalim/spec/ir/sources.py`          | Spec IR：`sources`（符号: `KeyIr`, `NormalizeCallByContext`, `SourceNormalizeProjectFieldRuleIr`）                | 12   | "Mapping[object, object]"×3; "Mapping[Hashable, object]"×3 | 0        |                   | 默认：重构优先；确属边界再行级 allow |             |


## src/scalim/utils


| 文件                                         | 主要用途                                                                           | cast | cast types(top)                                | no-cover | no-cover patterns | cast 推荐                        | no-cover 推荐 |
| ------------------------------------------ | ------------------------------------------------------------------------------ | ---- | ---------------------------------------------- | -------- | ----------------- | ------------------------------ | ----------- |
| `src/scalim/utils/converters.py`           | Utils：`converters`（符号: `NamedLookupCast`, `to_int`, `to_str`）                  | 0    |                                                | 0        |                   | ✅ 已重构移除 cast（通过 `TypeGuard` 做运行时窄化） |             |
| `src/scalim/utils/json_like.py`            | Utils：`json_like`（符号: `ensure_json_like`）                                      | 0    |                                                    | 0        |                   | ✅ 已重构移除 cast（通过 `TypeGuard` 做运行时窄化） |             |
| `src/scalim/utils/relation_diagnostics.py` | Utils：`relation_diagnostics`（符号: `TypeMismatchWarning`, `RelationDiagnostics`） | 0    |                                                | 0        |                   | ✅ 已重构移除 cast（通过 `TypeGuard` 做运行时窄化） |             |


## src/scalim/workflow


| 文件                                           | 主要用途                                                                                                                                                        | cast | cast types(top)                                   | no-cover | no-cover patterns | cast 推荐               | no-cover 推荐                             |
| -------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- | ------------------------------------------------- | -------- | ----------------- | --------------------- | --------------------------------------- |
| `src/scalim/workflow/execute.py`             | Workflow：`execute`（符号: `_CompilationLike`, `WorkflowArtifactsDirectory`, `_workflow_error_diff`）                                                            | 20   | "Any"×8; "Dict[str, object]"×3                    | 21       | plain×21          | 默认：重构优先；确属边界再行级 allow | 默认：补测试移除为主；确属不可达/防御分支则行级 allow-no-cover |
| `src/scalim/workflow/loaders.py`             | Workflow：`loaders`（符号: `_WorkflowLoaderContext`, `workflow_loader_context`, `_require_context`）                                                             | 0    |                                                   | 0        |                   | ✅ 已重构移除 cast（`TypeGuard` + `isinstance` 收紧线程局部上下文/参数） |                                         |
| `src/scalim/workflow/resources_base.py`      | Workflow 资源读写：`resources_base`（符号: `WorkflowResourceWaitDiagnostics`, `WorkflowWriteError`, `_clone_exception_for_reraise`）                                 | 0    |                                                   | 8        | plain×8           |                       | 默认：补测试移除为主；确属不可达/防御分支则行级 allow-no-cover |
| `src/scalim/workflow/resources_csv.py`       | Workflow 资源读写：`resources_csv`（符号: `_read_csv_header`, `_iter_csv_rows`, `_describe_header_diff`）                                                            | 5    | "_CsvPlan"×4; "List[_AppendSegment]"×1            | 0        |                   | 默认：重构优先；确属边界再行级 allow |                                         |
| `src/scalim/workflow/resources_sheetbook.py` | Workflow 资源读写：`resources_sheetbook`（符号: `_materialize_aligned_csv_columns`, `_write_sheetbook_plan_to_openpyxl_workbook`, `_save_openpyxl_workbook_atomic`） | 7    | "Any"×2; "_SheetBookPlan"×2                       | 7        | plain×7           | 默认：重构优先；确属边界再行级 allow | 默认：补测试移除为主；确属不可达/防御分支则行级 allow-no-cover |
| `src/scalim/workflow/resources_workbook.py`  | Workflow 资源读写：`resources_workbook`（符号: `_get_openpyxl_workbook_class`, `_best_effort_close_write_only_workbook_worksheets`, `_SheetPlan`）                   | 5    | "_WorkbookPlan"×4; "Any"×1                        | 2        | plain×2           | 默认：重构优先；确属边界再行级 allow | 默认：补测试移除为主；确属不可达/防御分支则行级 allow-no-cover |


## tests


| 文件                                         | 主要用途                                                                                                                                                                                                              | cast | cast types(top)             | no-cover | no-cover patterns | cast 推荐                      | no-cover 推荐          |
| ------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- | --------------------------- | -------- | ----------------- | ---------------------------- | -------------------- |
| `tests/test_adaptive_execution_tuning.py`  | 测试用例：`test_adaptive_execution_tuning`（符号: `_noop_loader`, `_main_loader`, `_loader_a`）                                                                                                                            | 2    | "LoadRefOperatorIr"×2       | 0        |                   | 允许（低优先级）；优先改夹具类型，必要时行级 allow |                      |
| `tests/test_adaptive_loadref_scheduler.py` | 测试用例：`test_adaptive_loadref_scheduler`（符号: `test_resolve_adaptive_max_workers_auto_uses_cpu_count`, `test_build_layers_splits_layers_and_falls_back_on_cycle`, `_make_loadref_op`）                                | 1    | "LoadRefOperatorIr"×1       | 0        |                   | 允许（低优先级）；优先改夹具类型，必要时行级 allow |                      |
| `tests/test_deterministic_ordering.py`     | 测试用例：`test_deterministic_ordering`（符号: `test_topological_sort_empty_returns_empty`, `test_topological_sort_stable_tie_break_for_same_layer_nodes`, `test_topological_sort_non_string_nodes_use_stable_tie_break`） | 0    |                             | 3        | plain×3           |                              | 重构/补测试优先；必要时行级 allow |
| `tests/test_yaml_dsl_workflow.py`          | 测试用例：`test_yaml_dsl_workflow`（符号: `_WorkflowEventRecorder`, `_write_text`, `_cache_pool_config`）                                                                                                                  | 8    | "Any"×3; "Dict[str, Any]"×3 | 0        |                   | 允许（低优先级）；优先改夹具类型，必要时行级 allow |                      |
| `tests/testing_utils.py`                   | 测试用例：`testing_utils`（符号: `InlineExecutor`, `NoOpLoadRefExecutor`, `RecordingLoadRefExecutor`）                                                                                                                     | 1    | "LoadRefOperatorIr"×1       | 0        |                   | 允许（低优先级）；优先改夹具类型，必要时行级 allow |                      |
