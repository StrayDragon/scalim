## 0. 调研
- [ ] 0.1 盘点当前输出路径(批量/行式/列式)的可用事件与数据粒度,确认可用于增量聚合的输入范围.(产出: 事件/数据粒度清单)
- [ ] 0.2 梳理现有输出容器的能力边界(是否支持多逻辑输出/多 sheet),明确抽象层需求.(产出: 容器能力矩阵与缺口列表)
- [ ] 0.3 评估并行模式与 hook 隔离对派生输出一致性的影响,形成约束清单.(产出: 并行一致性约束与建议)
- [ ] 0.4 汇总可增量/不可增量指标类别与推荐计算路径.(产出: 指标分类表与策略建议)

## 1. 规范增量
- [ ] 1.1 起草 output-composition 规范增量(多输出目标、容器映射、命名与失败策略).
- [ ] 1.2 起草 derived-outputs 规范增量(增量聚合、来源选择、收尾输出、IR-only 配置).
- [ ] 1.3 补充业务示例与验收场景(含订单明细→供应商汇总,以及其它示例).

## 2. 设计
- [ ] 2.1 对比方案(同跑增量聚合/二阶段后处理/离线工具),给出推荐与理由.
- [ ] 2.2 定义状态管理与内存上限策略(批次累计、溢出/落盘、失败策略).
- [ ] 2.3 明确多输出写入顺序与一致性规则,以及失败策略对输出顺序的影响.

## 3. 实施(提案批准后)
- [ ] 3.1 增加输出组合与派生输出的核心对象/接口,并评估对单输出路径的兼容性影响.
- [ ] 3.2 增加同一容器多逻辑输出的支持(例如多 sheet 或分区写入).
- [ ] 3.3 增加增量聚合器与示例聚合函数(如 group-by/ratio).
- [ ] 3.4 补充单元与集成测试(多输出、派生输出、失败策略、并行限制).

## 4. 验证
- [ ] 4.1 更新用户文档与示例(仅 IR/Python 方式).
- [ ] 4.2 运行 openspec validate add-derived-outputs --strict --no-interactive
