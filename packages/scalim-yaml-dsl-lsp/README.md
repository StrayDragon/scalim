# scalim-yaml-dsl-lsp

Editor/LSP shared core for `scalim` YAML DSL:

- project discovery
- diagnostics (library-only; no CLI subprocess)
- Python reference static resolution (definition/hover/completion)

This package requires Python 3.10+.

