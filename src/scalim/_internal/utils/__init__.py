"""内部工具模块(非稳定入口).

治理约定:
- 仅放置跨内部实现复用的小型辅助工具(纯函数/小数据结构);禁止放置领域逻辑与业务流程.
- 依赖方向: 内部模块可以依赖此处;此处不得反向依赖上层领域模块(例如 `dsl`/`workflow`/`execution`),避免循环依赖.
- 迁移策略: 一旦某工具需要成为稳定契约或被用户材料引用,应迁移到对应领域模块并同步替换内部引用(不保留长期兼容层).
"""

__all__ = ()
