{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://scalim.invalid/schemas/scalim_yaml.json",
  "title": "Scalim 项目配置(scalim.yaml)",
  "description": "Scalim 项目级配置文件 `scalim.yaml` 定义 Schema",
  "$comment": "自动生成, 请勿手动修改. 生成脚本: scripts/gen-yaml-dsl-schema.py",
  "oneOf": [
    {
      "type": "null"
    },
    {
      "$ref": "#/definitions/scalim_yaml"
    }
  ],
  "definitions": {
    "scalim_yaml_lsp_kind_override": {
      "type": "object",
      "properties": {
        "glob": {
          "type": "string",
          "description": "按文件路径覆盖 YAML 类型(demand/workflow), glob 相对 project root",
          "markdownDescription": "按文件路径覆盖 YAML 类型(demand/workflow), glob 相对 `project root`.",
          "minLength": 1,
          "examples": [
            "workflow/*.yaml"
          ]
        },
        "kind": {
          "type": "string",
          "description": "YAML 类型覆盖(demand/workflow)",
          "markdownDescription": "YAML 类型覆盖.\n\n- 允许值: `demand` / `workflow`",
          "enum": [
            "demand",
            "workflow"
          ],
          "examples": [
            "workflow"
          ]
        }
      },
      "required": [
        "glob",
        "kind"
      ],
      "additionalProperties": false
    },
    "scalim_yaml_lsp": {
      "type": "object",
      "properties": {
        "python_roots": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "可选: 用于静态解析 Python 引用的搜索根(相对 scalim.yaml 所在目录)",
          "markdownDescription": "用于静态解析 Python 引用的搜索根(相对 `scalim.yaml` 所在目录).",
          "default": [],
          "examples": [
            [
              ".",
              "./src"
            ]
          ]
        },
        "kind_overrides": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/definitions/scalim_yaml_lsp_kind_override"
          },
          "description": "可选: 按文件路径覆盖 YAML 类型(demand/workflow), glob 相对 project root",
          "markdownDescription": "按文件路径覆盖 YAML 类型(demand/workflow), glob 相对 `project root`.",
          "default": [],
          "examples": [
            [
              {
                "glob": "workflow/*.yaml",
                "kind": "workflow"
              }
            ]
          ]
        }
      },
      "additionalProperties": false
    },
    "scalim_yaml_yaml_dsl": {
      "type": "object",
      "properties": {
        "import_roots": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/definitions/scalim_yaml_import_root"
          },
          "description": "可选: imports roots 注册表(目录注册 + alias 可选)",
          "markdownDescription": "imports roots 注册表.\n\n- 每个条目为 `{path: <dir>, alias?: <alias>}`\n- `path` 相对 `scalim.yaml` 所在目录\n- 当未显式提供 `allowed_yaml_roots` 时,这些 roots 会扩展 imports 默认 allow-roots\n- 当条目提供 `alias` 时,可在 `imports.*` 中使用 `@/x.yaml` 或 `<alias>:/x.yaml`",
          "default": [],
          "examples": [
            [
              {
                "path": "./fragments",
                "alias": "fragments"
              },
              {
                "path": "./shared_yaml"
              }
            ]
          ]
        },
        "lsp": {
          "oneOf": [
            {
              "$ref": "#/definitions/scalim_yaml_lsp"
            },
            {
              "type": "null"
            }
          ],
          "description": "可选: LSP project discovery 配置",
          "markdownDescription": "LSP project discovery 配置(可选).",
          "default": null
        }
      },
      "additionalProperties": false
    },
    "scalim_yaml": {
      "type": "object",
      "properties": {
        "yaml_dsl": {
          "oneOf": [
            {
              "$ref": "#/definitions/scalim_yaml_yaml_dsl"
            },
            {
              "type": "null"
            }
          ],
          "description": "YAML DSL 相关配置(可选)",
          "markdownDescription": "YAML DSL 相关配置(可选).",
          "default": null
        }
      },
      "additionalProperties": true
    }
  },
  "markdownDescription": "Scalim 项目级配置文件 `scalim.yaml` 定义 Schema.\n\n- `scalim.yaml` 本身是可选配置(支持 zero-config fallback)\n- schema 仅描述单个文件的结构,不改变 nearest-wins project discovery 语义\n- v1 聚焦 `yaml_dsl.*` 配置面(imports/lsp project discovery)"
}
