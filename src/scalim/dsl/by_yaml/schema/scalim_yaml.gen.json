{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://scalim.invalid/schemas/scalim_yaml.json",
  "title": "Scalim 项目配置(scalim.yaml)",
  "description": "Scalim 项目级配置文件 `scalim.yaml` 定义 Schema",
  "$comment": "自动生成, 请勿手动修改. 生成脚本: scripts/gen-yaml-dsl-schema.py",
  "oneOf": [
    {
      "type": "null"
    },
    {
      "$ref": "#/definitions/scalim_yaml"
    }
  ],
  "definitions": {
    "scalim_yaml_lsp_kind_override": {
      "type": "object",
      "properties": {
        "glob": {
          "type": "string",
          "description": "按文件路径覆盖 YAML 类型(demand/workflow), glob 相对 `project root`.",
          "markdownDescription": "#### yaml_dsl.lsp.kind_overrides[*].glob\n\n按文件路径覆盖 YAML 类型(demand/workflow), glob 相对 `project root`.",
          "minLength": 1,
          "examples": [
            "workflow/*.yaml"
          ]
        },
        "kind": {
          "type": "string",
          "description": "YAML 类型覆盖.",
          "markdownDescription": "#### yaml_dsl.lsp.kind_overrides[*].kind\n\nYAML 类型覆盖.\n\n- `demand`: 按 demand YAML 对待(使用 demand schema/语义)\n- `workflow`: 按 workflow YAML 对待(使用 workflow schema/语义)\n\n##### 字段约束\n- 必填: 是\n- 类型: string\n- 取值: `demand`, `workflow`\n\n##### 例子\n```yaml\nworkflow\n```",
          "enum": [
            "demand",
            "workflow"
          ],
          "examples": [
            "workflow"
          ]
        }
      },
      "required": [
        "glob",
        "kind"
      ],
      "additionalProperties": false
    },
    "scalim_yaml_lsp": {
      "type": "object",
      "properties": {
        "python_roots": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "用于静态解析 Python 引用的搜索根(相对 `scalim.yaml` 所在目录).",
          "markdownDescription": "#### yaml_dsl.lsp.python_roots\n\n用于静态解析 Python 引用的搜索根(相对 `scalim.yaml` 所在目录).",
          "default": [],
          "examples": [
            [
              ".",
              "./src"
            ]
          ]
        },
        "kind_overrides": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/definitions/scalim_yaml_lsp_kind_override"
          },
          "description": "按文件路径覆盖 YAML 类型(demand/workflow), glob 相对 `project root`.",
          "markdownDescription": "#### yaml_dsl.lsp.kind_overrides\n\n按文件路径覆盖 YAML 类型(demand/workflow), glob 相对 `project root`.",
          "default": [],
          "examples": [
            [
              {
                "glob": "workflow/*.yaml",
                "kind": "workflow"
              }
            ]
          ]
        }
      },
      "additionalProperties": false
    },
    "scalim_yaml_yaml_dsl": {
      "type": "object",
      "properties": {
        "import_roots": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/definitions/scalim_yaml_import_root"
          },
          "description": "imports roots 注册表.",
          "markdownDescription": "#### yaml_dsl.import_roots\n\nimports roots 注册表.\n- 每个条目为 `{path: <dir>, alias?: <alias>}`\n- `path` 相对 `scalim.yaml` 所在目录",
          "default": [],
          "examples": [
            [
              {
                "path": "./fragments",
                "alias": "fragments"
              },
              {
                "path": "./shared_yaml"
              }
            ]
          ]
        },
        "lsp": {
          "oneOf": [
            {
              "$ref": "#/definitions/scalim_yaml_lsp"
            },
            {
              "type": "null"
            }
          ],
          "description": "LSP project discovery 配置(可选).",
          "markdownDescription": "#### yaml_dsl.lsp\n\nLSP project discovery 配置(可选).",
          "default": null
        }
      },
      "additionalProperties": false
    },
    "scalim_yaml": {
      "type": "object",
      "properties": {
        "yaml_dsl": {
          "oneOf": [
            {
              "$ref": "#/definitions/scalim_yaml_yaml_dsl"
            },
            {
              "type": "null"
            }
          ],
          "description": "YAML DSL 相关配置(可选).",
          "markdownDescription": "#### yaml_dsl\n\nYAML DSL 相关配置(可选).",
          "default": null
        }
      },
      "additionalProperties": true
    }
  },
  "markdownDescription": "Scalim 项目级配置文件 `scalim.yaml` 定义 Schema.\n\n- `scalim.yaml` 本身是可选配置(支持 zero-config fallback)\n- schema 仅描述单个文件的结构,不改变 nearest-wins project discovery 语义\n- v1 聚焦 `yaml_dsl.*` 配置面(imports/lsp project discovery)"
}
