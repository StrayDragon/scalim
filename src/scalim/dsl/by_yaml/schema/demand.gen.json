{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://scalim.example.com/schemas/demand.json",
  "title": "Scalim 需求配置",
  "description": "Scalim 框架 YAML 需求配置定义 Schema",
  "$comment": "自动生成, 请勿手动修改. 生成脚本: scripts/gen-yaml-dsl-schema.py",
  "type": "object",
  "anyOf": [
    {
      "required": [
        "name",
        "main_source"
      ]
    },
    {
      "required": [
        "$import"
      ]
    }
  ],
  "properties": {
    "name": {
      "type": "string",
      "description": "需求配置名称",
      "markdownDescription": "需求配置名称.\n\n- 必填, 用于标识当前配置",
      "examples": [
        "order_report"
      ]
    },
    "imports": {
      "type": "object",
      "description": "片段文件导入别名映射(编译期展开)",
      "markdownDescription": "片段文件导入别名映射.\n\n- key: alias\n- value: 片段文件路径(字符串)\n- V1 仅支持同级文件名: `x.yaml|x.yml` 或 `./x.yaml|./x.yml`\n- 禁止: 绝对路径/父目录/子目录/alias 前缀",
      "propertyNames": {
        "type": "string",
        "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$"
      },
      "additionalProperties": {
        "type": "string",
        "pattern": "^(\\./)?[^/\\\\:]+\\.ya?ml$"
      }
    },
    "$import": {
      "oneOf": [
        {
          "type": "string"
        },
        {
          "type": "array",
          "items": {
            "type": "string"
          },
          "minItems": 1
        }
      ],
      "description": "$import 引用(支持 string 或 string list)",
      "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
      "examples": [
        "common.sources",
        [
          "common.sources",
          "other.sources"
        ]
      ]
    },
    "_templates": {
      "type": "object",
      "description": "YAML anchor 模板集合(_templates), 供 fields/relations 复用",
      "markdownDescription": "YAML anchor 模板集合.\n\n- 仅用于 YAML 复用(anchors)\n- 常用于 `fields` / `relations` / `retry`",
      "properties": {
        "retry": {
          "type": "object",
          "description": "可复用的 retry 策略模板集合",
          "markdownDescription": "可复用的 retry 策略模板集合.\n\n- key 为模板名\n- value 为 retry policy 对象",
          "additionalProperties": {
            "$ref": "#/definitions/loader_retry"
          }
        }
      },
      "additionalProperties": true
    },
    "description": {
      "type": "string",
      "description": "配置描述",
      "markdownDescription": "配置描述(可选)."
    },
    "batch_size": {
      "oneOf": [
        {
          "type": "null"
        },
        {
          "type": "integer",
          "minimum": 1
        }
      ],
      "description": "批处理大小(null 或 >=1 的整数)",
      "markdownDescription": "批处理大小.\n\n- 未声明时使用默认值\n- `null` 表示禁用分批(单批执行)\n- `>=1` 的整数表示固定分批大小",
      "default": 1000,
      "examples": [
        null,
        1000
      ]
    },
    "retry": {
      "description": "Loader retry 策略(可选;默认关闭)",
      "markdownDescription": "Loader retry 策略.\n\n- 默认关闭: `enabled: false`\n- 启用后会对 loader 调用的瞬态错误做有限重试\n- 需要提供 `should_retry` 回调(安全引用),用于决定是否重试\n- 受硬上限保护: max_attempts<=5, max_elapsed_seconds<=20, max_delay_seconds<=5\n",
      "allOf": [
        {
          "$ref": "#/definitions/loader_retry"
        }
      ]
    },
    "main_source": {
      "description": "主数据源配置(必填: source_id/loader, 可选 params/order_by)",
      "markdownDescription": "主数据源配置.\n\n- 必填: `source_id`, `loader`\n- `source_id` 不能出现在 `sources` 中\n- `fields` 仅允许源字段(禁止 `compute`)\n- `order_by` 控制批次内写入顺序(字符串列表,`-` 前缀表示 desc)",
      "allOf": [
        {
          "$ref": "#/definitions/main_source"
        }
      ]
    },
    "sources": {
      "type": "object",
      "properties": {
        "$import": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ],
          "description": "$import 引用(支持 string 或 string list)",
          "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
          "examples": [
            "common.sources",
            [
              "common.sources",
              "other.sources"
            ]
          ]
        }
      },
      "description": "数据源配置映射, key 为 source_id",
      "markdownDescription": "数据源配置映射, key 为 `source_id`.\n\n- 每个 source 必填: `loader`, `key`\n- 不允许包含 `main_source.source_id`\n- `fields` 仅允许源字段(禁止 `compute`)",
      "additionalProperties": {
        "$ref": "#/definitions/source"
      },
      "minProperties": 0
    },
    "fields": {
      "type": "object",
      "description": "字段配置映射, key 为 field_id; 仅用于派生字段",
      "markdownDescription": "字段配置映射(仅用于派生字段).\n\n- 必须包含 `compute` 或 `call_by`\n- 不能与源字段同名(避免 source/derived 重名)\n- 支持 YAML anchor 复用",
      "additionalProperties": {
        "$ref": "#/definitions/field"
      }
    },
    "relations": {
      "type": "object",
      "properties": {
        "$import": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ],
          "description": "$import 引用(支持 string 或 string list)",
          "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
          "examples": [
            "common.sources",
            [
              "common.sources",
              "other.sources"
            ]
          ]
        }
      },
      "description": "命名关联关系映射(steps 模板),供字段通过 string ref/alias 复用",
      "markdownDescription": "命名关联关系映射(steps 模板).\n\n- 供 `fields.*.relation` 通过 string ref 或 YAML alias 复用\n- string ref: `relation: <relation_id>` 引用 `relations.<relation_id>`\n- alias 复用: `relation: *<anchor>` (YAML anchor)\n- steps 必须是等值关联链, 参考 `relation.steps`",
      "additionalProperties": {
        "$ref": "#/definitions/relation"
      }
    },
    "guardrails": {
      "description": "运行时护栏配置(可选;默认关闭)",
      "markdownDescription": "运行时护栏配置.\n\n- 默认关闭\n- 用于控制 loader/relations/compute 等运行期护栏策略",
      "allOf": [
        {
          "$ref": "#/definitions/guardrails"
        }
      ]
    },
    "outputs": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/output_target"
      },
      "description": "输出目标列表(多 sheet 分发 + 派生汇总)",
      "markdownDescription": "输出目标列表(有序).\n\n- 通过 `where` 分发到不同 sheet\n- 通过 `aggregate` 声明派生汇总输出\n- 通过 `from` 复用字段集合与容器配置\n- 不再支持旧写法: 顶层 `output:`",
      "minItems": 0,
      "examples": [
        [
          {
            "name": "detail",
            "container": {
              "type": "workbook",
              "path": "./output/report.xlsx",
              "sheet": "明细"
            },
            "fields": [
              "order_id",
              "user_id"
            ]
          }
        ]
      ]
    },
    "failure_policy": {
      "type": "string",
      "description": "多输出失败策略(all_fail/primary_only)",
      "markdownDescription": "多输出失败策略.\n\n- `all_fail`: 任一目标失败即失败\n- `primary_only`: 非主输出失败将被禁用但不阻断主输出",
      "enum": [
        "all_fail",
        "primary_only"
      ],
      "default": "all_fail",
      "examples": [
        "all_fail"
      ]
    },
    "include_full_error_message": {
      "type": "boolean",
      "description": "包含完整错误信息(可能包含敏感信息;默认 false)",
      "markdownDescription": "包含完整错误信息(可能包含敏感信息;默认 false).",
      "default": false,
      "examples": [
        false
      ]
    },
    "meta": {
      "oneOf": [
        {
          "type": "boolean"
        },
        {
          "$ref": "#/definitions/output_extra_sheet"
        }
      ],
      "description": "可选:启用 meta sheet(写入运行信息与统计)",
      "markdownDescription": "可选:启用 meta sheet.\n\n- `true` 表示启用并使用默认配置\n- 对象形式可覆盖 sheet 名称与 workbook 路径",
      "examples": [
        true,
        {
          "sheet": "__meta__"
        }
      ]
    },
    "audit": {
      "oneOf": [
        {
          "type": "boolean"
        },
        {
          "$ref": "#/definitions/output_extra_sheet"
        }
      ],
      "description": "可选:启用 audit sheet(写入目标失败等审计信息)",
      "markdownDescription": "可选:启用 audit sheet.\n\n- `true` 表示启用并使用默认配置\n- 对象形式可覆盖 sheet 名称与 workbook 路径",
      "examples": [
        true,
        {
          "sheet": "__audit__"
        }
      ]
    },
    "observability": {
      "description": "可观测性配置",
      "markdownDescription": "可观测性配置.\n\n包含 `logging`、`performance`、`relations`、`viz`、`trace`、`row_gap` 与 `memory_opt` 子配置.",
      "allOf": [
        {
          "$ref": "#/definitions/observability"
        }
      ]
    }
  },
  "definitions": {
    "main_source": {
      "type": "object",
      "properties": {
        "source_id": {
          "type": "string",
          "description": "主数据源的 source_id",
          "markdownDescription": "主数据源的 `source_id`.\n\n- 必填\n- 不能与 `sources` 的 key 重复"
        },
        "loader": {
          "type": "string",
          "description": "Python 可调用对象引用(支持绝对/相对模块引用;支持点式/类式)",
          "markdownDescription": "Python 可调用对象引用.\n\n绝对引用:\n- 点式引用: `module.path.function`\n- 类式引用: `module.path:ClassName` / `module.path:obj.method`\n\n相对引用:\n- 以 `.` / `..` 开头的模块路径,相对 YAML 文件所在目录对应的模块路径\n- 运行期会先归一化为绝对引用,再做白名单校验\n\n示例:\n- `.loaders:load_orders`\n- `.loaders.load_orders`",
          "examples": [
            "myapp.loaders:load_orders"
          ]
        },
        "fields": {
          "type": "object",
          "properties": {
            "$import": {
              "oneOf": [
                {
                  "type": "string"
                },
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "minItems": 1
                }
              ],
              "description": "$import 引用(支持 string 或 string list)",
              "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
              "examples": [
                "common.sources",
                [
                  "common.sources",
                  "other.sources"
                ]
              ]
            }
          },
          "description": "主数据源字段配置映射, key 为 field_id",
          "markdownDescription": "主数据源字段配置映射.\n\n- 仅允许源字段(禁止 `compute`)\n- `source` 可省略或必须等于 `main_source.source_id`\n- 支持 YAML anchor 复用",
          "additionalProperties": {
            "$ref": "#/definitions/source_field_inline"
          },
          "minProperties": 0
        },
        "params": {
          "type": "object",
          "properties": {
            "$import": {
              "oneOf": [
                {
                  "type": "string"
                },
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "minItems": 1
                }
              ],
              "description": "$import 引用(支持 string 或 string list)",
              "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
              "examples": [
                "common.sources",
                [
                  "common.sources",
                  "other.sources"
                ]
              ]
            }
          },
          "description": "调用 loader 时透传的 kwargs 模板(支持 `{$init_var: <name>}`; sources 支持 `$keys/$rows`)",
          "markdownDescription": "调用 loader 时透传的 kwargs 模板.\n\n- `main_source.params`: 直接以 kwargs 传给 main source loader\n  - 仅允许静态值与 `{$init_var: <name>}` 指令节点(编译期解析为调用方提供的 `init_vars[<name>]`)\n  - 禁止 `$keys/$rows`\n- `sources.<id>.params`: loader kwargs 模板(在 ref loader 与 preload 阶段复用)\n  - 支持 `{$init_var: <name>}` 指令节点(单键映射;inline/block 等价;编译期解析为 `init_vars[<name>]`)\n  - `$keys`: 注入 lookup keys(支持 nested/list 位置)\n    - 形式: `{$keys: {as: set|list}}`(默认 set)\n    - composite key 注入为 tuple 元素\n  - `$rows`: 注入 batch rows(支持 nested/list 位置)\n    - 形式: `{$rows: {cache_mode: batch|none}}`(默认 batch)\n    - 注意: `$rows` 会触发 rows barrier(该层 LoadRef 串行执行)\n    - 注意: `cache_mode: batch` 会启用批次内复用(减少同 relation 多字段的重复调用);大 batch 下构造 `batch_rows` 可能较重\n    - 内存提示: 系统不会将完整 `batch_rows` 快照存入长生命周期缓存(避免驻留放大)\n      若 loader 有副作用或依赖可变 `batch_rows`,请用 `cache_mode: none`\n- `cache_mode: preload_forever` 的 source:\n  - 预加载阶段会复用同一份编译后的 `sources.<id>.params` 模板并透传 kwargs\n  - 禁止 `$keys/$rows`\n\n迁移:\n- legacy `bind` / `to_bind` 已移除,请改用 `params` 模板中的 `$keys/$rows` 指令节点\n- legacy `{$runtime: <name>}` 指令节点已不支持,请改用 `{$init_var: <name>}`\n- legacy `$runtime.<name>` 字符串占位符已移除,请改用 `{$init_var: <name>}`",
          "additionalProperties": {}
        },
        "retry": {
          "description": "Loader retry 策略(可选;默认关闭)",
          "markdownDescription": "Loader retry 策略.\n\n- 默认关闭: `enabled: false`\n- 启用后会对 loader 调用的瞬态错误做有限重试\n- 需要提供 `should_retry` 回调(安全引用),用于决定是否重试\n- 受硬上限保护: max_attempts<=5, max_elapsed_seconds<=20, max_delay_seconds<=5\n",
          "allOf": [
            {
              "$ref": "#/definitions/loader_retry"
            }
          ]
        },
        "order_by": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "主数据源批次内排序字段列表(仅主数据源字段)",
          "markdownDescription": "主数据源批次内排序字段列表.\n\n- 每项为字段 id, 前缀 `-` 表示 desc\n- 未配置时保持 loader 原始顺序\n- 仅允许主数据源字段"
        },
        "$import": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ],
          "description": "$import 引用(支持 string 或 string list)",
          "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
          "examples": [
            "common.sources",
            [
              "common.sources",
              "other.sources"
            ]
          ]
        }
      },
      "anyOf": [
        {
          "required": [
            "source_id",
            "loader"
          ]
        },
        {
          "required": [
            "$import"
          ]
        }
      ],
      "additionalProperties": false
    },
    "source": {
      "type": "object",
      "properties": {
        "loader": {
          "type": "string",
          "description": "Python 可调用对象引用(支持绝对/相对模块引用;支持点式/类式)",
          "markdownDescription": "Python 可调用对象引用.\n\n绝对引用:\n- 点式引用: `module.path.function`\n- 类式引用: `module.path:ClassName` / `module.path:obj.method`\n\n相对引用:\n- 以 `.` / `..` 开头的模块路径,相对 YAML 文件所在目录对应的模块路径\n- 运行期会先归一化为绝对引用,再做白名单校验\n\n示例:\n- `.loaders:load_orders`\n- `.loaders.load_orders`",
          "examples": [
            "myapp.loaders:load_orders"
          ]
        },
        "key": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ],
          "description": "该 source loader 返回映射的 key 字段(支持复合键 tuple)",
          "markdownDescription": "该 source loader 返回映射的 key 字段.\n\n- 单字段: `key: order_id`\n- 复合键: `key: [region_id, institution_id]`",
          "examples": [
            "order_id",
            [
              "region_id",
              "institution_id"
            ]
          ]
        },
        "lookup_cast": {
          "type": "object",
          "required": [
            "name"
          ],
          "properties": {
            "name": {
              "type": "string",
              "enum": [
                "auto",
                "int",
                "str",
                "sep_first"
              ],
              "description": "转换名称(auto/int/str/sep_first)",
              "markdownDescription": "转换名称.\n\n- `auto`: 自动归一化\n- `int`: 转为 int\n- `str`: 转为 str\n- `sep_first`: 按 `sep` 截取首段再归一化",
              "examples": [
                "auto"
              ]
            },
            "sep": {
              "type": "string",
              "description": "sep_first 的分隔符(默认 ,)"
            }
          },
          "additionalProperties": false,
          "description": "归一化 lookup key 的转换(对象结构); sep_first 会先截取首段再做 auto_normalize_key, 例: {name: sep_first, sep: ','}",
          "markdownDescription": "归一化 lookup key 的转换.\n\n- `name`: auto / int / str / sep_first\n- `auto` 会拒绝 float lookup key(避免歧义,返回 None 并忽略该键);\n  若上游可能返回 float(例如 123.0/12.34),请用 `int`/`str` 显式归一化或在 loader 中修复\n- `sep_first` 先按 `sep` 截取首段再做 normalize"
        },
        "lookup_chunk_size": {
          "oneOf": [
            {
              "type": "integer",
              "minimum": 0
            },
            {
              "type": "null"
            }
          ],
          "description": "keys 模式 LoadRef 的 lookup_keys 分片大小(0/空表示不分片)",
          "markdownDescription": "keys 模式 lookup_keys 分片大小.\n\n- `0` / `null` 表示不分片"
        },
        "normalize": {
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "index_by_key",
                "take_first",
                "project_fields",
                "map_values"
              ],
              "description": "normalize 预置类型",
              "markdownDescription": "normalize 预置类型.\n\n- `index_by_key`: 将 `list[row]` 归一化为 `key -> row` 映射\n- `take_first`: 将 `mapping[key -> list[row]]` 归一化为 `mapping[key -> row]`(空列表由 `on_empty` 决定)\n- `project_fields`: 对 `mapping[key -> row]` 的 row value 做投影/重命名(支持 int-key path,可注入 `from_key`)\n- `map_values`: 对 `mapping` 的 values 批量应用 normalize steps(例如 take_first + project_fields)\n\n注意:\n- 顶层 `list[row]` 场景请用 `index_by_key`(通过 `on_conflict` 定义冲突策略)\n- `call_by` 为受控扩展点: whole-result `Mapping -> Mapping`(受 allowlist 约束)",
              "examples": [
                "index_by_key",
                "take_first",
                "project_fields",
                "map_values"
              ]
            },
            "key_field": {
              "type": "string",
              "description": "用于建立索引的 row 字段名(index_by_key)",
              "markdownDescription": "用于建立索引的 row 字段名.\n\n- 仅 `kind: index_by_key` 使用\n- 对应 `sources.<id>.key`",
              "examples": [
                "order_id"
              ]
            },
            "on_conflict": {
              "type": "string",
              "enum": [
                "error",
                "first",
                "last"
              ],
              "default": "error",
              "description": "duplicate key 冲突策略(error/first/last)",
              "markdownDescription": "duplicate key 冲突策略.\n\n- `error`: 报错(默认)\n- `first`: 保留第一条\n- `last`: 保留最后一条",
              "examples": [
                "error"
              ]
            },
            "on_empty": {
              "type": "string",
              "enum": [
                "miss",
                "null",
                "error"
              ],
              "default": "miss",
              "description": "空列表策略(miss/null/error)",
              "markdownDescription": "空列表策略.\n\n- `miss`: 该 key 视为 lookup miss(从结果映射中移除)\n- `null`: 写入 `None`\n- `error`: 抛错",
              "examples": [
                "miss"
              ]
            },
            "on_missing": {
              "type": "string",
              "enum": [
                "error",
                "null"
              ],
              "default": "error",
              "description": "缺失路径策略(error/null)",
              "markdownDescription": "缺失路径策略.\n\n- `error`: 抛错(默认)\n- `null`: 缺失则填 `None`",
              "examples": [
                "error"
              ]
            },
            "fields": {
              "type": "object",
              "minProperties": 1,
              "description": "投影字段映射(key 为输出字段名)",
              "markdownDescription": "投影字段映射.\n\n- key: 输出字段名(天然完成 rename)\n- value: 投影规则(`from_key` 或 `extract` 二选一)\n- `extract` 支持 int-key path(例如 `\"[1].x\"`)",
              "propertyNames": {
                "type": "string",
                "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$"
              },
              "additionalProperties": {
                "type": "object",
                "properties": {
                  "from_key": {
                    "type": "boolean",
                    "description": "将 lookup key 写入该字段",
                    "markdownDescription": "将 lookup key(外层 mapping 的 key)写入该字段.\n\n- true: 注入\n- false: 不注入",
                    "examples": [
                      true
                    ]
                  },
                  "extract": {
                    "type": "string",
                    "minLength": 1,
                    "description": "从当前 row value 提取字段值的路径表达式",
                    "markdownDescription": "从当前 key 对应的 row value 中提取字段值的路径表达式.\n\n- 语法与字段级 `extract` 一致: dot + bracket path\n- 支持 int-key: `\"[1].x\"`(表示 key=1,不是数组下标)\n- 缺失/路径不匹配时为 missing(由 `on_missing` 决定)",
                    "examples": [
                      "review_status",
                      "[1].clearn_reason_level"
                    ]
                  }
                },
                "additionalProperties": false,
                "allOf": [
                  {
                    "oneOf": [
                      {
                        "required": [
                          "from_key"
                        ],
                        "not": {
                          "required": [
                            "extract"
                          ]
                        }
                      },
                      {
                        "required": [
                          "extract"
                        ],
                        "not": {
                          "required": [
                            "from_key"
                          ]
                        }
                      }
                    ]
                  }
                ]
              }
            },
            "steps": {
              "type": "array",
              "items": {
                "type": "object",
                "required": [
                  "kind"
                ],
                "properties": {
                  "kind": {
                    "type": "string",
                    "enum": [
                      "take_first",
                      "project_fields"
                    ],
                    "description": "normalize step 类型(take_first/project_fields)",
                    "markdownDescription": "normalize step 类型.\n\n- `take_first`\n- `project_fields`",
                    "examples": [
                      "take_first"
                    ]
                  },
                  "on_empty": {
                    "type": "string",
                    "enum": [
                      "miss",
                      "null",
                      "error"
                    ],
                    "default": "miss",
                    "description": "空列表策略(miss/null/error)",
                    "markdownDescription": "空列表策略.\n\n- `miss`: 该 key 视为 lookup miss(从结果映射中移除)\n- `null`: 写入 `None`\n- `error`: 抛错",
                    "examples": [
                      "miss"
                    ]
                  },
                  "on_missing": {
                    "type": "string",
                    "enum": [
                      "error",
                      "null"
                    ],
                    "default": "error",
                    "description": "缺失路径策略(error/null)",
                    "markdownDescription": "缺失路径策略.\n\n- `error`: 抛错(默认)\n- `null`: 缺失则填 `None`",
                    "examples": [
                      "error"
                    ]
                  },
                  "fields": {
                    "type": "object",
                    "minProperties": 1,
                    "description": "投影字段映射(key 为输出字段名)",
                    "markdownDescription": "投影字段映射.\n\n- key: 输出字段名(天然完成 rename)\n- value: 投影规则(`from_key` 或 `extract` 二选一)\n- `extract` 支持 int-key path(例如 `\"[1].x\"`)",
                    "propertyNames": {
                      "type": "string",
                      "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$"
                    },
                    "additionalProperties": {
                      "type": "object",
                      "properties": {
                        "from_key": {
                          "type": "boolean",
                          "description": "将 lookup key 写入该字段",
                          "markdownDescription": "将 lookup key(外层 mapping 的 key)写入该字段.\n\n- true: 注入\n- false: 不注入",
                          "examples": [
                            true
                          ]
                        },
                        "extract": {
                          "type": "string",
                          "minLength": 1,
                          "description": "从当前 row value 提取字段值的路径表达式",
                          "markdownDescription": "从当前 key 对应的 row value 中提取字段值的路径表达式.\n\n- 语法与字段级 `extract` 一致: dot + bracket path\n- 支持 int-key: `\"[1].x\"`(表示 key=1,不是数组下标)\n- 缺失/路径不匹配时为 missing(由 `on_missing` 决定)",
                          "examples": [
                            "review_status",
                            "[1].clearn_reason_level"
                          ]
                        }
                      },
                      "additionalProperties": false,
                      "allOf": [
                        {
                          "oneOf": [
                            {
                              "required": [
                                "from_key"
                              ],
                              "not": {
                                "required": [
                                  "extract"
                                ]
                              }
                            },
                            {
                              "required": [
                                "extract"
                              ],
                              "not": {
                                "required": [
                                  "from_key"
                                ]
                              }
                            }
                          ]
                        }
                      ]
                    }
                  }
                },
                "additionalProperties": false,
                "allOf": [
                  {
                    "oneOf": [
                      {
                        "required": [
                          "kind"
                        ],
                        "properties": {
                          "kind": {
                            "enum": [
                              "take_first"
                            ]
                          }
                        },
                        "not": {
                          "anyOf": [
                            {
                              "required": [
                                "on_missing"
                              ]
                            },
                            {
                              "required": [
                                "fields"
                              ]
                            }
                          ]
                        }
                      },
                      {
                        "required": [
                          "kind",
                          "fields"
                        ],
                        "properties": {
                          "kind": {
                            "enum": [
                              "project_fields"
                            ]
                          }
                        },
                        "not": {
                          "anyOf": [
                            {
                              "required": [
                                "on_empty"
                              ]
                            }
                          ]
                        }
                      }
                    ]
                  }
                ]
              },
              "minItems": 1,
              "description": "按顺序执行的 normalize steps",
              "markdownDescription": "按顺序执行的 normalize steps.\n\n- 对每个 `(key, value)` 依次执行"
            },
            "call_by": {
              "type": "string",
              "description": "Normalize 受控扩展点(安全引用,由 allowlist 约束)",
              "markdownDescription": "Normalize 受控扩展点(可选).\n\n- 形式与 `loader` 引用一致(支持绝对/相对引用)\n- 相对引用会在运行期先归一化为绝对引用,再做 allowlist 校验\n- 固定 contract: 输入与输出均为 `Mapping`\n\n建议签名:\n- `fn(result, ctx) -> Mapping`\n  - `ctx.source_id`\n  - `ctx.kind`\n  - `ctx.config_path`",
              "examples": [
                "myapp.normalizes:normalize_source_x"
              ]
            }
          },
          "additionalProperties": false,
          "allOf": [
            {
              "oneOf": [
                {
                  "required": [
                    "kind",
                    "key_field"
                  ],
                  "properties": {
                    "kind": {
                      "enum": [
                        "index_by_key"
                      ]
                    }
                  },
                  "not": {
                    "anyOf": [
                      {
                        "required": [
                          "on_empty"
                        ]
                      },
                      {
                        "required": [
                          "on_missing"
                        ]
                      },
                      {
                        "required": [
                          "fields"
                        ]
                      },
                      {
                        "required": [
                          "steps"
                        ]
                      }
                    ]
                  }
                },
                {
                  "required": [
                    "kind"
                  ],
                  "properties": {
                    "kind": {
                      "enum": [
                        "take_first"
                      ]
                    }
                  },
                  "not": {
                    "anyOf": [
                      {
                        "required": [
                          "key_field"
                        ]
                      },
                      {
                        "required": [
                          "on_conflict"
                        ]
                      },
                      {
                        "required": [
                          "on_missing"
                        ]
                      },
                      {
                        "required": [
                          "fields"
                        ]
                      },
                      {
                        "required": [
                          "steps"
                        ]
                      }
                    ]
                  }
                },
                {
                  "required": [
                    "kind",
                    "fields"
                  ],
                  "properties": {
                    "kind": {
                      "enum": [
                        "project_fields"
                      ]
                    }
                  },
                  "not": {
                    "anyOf": [
                      {
                        "required": [
                          "key_field"
                        ]
                      },
                      {
                        "required": [
                          "on_conflict"
                        ]
                      },
                      {
                        "required": [
                          "on_empty"
                        ]
                      },
                      {
                        "required": [
                          "steps"
                        ]
                      }
                    ]
                  }
                },
                {
                  "required": [
                    "kind",
                    "steps"
                  ],
                  "properties": {
                    "kind": {
                      "enum": [
                        "map_values"
                      ]
                    }
                  },
                  "not": {
                    "anyOf": [
                      {
                        "required": [
                          "key_field"
                        ]
                      },
                      {
                        "required": [
                          "on_conflict"
                        ]
                      },
                      {
                        "required": [
                          "on_empty"
                        ]
                      },
                      {
                        "required": [
                          "on_missing"
                        ]
                      },
                      {
                        "required": [
                          "fields"
                        ]
                      }
                    ]
                  }
                }
              ]
            }
          ],
          "description": "源代码级整体结果 `normalize`(在字段级 `extract` 之前对 `loader` 整体返回值整形)",
          "markdownDescription": "`normalize` 是源代码级的整体结果整形.\n\n- 作用于 `loader` 的整个返回值,用于把整体结果整理成更适合字段读取的形状\n- 执行时机: 在字段级 `extract` 之前\n- 常见用法: 当 `loader` 返回 `list[row]` 时,用 `index_by_key` 归一化为 `key -> row`\n\n示例:\n```yaml\nnormalize:\n  kind: index_by_key\n  key_field: order_id\n  on_conflict: error\n```\n\n输入/输出形状:\n- 输入: `[{order_id: 101, ...}, ...]`\n- 输出: `{101: {order_id: 101, ...}, ...}`\n\n注意:\n- 若只是 `row` 内字段嵌套取值,请使用字段级 `extract`"
        },
        "cache_mode": {
          "type": "string",
          "description": "缓存模式:none=不缓存,preload_forever=预加载永久缓存",
          "markdownDescription": "缓存模式.\n\n- `none`: 不缓存\n- `preload_forever`: 预加载并长期缓存\n- 设为 `preload_forever` 时,预加载阶段会执行一次 loader 并将结果缓存\n- 预加载阶段同样会复用编译后的 `sources.<id>.params` kwargs 模板(禁用 `$keys/$rows`)\n- `preload_forever` 场景禁止在 `params` 中使用 `$keys/$rows`",
          "enum": [
            "none",
            "preload_forever"
          ],
          "default": "none"
        },
        "retry": {
          "description": "Loader retry 策略(可选;默认关闭)",
          "markdownDescription": "Loader retry 策略.\n\n- 默认关闭: `enabled: false`\n- 启用后会对 loader 调用的瞬态错误做有限重试\n- 需要提供 `should_retry` 回调(安全引用),用于决定是否重试\n- 受硬上限保护: max_attempts<=5, max_elapsed_seconds<=20, max_delay_seconds<=5\n",
          "allOf": [
            {
              "$ref": "#/definitions/loader_retry"
            }
          ]
        },
        "fields": {
          "type": "object",
          "properties": {
            "$import": {
              "oneOf": [
                {
                  "type": "string"
                },
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "minItems": 1
                }
              ],
              "description": "$import 引用(支持 string 或 string list)",
              "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
              "examples": [
                "common.sources",
                [
                  "common.sources",
                  "other.sources"
                ]
              ]
            }
          },
          "description": "数据源字段配置映射, key 为 field_id",
          "markdownDescription": "数据源字段配置映射.\n\n- 仅允许源字段(禁止 `compute`)\n- `source` 可省略或必须等于当前 `source_id`\n- 支持 YAML anchor 复用",
          "additionalProperties": {
            "$ref": "#/definitions/source_field_inline"
          },
          "minProperties": 0
        },
        "params": {
          "type": "object",
          "properties": {
            "$import": {
              "oneOf": [
                {
                  "type": "string"
                },
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "minItems": 1
                }
              ],
              "description": "$import 引用(支持 string 或 string list)",
              "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
              "examples": [
                "common.sources",
                [
                  "common.sources",
                  "other.sources"
                ]
              ]
            }
          },
          "description": "调用 loader 时透传的 kwargs 模板(支持 `{$init_var: <name>}`; sources 支持 `$keys/$rows`)",
          "markdownDescription": "调用 loader 时透传的 kwargs 模板.\n\n- `main_source.params`: 直接以 kwargs 传给 main source loader\n  - 仅允许静态值与 `{$init_var: <name>}` 指令节点(编译期解析为调用方提供的 `init_vars[<name>]`)\n  - 禁止 `$keys/$rows`\n- `sources.<id>.params`: loader kwargs 模板(在 ref loader 与 preload 阶段复用)\n  - 支持 `{$init_var: <name>}` 指令节点(单键映射;inline/block 等价;编译期解析为 `init_vars[<name>]`)\n  - `$keys`: 注入 lookup keys(支持 nested/list 位置)\n    - 形式: `{$keys: {as: set|list}}`(默认 set)\n    - composite key 注入为 tuple 元素\n  - `$rows`: 注入 batch rows(支持 nested/list 位置)\n    - 形式: `{$rows: {cache_mode: batch|none}}`(默认 batch)\n    - 注意: `$rows` 会触发 rows barrier(该层 LoadRef 串行执行)\n    - 注意: `cache_mode: batch` 会启用批次内复用(减少同 relation 多字段的重复调用);大 batch 下构造 `batch_rows` 可能较重\n    - 内存提示: 系统不会将完整 `batch_rows` 快照存入长生命周期缓存(避免驻留放大)\n      若 loader 有副作用或依赖可变 `batch_rows`,请用 `cache_mode: none`\n- `cache_mode: preload_forever` 的 source:\n  - 预加载阶段会复用同一份编译后的 `sources.<id>.params` 模板并透传 kwargs\n  - 禁止 `$keys/$rows`\n\n迁移:\n- legacy `bind` / `to_bind` 已移除,请改用 `params` 模板中的 `$keys/$rows` 指令节点\n- legacy `{$runtime: <name>}` 指令节点已不支持,请改用 `{$init_var: <name>}`\n- legacy `$runtime.<name>` 字符串占位符已移除,请改用 `{$init_var: <name>}`",
          "additionalProperties": {}
        },
        "$import": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ],
          "description": "$import 引用(支持 string 或 string list)",
          "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
          "examples": [
            "common.sources",
            [
              "common.sources",
              "other.sources"
            ]
          ]
        }
      },
      "anyOf": [
        {
          "required": [
            "loader",
            "key"
          ]
        },
        {
          "required": [
            "$import"
          ]
        }
      ],
      "additionalProperties": false
    },
    "source_field_inline": {
      "type": "object",
      "properties": {
        "source": {
          "type": "string",
          "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
          "description": "字段来源的 source_id (例: source: orders)",
          "markdownDescription": "字段来源的 `source_id`.\n\n- 在 `main_source.fields` / `sources.<id>.fields` 中可省略\n- 若显式提供, 必须与容器 `source_id` 一致"
        },
        "extract": {
          "type": "string",
          "minLength": 1,
          "description": "从当前 key 对应的 row value 中提取字段值的路径表达式(不是相对整个 loader-result mapping).",
          "markdownDescription": "从当前 key 对应的 row value 中提取字段值的路径表达式(不是相对整个 loader-result mapping).\n\n语法: dot + bracket path(建议写成字符串,避免 YAML 歧义):\n- `extract` 省略时,等价于 `extract: <field_id>`(顶层同名 key)\n- dot: `a.b.c`\n- int-key: `\"[1].clearn_reason_level\"` (表示 key=1,不是 list index)\n- 字面量 string key: `'[\"a.b\"].x'` (用于 key 本身包含点号等特殊字符)\n\n注意:\n- 不做 `\"1\"` ↔ `1` 的隐式转换(避免歧义)\n- 不支持数组/列表下标语义: `[1]` 永远表示 “key=1”\n- 缺失/路径不匹配时返回 `None`\n\n示例(含嵌套取值):\n\n```yaml\nmain_source:\n  fields:\n    # 顶层同名 key: extract 可省略\n    review_status:\n      name: 审核状态\n\n    # int-key nested dict: role_id 是 int(例如 1/2)\n    customer_clearn_reason_level:\n      name: 客户净利原因等级\n      extract: \"[1].clearn_reason_level\"\n\n    # dotted literal key: row 的 key 就叫 \"a.b\"\n    dotted_literal_key_value:\n      extract: '[\"a.b\"].x'\n```\n\n示例(给定 loader 返回值,extract 提取后的输出长什么样):\n\n```python\nresult = {\n  1: {1: {\"clearn_reason_level\": 2}, 2: {\"clearn_reason_level\": 1}, \"review_status\": 0},\n}\n```\n\n字段:\n\n```yaml\nsources:\n  clearn_reasons:\n    fields:\n      customer_level:\n        extract: \"[1].clearn_reason_level\"\n      operation_level:\n        extract: \"[2].clearn_reason_level\"\n      review_status:\n        extract: review_status\n```\n\n对 `lookup_key=1` 的 row value:\n- `customer_level` → `2`\n- `operation_level` → `1`\n- `review_status` → `0`\n\n最终输出行片段:\n\n```python\n{\"customer_level\": 2, \"operation_level\": 1, \"review_status\": 0}\n```\n\n补充边界:\n- 如果中间段是 list/tuple, `\"[1]\"` 也不会当作下标(会返回 `None`)\n- 如果同时存在 key `\"1\"` 与 `1`,需要用 `extract: '[\"1\"].x'` 与 `extract: \"[1].x\"` 明确区分\n",
          "examples": [
            "review_status",
            "CustomerMark.clearn_reason_level",
            "[1].clearn_reason_level",
            "[\"a.b\"].x"
          ]
        },
        "name": {
          "type": "string",
          "description": "字段显示名称",
          "markdownDescription": "字段显示名称.\n\n- `output.header_fields_output_by: name` 时作为表头"
        },
        "relation": {
          "oneOf": [
            {
              "type": "string",
              "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
              "description": "relation 引用(字符串 ref),示例: relation: orders_to_customers",
              "markdownDescription": "relation 引用(字符串 ref).\n\n- 形式: `relation: <relation_id>`\n- 表示引用 `relations.<relation_id>`\n- full validate 会校验 `relations.<relation_id>` 存在"
            },
            {
              "type": "object",
              "required": [
                "steps"
              ],
              "properties": {
                "steps": {
                  "type": "array",
                  "items": {
                    "type": "object",
                    "required": [
                      "from",
                      "to"
                    ],
                    "properties": {
                      "from": {
                        "oneOf": [
                          {
                            "type": "string",
                            "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*\\.[a-zA-Z_][a-zA-Z0-9_]*$",
                            "description": "格式: source.field (source 与 field 使用同样命名规则)",
                            "markdownDescription": "格式: `source.field`.\n\n- 仅允许字母/数字/下划线\n- `field` 必须是字段的 `field_id`(YAML key)\n- 不允许写 loader 的 data_key; 若 field_id != data_key,仍必须在 steps 中写 field_id\n- 对于非主源的 key 字段,也可直接引用 `sources.<id>.key` 中声明的字段名\n- 系统会将 `field_id` 映射为其 `field`(data_key)"
                          },
                          {
                            "type": "array",
                            "items": {
                              "type": "string",
                              "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*\\.[a-zA-Z_][a-zA-Z0-9_]*$",
                              "description": "格式: source.field (source 与 field 使用同样命名规则)",
                              "markdownDescription": "格式: `source.field`.\n\n- 仅允许字母/数字/下划线\n- `field` 必须是字段的 `field_id`(YAML key)\n- 不允许写 loader 的 data_key; 若 field_id != data_key,仍必须在 steps 中写 field_id\n- 对于非主源的 key 字段,也可直接引用 `sources.<id>.key` 中声明的字段名\n- 系统会将 `field_id` 映射为其 `field`(data_key)"
                            },
                            "minItems": 1
                          }
                        ],
                        "description": "上游字段(等值条件左侧),格式 source.field; 多字段用列表",
                        "markdownDescription": "上游字段(等值条件左侧).\n\n- `source.field` 或列表\n- `field` 必须是字段的 `field_id`(YAML key),不允许写 loader data_key\n- 列表用于复合键, 长度需与 `to` 对齐\n- 同一侧必须来自同一 source\n\n常见错误:\n- ❌ 写 data_key: `products.category_id`\n- ✅ 写 field_id: `products.product_category_id` (schema 会映射到 data_key)",
                        "examples": [
                          "orders.customer_id",
                          [
                            "orders.region_id",
                            "orders.institution_id"
                          ]
                        ]
                      },
                      "to": {
                        "oneOf": [
                          {
                            "type": "string",
                            "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*\\.[a-zA-Z_][a-zA-Z0-9_]*$",
                            "description": "格式: source.field (source 与 field 使用同样命名规则)",
                            "markdownDescription": "格式: `source.field`.\n\n- 仅允许字母/数字/下划线\n- `field` 必须是字段的 `field_id`(YAML key)\n- 不允许写 loader 的 data_key; 若 field_id != data_key,仍必须在 steps 中写 field_id\n- 对于非主源的 key 字段,也可直接引用 `sources.<id>.key` 中声明的字段名\n- 系统会将 `field_id` 映射为其 `field`(data_key)"
                          },
                          {
                            "type": "array",
                            "items": {
                              "type": "string",
                              "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*\\.[a-zA-Z_][a-zA-Z0-9_]*$",
                              "description": "格式: source.field (source 与 field 使用同样命名规则)",
                              "markdownDescription": "格式: `source.field`.\n\n- 仅允许字母/数字/下划线\n- `field` 必须是字段的 `field_id`(YAML key)\n- 不允许写 loader 的 data_key; 若 field_id != data_key,仍必须在 steps 中写 field_id\n- 对于非主源的 key 字段,也可直接引用 `sources.<id>.key` 中声明的字段名\n- 系统会将 `field_id` 映射为其 `field`(data_key)"
                            },
                            "minItems": 1
                          }
                        ],
                        "description": "下游字段(等值条件右侧/lookup key),格式 source.field; 多字段用列表",
                        "markdownDescription": "下游字段(等值条件右侧/lookup key).\n\n- `source.field` 或列表\n- `field` 必须是字段的 `field_id`(YAML key),不允许写 loader data_key\n- 列表用于复合键, 长度需与 `from` 对齐\n- 同一侧必须来自同一 source\n\n常见错误:\n- ❌ 写 data_key: `products.category_id`\n- ✅ 写 field_id: `products.product_category_id` (schema 会映射到 data_key)",
                        "examples": [
                          "customers.customer_id",
                          [
                            "regions.region_id",
                            "mappings.institution_id"
                          ]
                        ]
                      },
                      "lookup_cast": {
                        "type": "object",
                        "required": [
                          "name"
                        ],
                        "properties": {
                          "name": {
                            "type": "string",
                            "enum": [
                              "auto",
                              "int",
                              "str",
                              "sep_first"
                            ],
                            "description": "转换名称(auto/int/str/sep_first)",
                            "markdownDescription": "转换名称.\n\n- `auto`: 自动归一化\n- `int`: 转为 int\n- `str`: 转为 str\n- `sep_first`: 按 `sep` 截取首段再归一化",
                            "examples": [
                              "auto"
                            ]
                          },
                          "sep": {
                            "type": "string",
                            "description": "sep_first 的分隔符(默认 ,)"
                          }
                        },
                        "additionalProperties": false,
                        "description": "归一化 lookup key 的转换(对象结构); sep_first 会先截取首段再做 auto_normalize_key, 例: {name: sep_first, sep: ','}",
                        "markdownDescription": "归一化 lookup key 的转换.\n\n- `name`: auto / int / str / sep_first\n- `auto` 会拒绝 float lookup key(避免歧义,返回 None 并忽略该键);\n  若上游可能返回 float(例如 123.0/12.34),请用 `int`/`str` 显式归一化或在 loader 中修复\n- `sep_first` 先按 `sep` 截取首段再做 normalize"
                      }
                    },
                    "additionalProperties": false
                  },
                  "minItems": 1,
                  "description": "按顺序定义等值关联链(from == to),系统不会重排 steps, 表示从 main_source 出发沿链路到达当前字段 source (例: steps: [{from: orders.customer_id, to: customers.customer_id}])",
                  "markdownDescription": "按顺序定义等值关联链(from == to).\n\n- `from`/`to` 支持 `source.field` 或列表\n- `field` 必须是字段的 `field_id`(YAML key),或 `sources.<id>.key` 中声明的 key 字段(不允许写 loader data_key)\n- 列表用于复合键, 长度需一致\n- 链路必须连续: steps[n].from 的 source = steps[n-1].to 的 source"
                }
              },
              "additionalProperties": false,
              "description": "内联 steps 或 alias 引用(需先定义),示例: relation: {steps: [...]}",
              "markdownDescription": "关系路径引用(内联 steps 或 YAML alias; alias 需先在 relations 定义).\n\n- `relation: *orders_to_customers`\n- `relation: {steps: [...]}`\n- steps 必须从 `main_source` 开始, 以当前 `source` 结束\n- 若 `source` 非 `main_source`, 且未显式 relation, 需确保 `relations` 中存在唯一链路"
            }
          ],
          "description": "关系路径(支持 string ref / steps 对象 / YAML alias; alias 需先定义),表示从 main_source 到当前字段 source 的等值关联链 (例: relation: orders_to_customers)",
          "markdownDescription": "关系路径(支持 string ref / steps 对象 / YAML alias; alias 需先定义),表示从 main_source 到当前字段 source 的等值关联链 (例: relation: orders_to_customers)"
        },
        "value_cast": {
          "type": "string",
          "description": "字段值转换(仅源字段),用于写入上下文/输出前的类型调整",
          "markdownDescription": "字段值转换(仅源字段).\n\n- `auto`: 自动转换\n- `int`: 转为 int\n- `str`: 转为 str",
          "enum": [
            "auto",
            "int",
            "str"
          ],
          "examples": [
            "auto"
          ]
        },
        "$import": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ],
          "description": "$import 引用(支持 string 或 string list)",
          "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
          "examples": [
            "common.sources",
            [
              "common.sources",
              "other.sources"
            ]
          ]
        }
      },
      "allOf": [
        {
          "not": {
            "required": [
              "call_by"
            ]
          }
        },
        {
          "not": {
            "required": [
              "field"
            ]
          }
        }
      ]
    },
    "field": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "source": {
          "type": "string",
          "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
          "description": "字段来源的 source_id (例: source: orders)",
          "markdownDescription": "字段来源的 `source_id`.\n\n- 在 `main_source.fields` / `sources.<id>.fields` 中可省略\n- 若显式提供, 必须与容器 `source_id` 一致"
        },
        "extract": {
          "type": "string",
          "minLength": 1,
          "description": "从当前 key 对应的 row value 中提取字段值的路径表达式(不是相对整个 loader-result mapping).",
          "markdownDescription": "从当前 key 对应的 row value 中提取字段值的路径表达式(不是相对整个 loader-result mapping).\n\n语法: dot + bracket path(建议写成字符串,避免 YAML 歧义):\n- `extract` 省略时,等价于 `extract: <field_id>`(顶层同名 key)\n- dot: `a.b.c`\n- int-key: `\"[1].clearn_reason_level\"` (表示 key=1,不是 list index)\n- 字面量 string key: `'[\"a.b\"].x'` (用于 key 本身包含点号等特殊字符)\n\n注意:\n- 不做 `\"1\"` ↔ `1` 的隐式转换(避免歧义)\n- 不支持数组/列表下标语义: `[1]` 永远表示 “key=1”\n- 缺失/路径不匹配时返回 `None`\n\n示例(含嵌套取值):\n\n```yaml\nmain_source:\n  fields:\n    # 顶层同名 key: extract 可省略\n    review_status:\n      name: 审核状态\n\n    # int-key nested dict: role_id 是 int(例如 1/2)\n    customer_clearn_reason_level:\n      name: 客户净利原因等级\n      extract: \"[1].clearn_reason_level\"\n\n    # dotted literal key: row 的 key 就叫 \"a.b\"\n    dotted_literal_key_value:\n      extract: '[\"a.b\"].x'\n```\n\n示例(给定 loader 返回值,extract 提取后的输出长什么样):\n\n```python\nresult = {\n  1: {1: {\"clearn_reason_level\": 2}, 2: {\"clearn_reason_level\": 1}, \"review_status\": 0},\n}\n```\n\n字段:\n\n```yaml\nsources:\n  clearn_reasons:\n    fields:\n      customer_level:\n        extract: \"[1].clearn_reason_level\"\n      operation_level:\n        extract: \"[2].clearn_reason_level\"\n      review_status:\n        extract: review_status\n```\n\n对 `lookup_key=1` 的 row value:\n- `customer_level` → `2`\n- `operation_level` → `1`\n- `review_status` → `0`\n\n最终输出行片段:\n\n```python\n{\"customer_level\": 2, \"operation_level\": 1, \"review_status\": 0}\n```\n\n补充边界:\n- 如果中间段是 list/tuple, `\"[1]\"` 也不会当作下标(会返回 `None`)\n- 如果同时存在 key `\"1\"` 与 `1`,需要用 `extract: '[\"1\"].x'` 与 `extract: \"[1].x\"` 明确区分\n",
          "examples": [
            "review_status",
            "CustomerMark.clearn_reason_level",
            "[1].clearn_reason_level",
            "[\"a.b\"].x"
          ]
        },
        "name": {
          "type": "string",
          "description": "字段显示名称",
          "markdownDescription": "字段显示名称.\n\n- `output.header_fields_output_by: name` 时作为表头"
        },
        "relation": {
          "oneOf": [
            {
              "type": "string",
              "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
              "description": "relation 引用(字符串 ref),示例: relation: orders_to_customers",
              "markdownDescription": "relation 引用(字符串 ref).\n\n- 形式: `relation: <relation_id>`\n- 表示引用 `relations.<relation_id>`\n- full validate 会校验 `relations.<relation_id>` 存在"
            },
            {
              "type": "object",
              "required": [
                "steps"
              ],
              "properties": {
                "steps": {
                  "type": "array",
                  "items": {
                    "type": "object",
                    "required": [
                      "from",
                      "to"
                    ],
                    "properties": {
                      "from": {
                        "oneOf": [
                          {
                            "type": "string",
                            "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*\\.[a-zA-Z_][a-zA-Z0-9_]*$",
                            "description": "格式: source.field (source 与 field 使用同样命名规则)",
                            "markdownDescription": "格式: `source.field`.\n\n- 仅允许字母/数字/下划线\n- `field` 必须是字段的 `field_id`(YAML key)\n- 不允许写 loader 的 data_key; 若 field_id != data_key,仍必须在 steps 中写 field_id\n- 对于非主源的 key 字段,也可直接引用 `sources.<id>.key` 中声明的字段名\n- 系统会将 `field_id` 映射为其 `field`(data_key)"
                          },
                          {
                            "type": "array",
                            "items": {
                              "type": "string",
                              "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*\\.[a-zA-Z_][a-zA-Z0-9_]*$",
                              "description": "格式: source.field (source 与 field 使用同样命名规则)",
                              "markdownDescription": "格式: `source.field`.\n\n- 仅允许字母/数字/下划线\n- `field` 必须是字段的 `field_id`(YAML key)\n- 不允许写 loader 的 data_key; 若 field_id != data_key,仍必须在 steps 中写 field_id\n- 对于非主源的 key 字段,也可直接引用 `sources.<id>.key` 中声明的字段名\n- 系统会将 `field_id` 映射为其 `field`(data_key)"
                            },
                            "minItems": 1
                          }
                        ],
                        "description": "上游字段(等值条件左侧),格式 source.field; 多字段用列表",
                        "markdownDescription": "上游字段(等值条件左侧).\n\n- `source.field` 或列表\n- `field` 必须是字段的 `field_id`(YAML key),不允许写 loader data_key\n- 列表用于复合键, 长度需与 `to` 对齐\n- 同一侧必须来自同一 source\n\n常见错误:\n- ❌ 写 data_key: `products.category_id`\n- ✅ 写 field_id: `products.product_category_id` (schema 会映射到 data_key)",
                        "examples": [
                          "orders.customer_id",
                          [
                            "orders.region_id",
                            "orders.institution_id"
                          ]
                        ]
                      },
                      "to": {
                        "oneOf": [
                          {
                            "type": "string",
                            "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*\\.[a-zA-Z_][a-zA-Z0-9_]*$",
                            "description": "格式: source.field (source 与 field 使用同样命名规则)",
                            "markdownDescription": "格式: `source.field`.\n\n- 仅允许字母/数字/下划线\n- `field` 必须是字段的 `field_id`(YAML key)\n- 不允许写 loader 的 data_key; 若 field_id != data_key,仍必须在 steps 中写 field_id\n- 对于非主源的 key 字段,也可直接引用 `sources.<id>.key` 中声明的字段名\n- 系统会将 `field_id` 映射为其 `field`(data_key)"
                          },
                          {
                            "type": "array",
                            "items": {
                              "type": "string",
                              "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*\\.[a-zA-Z_][a-zA-Z0-9_]*$",
                              "description": "格式: source.field (source 与 field 使用同样命名规则)",
                              "markdownDescription": "格式: `source.field`.\n\n- 仅允许字母/数字/下划线\n- `field` 必须是字段的 `field_id`(YAML key)\n- 不允许写 loader 的 data_key; 若 field_id != data_key,仍必须在 steps 中写 field_id\n- 对于非主源的 key 字段,也可直接引用 `sources.<id>.key` 中声明的字段名\n- 系统会将 `field_id` 映射为其 `field`(data_key)"
                            },
                            "minItems": 1
                          }
                        ],
                        "description": "下游字段(等值条件右侧/lookup key),格式 source.field; 多字段用列表",
                        "markdownDescription": "下游字段(等值条件右侧/lookup key).\n\n- `source.field` 或列表\n- `field` 必须是字段的 `field_id`(YAML key),不允许写 loader data_key\n- 列表用于复合键, 长度需与 `from` 对齐\n- 同一侧必须来自同一 source\n\n常见错误:\n- ❌ 写 data_key: `products.category_id`\n- ✅ 写 field_id: `products.product_category_id` (schema 会映射到 data_key)",
                        "examples": [
                          "customers.customer_id",
                          [
                            "regions.region_id",
                            "mappings.institution_id"
                          ]
                        ]
                      },
                      "lookup_cast": {
                        "type": "object",
                        "required": [
                          "name"
                        ],
                        "properties": {
                          "name": {
                            "type": "string",
                            "enum": [
                              "auto",
                              "int",
                              "str",
                              "sep_first"
                            ],
                            "description": "转换名称(auto/int/str/sep_first)",
                            "markdownDescription": "转换名称.\n\n- `auto`: 自动归一化\n- `int`: 转为 int\n- `str`: 转为 str\n- `sep_first`: 按 `sep` 截取首段再归一化",
                            "examples": [
                              "auto"
                            ]
                          },
                          "sep": {
                            "type": "string",
                            "description": "sep_first 的分隔符(默认 ,)"
                          }
                        },
                        "additionalProperties": false,
                        "description": "归一化 lookup key 的转换(对象结构); sep_first 会先截取首段再做 auto_normalize_key, 例: {name: sep_first, sep: ','}",
                        "markdownDescription": "归一化 lookup key 的转换.\n\n- `name`: auto / int / str / sep_first\n- `auto` 会拒绝 float lookup key(避免歧义,返回 None 并忽略该键);\n  若上游可能返回 float(例如 123.0/12.34),请用 `int`/`str` 显式归一化或在 loader 中修复\n- `sep_first` 先按 `sep` 截取首段再做 normalize"
                      }
                    },
                    "additionalProperties": false
                  },
                  "minItems": 1,
                  "description": "按顺序定义等值关联链(from == to),系统不会重排 steps, 表示从 main_source 出发沿链路到达当前字段 source (例: steps: [{from: orders.customer_id, to: customers.customer_id}])",
                  "markdownDescription": "按顺序定义等值关联链(from == to).\n\n- `from`/`to` 支持 `source.field` 或列表\n- `field` 必须是字段的 `field_id`(YAML key),或 `sources.<id>.key` 中声明的 key 字段(不允许写 loader data_key)\n- 列表用于复合键, 长度需一致\n- 链路必须连续: steps[n].from 的 source = steps[n-1].to 的 source"
                }
              },
              "additionalProperties": false,
              "description": "内联 steps 或 alias 引用(需先定义),示例: relation: {steps: [...]}",
              "markdownDescription": "关系路径引用(内联 steps 或 YAML alias; alias 需先在 relations 定义).\n\n- `relation: *orders_to_customers`\n- `relation: {steps: [...]}`\n- steps 必须从 `main_source` 开始, 以当前 `source` 结束\n- 若 `source` 非 `main_source`, 且未显式 relation, 需确保 `relations` 中存在唯一链路"
            }
          ],
          "description": "关系路径(支持 string ref / steps 对象 / YAML alias; alias 需先定义),表示从 main_source 到当前字段 source 的等值关联链 (例: relation: orders_to_customers)",
          "markdownDescription": "关系路径(支持 string ref / steps 对象 / YAML alias; alias 需先定义),表示从 main_source 到当前字段 source 的等值关联链 (例: relation: orders_to_customers)"
        },
        "value_cast": {
          "type": "string",
          "description": "字段值转换(仅源字段),用于写入上下文/输出前的类型调整",
          "markdownDescription": "字段值转换(仅源字段).\n\n- `auto`: 自动转换\n- `int`: 转为 int\n- `str`: 转为 str",
          "enum": [
            "auto",
            "int",
            "str"
          ],
          "examples": [
            "auto"
          ]
        },
        "compute": {
          "type": "string",
          "description": "派生字段计算表达式(使用 field_id 作为变量名)",
          "markdownDescription": "派生字段计算表达式(与 `call_by` 互斥).\n\n- 使用 `field_id` 作为变量名\n- 必填, 不能为空",
          "examples": [
            "revenue - cost"
          ]
        },
        "call_by": {
          "type": "string",
          "description": "派生字段函数调用(函数引用 + 参数列表),与 compute 互斥",
          "markdownDescription": "派生字段函数调用(与 `compute` 互斥).\n\n- 语法: `reference(args...)`\n- `reference` 形式与 `loader` 引用一致,支持:\n  - 绝对引用: `module.path.function` / `module.path:function` / `module.path:obj.method`\n  - 相对引用: 以 `.` / `..` 开头的 module path(相对 YAML 文件所在目录)\n- 相对引用会在运行期先归一化为绝对引用,并继续受 allowlist(allowed_modules/allowed_functions) 约束\n- 支持位置参数与 kwargs\n- Python 字面量: `1`/`1.5`/`'ok'`/`True`/`False`/`None`\n- 上下文引用: `$ctx` 或 `$ctx.<attr>`\n- 可用 ctx 属性: `row_id`/`batch_num`/`field_id`/`deps`/`values`",
          "examples": [
            "myapp.enums:get_status_text(status)",
            "myapp.enums:get_status_text(status=status, ctx=$ctx)",
            ".helpers:to_text(status)"
          ]
        },
        "$import": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ],
          "description": "$import 引用(支持 string 或 string list)",
          "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
          "examples": [
            "common.sources",
            [
              "common.sources",
              "other.sources"
            ]
          ]
        }
      },
      "allOf": [
        {
          "oneOf": [
            {
              "required": [
                "compute"
              ],
              "not": {
                "required": [
                  "call_by"
                ]
              }
            },
            {
              "required": [
                "call_by"
              ],
              "not": {
                "required": [
                  "compute"
                ]
              }
            }
          ]
        }
      ]
    },
    "relation": {
      "type": "object",
      "properties": {
        "steps": {
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "from",
              "to"
            ],
            "properties": {
              "from": {
                "oneOf": [
                  {
                    "type": "string",
                    "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*\\.[a-zA-Z_][a-zA-Z0-9_]*$",
                    "description": "格式: source.field (source 与 field 使用同样命名规则)",
                    "markdownDescription": "格式: `source.field`.\n\n- 仅允许字母/数字/下划线\n- `field` 必须是字段的 `field_id`(YAML key)\n- 不允许写 loader 的 data_key; 若 field_id != data_key,仍必须在 steps 中写 field_id\n- 对于非主源的 key 字段,也可直接引用 `sources.<id>.key` 中声明的字段名\n- 系统会将 `field_id` 映射为其 `field`(data_key)"
                  },
                  {
                    "type": "array",
                    "items": {
                      "type": "string",
                      "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*\\.[a-zA-Z_][a-zA-Z0-9_]*$",
                      "description": "格式: source.field (source 与 field 使用同样命名规则)",
                      "markdownDescription": "格式: `source.field`.\n\n- 仅允许字母/数字/下划线\n- `field` 必须是字段的 `field_id`(YAML key)\n- 不允许写 loader 的 data_key; 若 field_id != data_key,仍必须在 steps 中写 field_id\n- 对于非主源的 key 字段,也可直接引用 `sources.<id>.key` 中声明的字段名\n- 系统会将 `field_id` 映射为其 `field`(data_key)"
                    },
                    "minItems": 1
                  }
                ],
                "description": "上游字段(等值条件左侧),格式 source.field; 多字段用列表",
                "markdownDescription": "上游字段(等值条件左侧).\n\n- `source.field` 或列表\n- `field` 必须是字段的 `field_id`(YAML key),不允许写 loader data_key\n- 列表用于复合键, 长度需与 `to` 对齐\n- 同一侧必须来自同一 source\n\n常见错误:\n- ❌ 写 data_key: `products.category_id`\n- ✅ 写 field_id: `products.product_category_id` (schema 会映射到 data_key)",
                "examples": [
                  "orders.customer_id",
                  [
                    "orders.region_id",
                    "orders.institution_id"
                  ]
                ]
              },
              "to": {
                "oneOf": [
                  {
                    "type": "string",
                    "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*\\.[a-zA-Z_][a-zA-Z0-9_]*$",
                    "description": "格式: source.field (source 与 field 使用同样命名规则)",
                    "markdownDescription": "格式: `source.field`.\n\n- 仅允许字母/数字/下划线\n- `field` 必须是字段的 `field_id`(YAML key)\n- 不允许写 loader 的 data_key; 若 field_id != data_key,仍必须在 steps 中写 field_id\n- 对于非主源的 key 字段,也可直接引用 `sources.<id>.key` 中声明的字段名\n- 系统会将 `field_id` 映射为其 `field`(data_key)"
                  },
                  {
                    "type": "array",
                    "items": {
                      "type": "string",
                      "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*\\.[a-zA-Z_][a-zA-Z0-9_]*$",
                      "description": "格式: source.field (source 与 field 使用同样命名规则)",
                      "markdownDescription": "格式: `source.field`.\n\n- 仅允许字母/数字/下划线\n- `field` 必须是字段的 `field_id`(YAML key)\n- 不允许写 loader 的 data_key; 若 field_id != data_key,仍必须在 steps 中写 field_id\n- 对于非主源的 key 字段,也可直接引用 `sources.<id>.key` 中声明的字段名\n- 系统会将 `field_id` 映射为其 `field`(data_key)"
                    },
                    "minItems": 1
                  }
                ],
                "description": "下游字段(等值条件右侧/lookup key),格式 source.field; 多字段用列表",
                "markdownDescription": "下游字段(等值条件右侧/lookup key).\n\n- `source.field` 或列表\n- `field` 必须是字段的 `field_id`(YAML key),不允许写 loader data_key\n- 列表用于复合键, 长度需与 `from` 对齐\n- 同一侧必须来自同一 source\n\n常见错误:\n- ❌ 写 data_key: `products.category_id`\n- ✅ 写 field_id: `products.product_category_id` (schema 会映射到 data_key)",
                "examples": [
                  "customers.customer_id",
                  [
                    "regions.region_id",
                    "mappings.institution_id"
                  ]
                ]
              },
              "lookup_cast": {
                "type": "object",
                "required": [
                  "name"
                ],
                "properties": {
                  "name": {
                    "type": "string",
                    "enum": [
                      "auto",
                      "int",
                      "str",
                      "sep_first"
                    ],
                    "description": "转换名称(auto/int/str/sep_first)",
                    "markdownDescription": "转换名称.\n\n- `auto`: 自动归一化\n- `int`: 转为 int\n- `str`: 转为 str\n- `sep_first`: 按 `sep` 截取首段再归一化",
                    "examples": [
                      "auto"
                    ]
                  },
                  "sep": {
                    "type": "string",
                    "description": "sep_first 的分隔符(默认 ,)"
                  }
                },
                "additionalProperties": false,
                "description": "归一化 lookup key 的转换(对象结构); sep_first 会先截取首段再做 auto_normalize_key, 例: {name: sep_first, sep: ','}",
                "markdownDescription": "归一化 lookup key 的转换.\n\n- `name`: auto / int / str / sep_first\n- `auto` 会拒绝 float lookup key(避免歧义,返回 None 并忽略该键);\n  若上游可能返回 float(例如 123.0/12.34),请用 `int`/`str` 显式归一化或在 loader 中修复\n- `sep_first` 先按 `sep` 截取首段再做 normalize"
              }
            },
            "additionalProperties": false
          },
          "minItems": 1,
          "description": "按顺序定义等值关联链(from == to),系统不会重排 steps, 表示从 main_source 出发沿链路到达当前字段 source (例: steps: [{from: orders.customer_id, to: customers.customer_id}])",
          "markdownDescription": "按顺序定义等值关联链(from == to),系统不会重排 steps, 表示从 main_source 出发沿链路到达当前字段 source (例: steps: [{from: orders.customer_id, to: customers.customer_id}])"
        },
        "$import": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ],
          "description": "$import 引用(支持 string 或 string list)",
          "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
          "examples": [
            "common.sources",
            [
              "common.sources",
              "other.sources"
            ]
          ]
        }
      },
      "anyOf": [
        {
          "required": [
            "steps"
          ]
        },
        {
          "required": [
            "$import"
          ]
        }
      ],
      "additionalProperties": false
    },
    "output_container": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "description": "输出容器类型(workbook/csv)",
          "markdownDescription": "输出容器类型.\n\n- `workbook`: Excel 工作簿容器(支持多 sheet)\n- `csv`: CSV 文件",
          "enum": [
            "workbook",
            "csv"
          ],
          "examples": [
            "workbook"
          ]
        },
        "path": {
          "type": "string",
          "minLength": 1,
          "description": "输出文件路径(相对路径以进程CWD为基准;自动mkdir父目录)",
          "markdownDescription": "输出文件路径.\n\n- 相对路径以运行时进程当前工作目录(CWD)为基准(不是 YAML 文件所在目录)\n- 会自动创建父目录: `mkdir(parents=True, exist_ok=True)`\n- 可能覆盖同名文件\n- 安全提示: 该路径完全由配置控制, 不要对不可信 YAML 开启文件输出",
          "examples": [
            "./output/report.xlsx",
            "./output/report.csv"
          ]
        },
        "sheet": {
          "type": "string",
          "minLength": 1,
          "description": "Excel sheet 名称(仅 workbook)",
          "markdownDescription": "Excel sheet 名称(仅 `type: workbook`).",
          "examples": [
            "订单明细",
            "Summary"
          ]
        },
        "encoding": {
          "type": "string",
          "description": "文件编码(CSV 输出使用)",
          "markdownDescription": "文件编码(CSV 输出使用).",
          "default": "utf-8",
          "examples": [
            "utf-8"
          ]
        },
        "streaming": {
          "type": "boolean",
          "description": "启用流式输出(必须为 true)",
          "markdownDescription": "启用流式输出(按行写入).\n\n- composed outputs 仅支持 `true`",
          "default": true,
          "examples": [
            true
          ]
        },
        "include_header": {
          "type": "boolean",
          "description": "包含表头行",
          "markdownDescription": "包含表头行.",
          "default": true,
          "examples": [
            true
          ]
        },
        "header_fields_output_by": {
          "type": "string",
          "description": "表头字段名来源: field_id/name",
          "markdownDescription": "表头字段名来源.\n\n- `field_id`: 使用字段 ID\n- `name`: 使用字段的 `name`(为空或等于 field_id 时回退为 field_id)",
          "enum": [
            "field_id",
            "name"
          ],
          "default": "field_id",
          "examples": [
            "field_id"
          ]
        },
        "allow_formulas": {
          "type": "boolean",
          "description": "允许 Excel 公式(仅 workbook)",
          "markdownDescription": "允许 Excel 公式(仅 `type: workbook`).",
          "default": false,
          "examples": [
            false
          ]
        },
        "write_lock": {
          "type": "boolean",
          "description": "写锁(仅 workbook)",
          "markdownDescription": "写锁(仅 `type: workbook`; 多目标共享同一 workbook 时建议开启).",
          "default": false,
          "examples": [
            true
          ]
        },
        "$import": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ],
          "description": "$import 引用(支持 string 或 string list)",
          "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
          "examples": [
            "common.sources",
            [
              "common.sources",
              "other.sources"
            ]
          ]
        }
      },
      "anyOf": [
        {
          "required": [
            "type",
            "path"
          ]
        },
        {
          "required": [
            "$import"
          ]
        }
      ],
      "additionalProperties": false
    },
    "output_aggregate": {
      "type": "object",
      "properties": {
        "group_by": {
          "type": "array",
          "minItems": 1,
          "items": {
            "anyOf": [
              {
                "type": "string",
                "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
                "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
              },
              {
                "type": "object"
              },
              {
                "type": "array",
                "items": {
                  "anyOf": [
                    {
                      "type": "string",
                      "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                      "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
                      "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
                    },
                    {
                      "type": "object"
                    }
                  ]
                },
                "minItems": 1
              }
            ]
          },
          "description": "分组字段列表",
          "markdownDescription": "分组字段列表(`field_id` 列表; 支持 YAML alias).\n\n支持两种等价写法:\n\n1) 直接写 `field_id` 字符串:\n\n```yaml\ngroup_by: [cs_id, cs_name]\n```\n\n2) 使用 YAML alias 引用字段定义对象以推导 `field_id`:\n\n```yaml\nmain_source:\n  fields:\n    cs_id: &cs_id {extract: cs_id}\noutputs:\n  - name: by_cs\n    aggregate:\n      group_by:\n        - *cs_id\n```\n\n注意: YAML merge(`<<`) 可能产生新对象并丢失 alias identity;此时建议直接使用字符串 `field_id`.\n\n- `group_by` 引用的是输入行字段(`field_id`),来自 `where` 过滤后的行流\n- 每个唯一的 group key 组合会产生 1 行聚合输出\n- `partition_by`(排名分区)要求为 `group_by` 子集,用于保证聚合输出可解释性",
          "examples": [
            [
              "cs_id",
              "cs_name"
            ]
          ]
        },
        "fields": {
          "type": "object",
          "minProperties": 1,
          "propertyNames": {
            "type": "string",
            "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
            "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
            "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
          },
          "additionalProperties": {
            "oneOf": [
              {
                "type": "object",
                "additionalProperties": false,
                "required": [
                  "count"
                ],
                "properties": {
                  "name": {
                    "type": "string",
                    "minLength": 1,
                    "description": "可选:表头显示名(仅对 aggregate 字段生效)",
                    "markdownDescription": "可选:表头显示名.\n\n- 仅对 `outputs.*.aggregate.fields.*` 的字段生效\n- 仅在 `outputs.*.container.header_fields_output_by: name` 时输出该 name\n- 允许重复 name(用于复刻重复表头合同)\n- 缺省/为空时回退为 out_field_id",
                    "examples": [
                      "订单量",
                      "积分",
                      "排名"
                    ]
                  },
                  "count": {
                    "type": "object",
                    "additionalProperties": false,
                    "properties": {
                      "field": {
                        "anyOf": [
                          {
                            "type": "string",
                            "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                            "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
                            "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
                          },
                          {
                            "type": "object"
                          }
                        ],
                        "description": "可选:输入字段(field_id);提供时统计非空值个数",
                        "markdownDescription": "可选:输入字段(`field_id`);提供时统计非空值个数.",
                        "examples": [
                          "order_id"
                        ]
                      }
                    },
                    "description": "count: 行数(或非空值数)",
                    "markdownDescription": "行数/计数指标.\n\n- `count: {}`: 统计组内行数\n- `count: {field: <field_id>}`: 统计组内该字段非空值个数\n\n执行阶段: 聚合指标(先于排名/派生字段).",
                    "examples": [
                      {},
                      {
                        "field": "order_id"
                      }
                    ]
                  }
                }
              },
              {
                "type": "object",
                "additionalProperties": false,
                "required": [
                  "sum"
                ],
                "properties": {
                  "name": {
                    "type": "string",
                    "minLength": 1,
                    "description": "可选:表头显示名(仅对 aggregate 字段生效)",
                    "markdownDescription": "可选:表头显示名.\n\n- 仅对 `outputs.*.aggregate.fields.*` 的字段生效\n- 仅在 `outputs.*.container.header_fields_output_by: name` 时输出该 name\n- 允许重复 name(用于复刻重复表头合同)\n- 缺省/为空时回退为 out_field_id",
                    "examples": [
                      "订单量",
                      "积分",
                      "排名"
                    ]
                  },
                  "sum": {
                    "type": "object",
                    "additionalProperties": false,
                    "required": [
                      "field"
                    ],
                    "properties": {
                      "field": {
                        "anyOf": [
                          {
                            "type": "string",
                            "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                            "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
                            "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
                          },
                          {
                            "type": "object"
                          }
                        ],
                        "description": "输入字段(field_id)",
                        "markdownDescription": "输入字段(`field_id`).",
                        "examples": [
                          "amount_yuan"
                        ]
                      }
                    },
                    "description": "sum: 数值求和",
                    "markdownDescription": "数值求和指标.\n\n- 跳过 `None`\n- 对非数值输入尽量做安全转换(失败则跳过)\n\n参数:\n- `field`: 输入字段(`field_id`).",
                    "examples": [
                      {
                        "field": "amount_yuan"
                      }
                    ]
                  }
                }
              },
              {
                "type": "object",
                "additionalProperties": false,
                "required": [
                  "min"
                ],
                "properties": {
                  "name": {
                    "type": "string",
                    "minLength": 1,
                    "description": "可选:表头显示名(仅对 aggregate 字段生效)",
                    "markdownDescription": "可选:表头显示名.\n\n- 仅对 `outputs.*.aggregate.fields.*` 的字段生效\n- 仅在 `outputs.*.container.header_fields_output_by: name` 时输出该 name\n- 允许重复 name(用于复刻重复表头合同)\n- 缺省/为空时回退为 out_field_id",
                    "examples": [
                      "订单量",
                      "积分",
                      "排名"
                    ]
                  },
                  "min": {
                    "type": "object",
                    "additionalProperties": false,
                    "required": [
                      "field"
                    ],
                    "properties": {
                      "field": {
                        "anyOf": [
                          {
                            "type": "string",
                            "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                            "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
                            "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
                          },
                          {
                            "type": "object"
                          }
                        ],
                        "description": "输入字段(field_id)",
                        "markdownDescription": "输入字段(`field_id`).",
                        "examples": [
                          "amount_yuan"
                        ]
                      }
                    },
                    "description": "min: 最小值",
                    "markdownDescription": "最小值指标.\n\n参数:\n- `field`: 输入字段(`field_id`).",
                    "examples": [
                      {
                        "field": "amount_yuan"
                      }
                    ]
                  }
                }
              },
              {
                "type": "object",
                "additionalProperties": false,
                "required": [
                  "max"
                ],
                "properties": {
                  "name": {
                    "type": "string",
                    "minLength": 1,
                    "description": "可选:表头显示名(仅对 aggregate 字段生效)",
                    "markdownDescription": "可选:表头显示名.\n\n- 仅对 `outputs.*.aggregate.fields.*` 的字段生效\n- 仅在 `outputs.*.container.header_fields_output_by: name` 时输出该 name\n- 允许重复 name(用于复刻重复表头合同)\n- 缺省/为空时回退为 out_field_id",
                    "examples": [
                      "订单量",
                      "积分",
                      "排名"
                    ]
                  },
                  "max": {
                    "type": "object",
                    "additionalProperties": false,
                    "required": [
                      "field"
                    ],
                    "properties": {
                      "field": {
                        "anyOf": [
                          {
                            "type": "string",
                            "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                            "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
                            "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
                          },
                          {
                            "type": "object"
                          }
                        ],
                        "description": "输入字段(field_id)",
                        "markdownDescription": "输入字段(`field_id`).",
                        "examples": [
                          "amount_yuan"
                        ]
                      }
                    },
                    "description": "max: 最大值",
                    "markdownDescription": "最大值指标.\n\n参数:\n- `field`: 输入字段(`field_id`).",
                    "examples": [
                      {
                        "field": "amount_yuan"
                      }
                    ]
                  }
                }
              },
              {
                "type": "object",
                "additionalProperties": false,
                "required": [
                  "count_true"
                ],
                "properties": {
                  "name": {
                    "type": "string",
                    "minLength": 1,
                    "description": "可选:表头显示名(仅对 aggregate 字段生效)",
                    "markdownDescription": "可选:表头显示名.\n\n- 仅对 `outputs.*.aggregate.fields.*` 的字段生效\n- 仅在 `outputs.*.container.header_fields_output_by: name` 时输出该 name\n- 允许重复 name(用于复刻重复表头合同)\n- 缺省/为空时回退为 out_field_id",
                    "examples": [
                      "订单量",
                      "积分",
                      "排名"
                    ]
                  },
                  "count_true": {
                    "type": "object",
                    "additionalProperties": false,
                    "required": [
                      "field"
                    ],
                    "properties": {
                      "field": {
                        "anyOf": [
                          {
                            "type": "string",
                            "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                            "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
                            "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
                          },
                          {
                            "type": "object"
                          }
                        ],
                        "description": "输入字段(field_id)",
                        "markdownDescription": "输入字段(`field_id`).",
                        "examples": [
                          "is_paid"
                        ]
                      }
                    },
                    "description": "count_true: 统计 truthy 行数",
                    "markdownDescription": "统计组内某字段为 truthy 的行数.\n\n参数:\n- `field`: 输入字段(`field_id`).",
                    "examples": [
                      {
                        "field": "is_paid"
                      }
                    ]
                  }
                }
              },
              {
                "type": "object",
                "additionalProperties": false,
                "required": [
                  "count_true_gte"
                ],
                "properties": {
                  "name": {
                    "type": "string",
                    "minLength": 1,
                    "description": "可选:表头显示名(仅对 aggregate 字段生效)",
                    "markdownDescription": "可选:表头显示名.\n\n- 仅对 `outputs.*.aggregate.fields.*` 的字段生效\n- 仅在 `outputs.*.container.header_fields_output_by: name` 时输出该 name\n- 允许重复 name(用于复刻重复表头合同)\n- 缺省/为空时回退为 out_field_id",
                    "examples": [
                      "订单量",
                      "积分",
                      "排名"
                    ]
                  },
                  "count_true_gte": {
                    "type": "object",
                    "additionalProperties": false,
                    "required": [
                      "field",
                      "threshold"
                    ],
                    "properties": {
                      "field": {
                        "anyOf": [
                          {
                            "type": "string",
                            "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                            "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
                            "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
                          },
                          {
                            "type": "object"
                          }
                        ],
                        "description": "输入字段(field_id)",
                        "markdownDescription": "输入字段(`field_id`).",
                        "examples": [
                          "amount_yuan"
                        ]
                      },
                      "threshold": {
                        "description": "阈值(>= threshold 为 true)",
                        "markdownDescription": "阈值(>= threshold 为 true).",
                        "examples": [
                          0,
                          100,
                          99.5
                        ]
                      }
                    },
                    "description": "count_true_gte: 统计数值 >= 阈值的行数",
                    "markdownDescription": "统计组内某字段数值 >= 阈值的行数.\n\n参数:\n- `field`: 输入字段(`field_id`).\n- `threshold`: 阈值.",
                    "examples": [
                      {
                        "field": "amount_yuan",
                        "threshold": 100
                      }
                    ]
                  }
                }
              },
              {
                "type": "object",
                "additionalProperties": false,
                "required": [
                  "count_distinct"
                ],
                "properties": {
                  "name": {
                    "type": "string",
                    "minLength": 1,
                    "description": "可选:表头显示名(仅对 aggregate 字段生效)",
                    "markdownDescription": "可选:表头显示名.\n\n- 仅对 `outputs.*.aggregate.fields.*` 的字段生效\n- 仅在 `outputs.*.container.header_fields_output_by: name` 时输出该 name\n- 允许重复 name(用于复刻重复表头合同)\n- 缺省/为空时回退为 out_field_id",
                    "examples": [
                      "订单量",
                      "积分",
                      "排名"
                    ]
                  },
                  "count_distinct": {
                    "type": "object",
                    "additionalProperties": false,
                    "oneOf": [
                      {
                        "required": [
                          "field"
                        ],
                        "not": {
                          "required": [
                            "fields"
                          ]
                        }
                      },
                      {
                        "required": [
                          "fields"
                        ],
                        "not": {
                          "required": [
                            "field"
                          ]
                        }
                      }
                    ],
                    "properties": {
                      "field": {
                        "anyOf": [
                          {
                            "type": "string",
                            "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                            "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
                            "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
                          },
                          {
                            "type": "object"
                          }
                        ],
                        "description": "输入字段(field_id)",
                        "markdownDescription": "输入字段(`field_id`).",
                        "examples": [
                          "user_id"
                        ]
                      },
                      "fields": {
                        "type": "array",
                        "items": {
                          "anyOf": [
                            {
                              "type": "string",
                              "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                              "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
                              "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
                            },
                            {
                              "type": "object"
                            },
                            {
                              "type": "array",
                              "items": {
                                "anyOf": [
                                  {
                                    "type": "string",
                                    "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                                    "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
                                    "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
                                  },
                                  {
                                    "type": "object"
                                  }
                                ]
                              },
                              "minItems": 1
                            }
                          ]
                        },
                        "minItems": 1,
                        "description": "复合去重键(字段列表)",
                        "markdownDescription": "复合去重键(`field_id` 列表).",
                        "examples": [
                          [
                            "user_id",
                            "item_id"
                          ]
                        ]
                      }
                    },
                    "description": "count_distinct: 去重计数",
                    "markdownDescription": "去重计数指标.\n\n- 支持单字段去重: `field`\n- 支持复合键去重: `fields`\n- 去重护栏由 `max_distinct`/`distinct_on_overflow` 控制\n\n约束:\n- 必须且只能提供 `field` 或 `fields` 之一.",
                    "examples": [
                      {
                        "field": "user_id"
                      },
                      {
                        "fields": [
                          "user_id",
                          "item_id"
                        ]
                      }
                    ]
                  }
                }
              },
              {
                "type": "object",
                "additionalProperties": false,
                "required": [
                  "row_number"
                ],
                "properties": {
                  "name": {
                    "type": "string",
                    "minLength": 1,
                    "description": "可选:表头显示名(仅对 aggregate 字段生效)",
                    "markdownDescription": "可选:表头显示名.\n\n- 仅对 `outputs.*.aggregate.fields.*` 的字段生效\n- 仅在 `outputs.*.container.header_fields_output_by: name` 时输出该 name\n- 允许重复 name(用于复刻重复表头合同)\n- 缺省/为空时回退为 out_field_id",
                    "examples": [
                      "订单量",
                      "积分",
                      "排名"
                    ]
                  },
                  "row_number": {
                    "type": "object",
                    "additionalProperties": false,
                    "required": [
                      "by"
                    ],
                    "properties": {
                      "by": {
                        "anyOf": [
                          {
                            "type": "string",
                            "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                            "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
                            "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
                          },
                          {
                            "type": "object"
                          }
                        ],
                        "description": "用于排序的字段(引用 group_by 或 aggregate.fields 任意字段,含派生字段)",
                        "markdownDescription": "用于排序的字段(引用 `group_by` 或 `aggregate.fields` 任意字段,含派生字段).",
                        "examples": [
                          "sum_amount"
                        ]
                      },
                      "partition_by": {
                        "type": "array",
                        "items": {
                          "anyOf": [
                            {
                              "type": "string",
                              "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                              "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
                              "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
                            },
                            {
                              "type": "object"
                            },
                            {
                              "type": "array",
                              "items": {
                                "anyOf": [
                                  {
                                    "type": "string",
                                    "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                                    "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
                                    "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
                                  },
                                  {
                                    "type": "object"
                                  }
                                ]
                              },
                              "minItems": 1
                            }
                          ]
                        },
                        "minItems": 1,
                        "description": "可选:排名分区字段列表(必须是 group_by 子集)",
                        "markdownDescription": "可选:排名分区字段列表(必须是 `group_by` 子集).",
                        "examples": [
                          [
                            "region_id"
                          ]
                        ]
                      },
                      "order": {
                        "type": "string",
                        "enum": [
                          "asc",
                          "desc"
                        ],
                        "default": "desc",
                        "description": "排序方向(asc/desc)",
                        "markdownDescription": "排序方向.\n\n- `asc`: 升序\n- `desc`: 降序",
                        "examples": [
                          "desc"
                        ]
                      },
                      "order_by": {
                        "type": "array",
                        "items": {
                          "anyOf": [
                            {
                              "type": "string",
                              "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                              "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
                              "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
                            },
                            {
                              "type": "object"
                            },
                            {
                              "type": "array",
                              "items": {
                                "anyOf": [
                                  {
                                    "type": "string",
                                    "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                                    "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
                                    "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
                                  },
                                  {
                                    "type": "object"
                                  }
                                ]
                              },
                              "minItems": 1
                            }
                          ]
                        },
                        "minItems": 1,
                        "description": "可选:稳定排序字段列表(用于 tie-break; top_k_mode=rows 必填)",
                        "markdownDescription": "可选:稳定排序字段列表.\n\n- 用于输出稳定排序与 `top_k_mode=rows` 的稳定 tie-break\n- 缺省时等价于 `[by]`",
                        "examples": [
                          [
                            "sum_amount",
                            "cs_id"
                          ]
                        ]
                      },
                      "top_k": {
                        "type": "integer",
                        "minimum": 0,
                        "default": 0,
                        "description": "可选:每个分区保留前 K 个(0 表示不限制)",
                        "markdownDescription": "可选:每个分区保留前 K 个(0 表示不限制).",
                        "examples": [
                          0,
                          100
                        ]
                      },
                      "top_k_mode": {
                        "type": "string",
                        "enum": [
                          "rank",
                          "rows"
                        ],
                        "default": "rank",
                        "description": "top_k 模式(rank=含并列扩张; rows=固定 K 行)",
                        "markdownDescription": "top_k 模式.\n\n- `rank`(默认): 保留 `rank_value <= K` 的所有行(含并列扩张)\n- `rows`: 强行取前 K 行(允许截断并列);为保证确定性,必须提供 `order_by`",
                        "examples": [
                          "rank"
                        ]
                      }
                    },
                    "description": "row_number: 连续序号(不合并并列)",
                    "markdownDescription": "连续序号(1..N),不合并并列.\n\n- `by`: 用于排序(并列不合并)\n- `partition_by`: 分区内重置序号(必须是 `group_by` 子集)\n\n执行阶段: 排名字段(在聚合指标之后).",
                    "examples": [
                      {
                        "by": "sum_amount",
                        "order": "desc"
                      }
                    ]
                  }
                }
              },
              {
                "type": "object",
                "additionalProperties": false,
                "required": [
                  "rank"
                ],
                "properties": {
                  "name": {
                    "type": "string",
                    "minLength": 1,
                    "description": "可选:表头显示名(仅对 aggregate 字段生效)",
                    "markdownDescription": "可选:表头显示名.\n\n- 仅对 `outputs.*.aggregate.fields.*` 的字段生效\n- 仅在 `outputs.*.container.header_fields_output_by: name` 时输出该 name\n- 允许重复 name(用于复刻重复表头合同)\n- 缺省/为空时回退为 out_field_id",
                    "examples": [
                      "订单量",
                      "积分",
                      "排名"
                    ]
                  },
                  "rank": {
                    "type": "object",
                    "additionalProperties": false,
                    "required": [
                      "by"
                    ],
                    "properties": {
                      "by": {
                        "anyOf": [
                          {
                            "type": "string",
                            "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                            "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
                            "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
                          },
                          {
                            "type": "object"
                          }
                        ],
                        "description": "用于计算 rank 值与并列判断的字段(引用 group_by 或 aggregate.fields 任意字段,含派生字段)",
                        "markdownDescription": "用于计算 rank 值与并列判断的字段(引用 `group_by` 或 `aggregate.fields` 任意字段,含派生字段).",
                        "examples": [
                          "sum_amount"
                        ]
                      },
                      "partition_by": {
                        "type": "array",
                        "items": {
                          "anyOf": [
                            {
                              "type": "string",
                              "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                              "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
                              "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
                            },
                            {
                              "type": "object"
                            },
                            {
                              "type": "array",
                              "items": {
                                "anyOf": [
                                  {
                                    "type": "string",
                                    "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                                    "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
                                    "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
                                  },
                                  {
                                    "type": "object"
                                  }
                                ]
                              },
                              "minItems": 1
                            }
                          ]
                        },
                        "minItems": 1,
                        "description": "可选:排名分区字段列表(必须是 group_by 子集)",
                        "markdownDescription": "可选:排名分区字段列表(必须是 `group_by` 子集).",
                        "examples": [
                          [
                            "region_id"
                          ]
                        ]
                      },
                      "order": {
                        "type": "string",
                        "enum": [
                          "asc",
                          "desc"
                        ],
                        "default": "desc",
                        "description": "排序方向(asc/desc)",
                        "markdownDescription": "排序方向.\n\n- `asc`: 升序\n- `desc`: 降序",
                        "examples": [
                          "desc"
                        ]
                      },
                      "order_by": {
                        "type": "array",
                        "items": {
                          "anyOf": [
                            {
                              "type": "string",
                              "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                              "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
                              "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
                            },
                            {
                              "type": "object"
                            },
                            {
                              "type": "array",
                              "items": {
                                "anyOf": [
                                  {
                                    "type": "string",
                                    "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                                    "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
                                    "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
                                  },
                                  {
                                    "type": "object"
                                  }
                                ]
                              },
                              "minItems": 1
                            }
                          ]
                        },
                        "minItems": 1,
                        "description": "可选:稳定排序字段列表(用于 tie-break; top_k_mode=rows 必填)",
                        "markdownDescription": "可选:稳定排序字段列表.\n\n- 用于输出稳定排序与 `top_k_mode=rows` 的稳定 tie-break\n- 缺省时等价于 `[by]`",
                        "examples": [
                          [
                            "sum_amount",
                            "cs_id"
                          ]
                        ]
                      },
                      "top_k": {
                        "type": "integer",
                        "minimum": 0,
                        "default": 0,
                        "description": "可选:每个分区保留前 K 个(0 表示不限制)",
                        "markdownDescription": "可选:每个分区保留前 K 个(0 表示不限制).",
                        "examples": [
                          0,
                          100
                        ]
                      },
                      "top_k_mode": {
                        "type": "string",
                        "enum": [
                          "rank",
                          "rows"
                        ],
                        "default": "rank",
                        "description": "top_k 模式(rank=含并列扩张; rows=固定 K 行)",
                        "markdownDescription": "top_k 模式.\n\n- `rank`(默认): 保留 `rank_value <= K` 的所有行(含并列扩张)\n- `rows`: 强行取前 K 行(允许截断并列);为保证确定性,必须提供 `order_by`",
                        "examples": [
                          "rank"
                        ]
                      }
                    },
                    "description": "rank: SQL rank(并列共享名次,后续跳号)",
                    "markdownDescription": "SQL `rank` 语义: (1,1,3...).\n\n- 并列判断只由 `by` 的值决定(不要用 `order_by` 打散并列)\n- `order_by` 仅用于输出稳定排序与 `top_k_mode=rows` 的确定性 tie-break\n\n执行阶段: 排名字段(在聚合指标之后).",
                    "examples": [
                      {
                        "by": "sum_amount",
                        "order": "desc"
                      }
                    ]
                  }
                }
              },
              {
                "type": "object",
                "additionalProperties": false,
                "required": [
                  "dense_rank"
                ],
                "properties": {
                  "name": {
                    "type": "string",
                    "minLength": 1,
                    "description": "可选:表头显示名(仅对 aggregate 字段生效)",
                    "markdownDescription": "可选:表头显示名.\n\n- 仅对 `outputs.*.aggregate.fields.*` 的字段生效\n- 仅在 `outputs.*.container.header_fields_output_by: name` 时输出该 name\n- 允许重复 name(用于复刻重复表头合同)\n- 缺省/为空时回退为 out_field_id",
                    "examples": [
                      "订单量",
                      "积分",
                      "排名"
                    ]
                  },
                  "dense_rank": {
                    "type": "object",
                    "additionalProperties": false,
                    "required": [
                      "by"
                    ],
                    "properties": {
                      "by": {
                        "anyOf": [
                          {
                            "type": "string",
                            "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                            "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
                            "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
                          },
                          {
                            "type": "object"
                          }
                        ],
                        "description": "用于计算 rank 值与并列判断的字段(引用 group_by 或 aggregate.fields 任意字段,含派生字段)",
                        "markdownDescription": "用于计算 rank 值与并列判断的字段(引用 `group_by` 或 `aggregate.fields` 任意字段,含派生字段).",
                        "examples": [
                          "sum_amount"
                        ]
                      },
                      "partition_by": {
                        "type": "array",
                        "items": {
                          "anyOf": [
                            {
                              "type": "string",
                              "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                              "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
                              "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
                            },
                            {
                              "type": "object"
                            },
                            {
                              "type": "array",
                              "items": {
                                "anyOf": [
                                  {
                                    "type": "string",
                                    "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                                    "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
                                    "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
                                  },
                                  {
                                    "type": "object"
                                  }
                                ]
                              },
                              "minItems": 1
                            }
                          ]
                        },
                        "minItems": 1,
                        "description": "可选:排名分区字段列表(必须是 group_by 子集)",
                        "markdownDescription": "可选:排名分区字段列表(必须是 `group_by` 子集).",
                        "examples": [
                          [
                            "region_id"
                          ]
                        ]
                      },
                      "order": {
                        "type": "string",
                        "enum": [
                          "asc",
                          "desc"
                        ],
                        "default": "desc",
                        "description": "排序方向(asc/desc)",
                        "markdownDescription": "排序方向.\n\n- `asc`: 升序\n- `desc`: 降序",
                        "examples": [
                          "desc"
                        ]
                      },
                      "order_by": {
                        "type": "array",
                        "items": {
                          "anyOf": [
                            {
                              "type": "string",
                              "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                              "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
                              "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
                            },
                            {
                              "type": "object"
                            },
                            {
                              "type": "array",
                              "items": {
                                "anyOf": [
                                  {
                                    "type": "string",
                                    "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                                    "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
                                    "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
                                  },
                                  {
                                    "type": "object"
                                  }
                                ]
                              },
                              "minItems": 1
                            }
                          ]
                        },
                        "minItems": 1,
                        "description": "可选:稳定排序字段列表(用于 tie-break; top_k_mode=rows 必填)",
                        "markdownDescription": "可选:稳定排序字段列表.\n\n- 用于输出稳定排序与 `top_k_mode=rows` 的稳定 tie-break\n- 缺省时等价于 `[by]`",
                        "examples": [
                          [
                            "sum_amount",
                            "cs_id"
                          ]
                        ]
                      },
                      "top_k": {
                        "type": "integer",
                        "minimum": 0,
                        "default": 0,
                        "description": "可选:每个分区保留前 K 个(0 表示不限制)",
                        "markdownDescription": "可选:每个分区保留前 K 个(0 表示不限制).",
                        "examples": [
                          0,
                          100
                        ]
                      },
                      "top_k_mode": {
                        "type": "string",
                        "enum": [
                          "rank",
                          "rows"
                        ],
                        "default": "rank",
                        "description": "top_k 模式(rank=含并列扩张; rows=固定 K 行)",
                        "markdownDescription": "top_k 模式.\n\n- `rank`(默认): 保留 `rank_value <= K` 的所有行(含并列扩张)\n- `rows`: 强行取前 K 行(允许截断并列);为保证确定性,必须提供 `order_by`",
                        "examples": [
                          "rank"
                        ]
                      }
                    },
                    "description": "dense_rank: SQL dense_rank(并列共享名次,后续不跳号)",
                    "markdownDescription": "SQL `dense_rank` 语义: (1,1,2...).\n\n- 并列判断只由 `by` 的值决定(不要用 `order_by` 打散并列)\n- `order_by` 仅用于输出稳定排序与 `top_k_mode=rows` 的确定性 tie-break\n\n执行阶段: 排名字段(在聚合指标之后).",
                    "examples": [
                      {
                        "by": "sum_amount",
                        "order": "desc"
                      }
                    ]
                  }
                }
              },
              {
                "type": "object",
                "additionalProperties": false,
                "required": [
                  "score_by_rank"
                ],
                "properties": {
                  "name": {
                    "type": "string",
                    "minLength": 1,
                    "description": "可选:表头显示名(仅对 aggregate 字段生效)",
                    "markdownDescription": "可选:表头显示名.\n\n- 仅对 `outputs.*.aggregate.fields.*` 的字段生效\n- 仅在 `outputs.*.container.header_fields_output_by: name` 时输出该 name\n- 允许重复 name(用于复刻重复表头合同)\n- 缺省/为空时回退为 out_field_id",
                    "examples": [
                      "订单量",
                      "积分",
                      "排名"
                    ]
                  },
                  "score_by_rank": {
                    "type": "object",
                    "additionalProperties": false,
                    "properties": {
                      "rank_field": {
                        "anyOf": [
                          {
                            "type": "string",
                            "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                            "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
                            "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
                          },
                          {
                            "type": "object"
                          }
                        ],
                        "description": "可选:引用的排名字段(out_field_id);缺省为 'rank'",
                        "markdownDescription": "可选:引用的排名字段(out_field_id);缺省为 `rank`.",
                        "examples": [
                          "rank"
                        ]
                      },
                      "base": {
                        "description": "可选:基础分数(base)",
                        "markdownDescription": "可选:基础分数(base).",
                        "examples": [
                          100
                        ]
                      },
                      "step": {
                        "description": "可选:每名次衰减(step)",
                        "markdownDescription": "可选:每名次衰减(step).",
                        "examples": [
                          3
                        ]
                      }
                    },
                    "description": "score_by_rank: 基于 rank 计算 score",
                    "markdownDescription": "基于排名字段计算 score(聚合后派生字段).\n\n默认公式:\n- `score = base - (rank - 1) * step`\n\n参数:\n- `rank_field`: 引用的排名字段(out_field_id),缺省为 `rank`\n- `base`: 基础分数(缺省 0)\n- `step`: 每名次衰减(缺省 1)\n\n执行语义: 聚合后派生字段 DAG 的一个节点(按依赖驱动在 finalize 阶段计算).",
                    "examples": [
                      {
                        "rank_field": "rank",
                        "base": 100,
                        "step": 3
                      }
                    ]
                  }
                }
              },
              {
                "type": "object",
                "additionalProperties": false,
                "required": [
                  "call_by"
                ],
                "properties": {
                  "name": {
                    "type": "string",
                    "minLength": 1,
                    "description": "可选:表头显示名(仅对 aggregate 字段生效)",
                    "markdownDescription": "可选:表头显示名.\n\n- 仅对 `outputs.*.aggregate.fields.*` 的字段生效\n- 仅在 `outputs.*.container.header_fields_output_by: name` 时输出该 name\n- 允许重复 name(用于复刻重复表头合同)\n- 缺省/为空时回退为 out_field_id",
                    "examples": [
                      "订单量",
                      "积分",
                      "排名"
                    ]
                  },
                  "call_by": {
                    "type": "string",
                    "minLength": 1,
                    "description": "call_by: 聚合后派生字段的 hotfix 口子(安全引用,受 allowlist 约束)",
                    "markdownDescription": "聚合后派生字段的 hotfix 口子(受 allowlist 约束).\n\n- 与 `compute` 一样属于聚合后派生字段,会与 rank/其它派生字段一起组成 DAG\n- 只可引用聚合输出行内字段: `group_by` + `aggregate.fields` 中声明的字段(含派生字段)\n- 不可引用明细行字段(聚合前状态不可用)\n- 若存在循环依赖,编译期会报错并给出依赖链路\n\n示例:\n```yaml\nscore: {call_by: \"pkg.mod:fn(rank=rank, base=100, step=3)\"}\n```",
                    "examples": [
                      "pkg.mod:fn(rank=rank, base=100, step=3)"
                    ]
                  }
                }
              },
              {
                "type": "object",
                "additionalProperties": false,
                "required": [
                  "compute"
                ],
                "properties": {
                  "name": {
                    "type": "string",
                    "minLength": 1,
                    "description": "可选:表头显示名(仅对 aggregate 字段生效)",
                    "markdownDescription": "可选:表头显示名.\n\n- 仅对 `outputs.*.aggregate.fields.*` 的字段生效\n- 仅在 `outputs.*.container.header_fields_output_by: name` 时输出该 name\n- 允许重复 name(用于复刻重复表头合同)\n- 缺省/为空时回退为 out_field_id",
                    "examples": [
                      "订单量",
                      "积分",
                      "排名"
                    ]
                  },
                  "compute": {
                    "type": "string",
                    "minLength": 1,
                    "description": "compute: 聚合后派生字段安全表达式(推荐)",
                    "markdownDescription": "安全表达式派生字段(推荐).\n\n- 语法与 `fields.*.compute` / `where` 一致\n- 变量名为聚合输出行字段 ID: `group_by` + `aggregate.fields` 中声明的字段(含派生字段)\n- 会参与 aggregate DAG: 按依赖驱动计算; 循环依赖会在编译期报错\n- 适合 ratio/求和/加权等简单派生; 复杂逻辑可用 `call_by`(仍受 allowlist 约束)",
                    "examples": [
                      "sum_a / sum_b",
                      "score1 + score2"
                    ]
                  }
                }
              }
            ]
          },
          "description": "聚合输出字段映射(key 为 out_field_id)",
          "markdownDescription": "聚合输出字段映射(key 为 out_field_id).\n\n- map key 是输出字段 ID\n- map value 是“该字段如何产生”的声明,必须且只能选择一个 producer key\n- producer key 分三类:\n  1) 聚合指标函数(例如 `count`/`sum`/`count_distinct`)\n  2) 排名函数(例如 `row_number`/`rank`/`dense_rank`)\n  3) 聚合后派生字段(例如 `compute`/`score_by_rank`/`call_by`)\n\n执行顺序/语义(依赖驱动 DAG):\n- `rank` + 聚合后派生字段统一视为同一套 DAG,在 finalize 阶段按拓扑序执行\n- 引用范围: `group_by` + `aggregate.fields` 任意 out_field_id(含派生字段)\n- 系统会在编译期构建依赖图,检测循环依赖并给出依赖链路\n- `top_k/sort` 会在计算完所有 rank 字段及其上游依赖后执行; 其余派生字段可在过滤后计算\n\n边界:\n- 不可引用明细行字段(除 `group_by`);聚合前中间状态不会被保留"
        },
        "max_groups": {
          "type": "integer",
          "description": "max_groups 护栏(0 表示不限制)",
          "markdownDescription": "max_groups 护栏(0 表示不限制).",
          "minimum": 0,
          "default": 0,
          "examples": [
            0,
            10000
          ]
        },
        "max_distinct": {
          "type": "integer",
          "description": "max_distinct 护栏(0 表示不限制)",
          "markdownDescription": "max_distinct 护栏(0 表示不限制).",
          "minimum": 0,
          "default": 0,
          "examples": [
            0,
            200000
          ]
        },
        "distinct_on_overflow": {
          "type": "string",
          "description": "distinct 护栏溢出策略(error/truncate)",
          "markdownDescription": "distinct 护栏溢出策略.\n\n- `error`: 失败\n- `truncate`: 截断并继续",
          "enum": [
            "error",
            "truncate"
          ],
          "default": "error",
          "examples": [
            "error"
          ]
        },
        "$import": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ],
          "description": "$import 引用(支持 string 或 string list)",
          "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
          "examples": [
            "common.sources",
            [
              "common.sources",
              "other.sources"
            ]
          ]
        }
      },
      "anyOf": [
        {
          "required": [
            "group_by",
            "fields"
          ]
        },
        {
          "required": [
            "$import"
          ]
        }
      ],
      "additionalProperties": false
    },
    "output_target": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
          "description": "输出名称(name)",
          "markdownDescription": "输出名称(name)",
          "examples": [
            "detail",
            "direct_detail",
            "by_cs"
          ]
        },
        "from": {
          "type": "string",
          "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
          "description": "可选:继承来源输出(name)",
          "markdownDescription": "可选:继承来源输出(name).\n\n- 继承字段集合与容器配置\n- 不继承 where/aggregate",
          "examples": [
            "detail"
          ]
        },
        "container": {
          "description": "输出容器配置(workbook/csv)",
          "markdownDescription": "输出容器配置(workbook/csv)",
          "allOf": [
            {
              "$ref": "#/definitions/output_container"
            }
          ]
        },
        "fields": {
          "type": "array",
          "items": {
            "anyOf": [
              {
                "type": "string",
                "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
                "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
              },
              {
                "type": "object"
              },
              {
                "type": "array",
                "items": {
                  "anyOf": [
                    {
                      "type": "string",
                      "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
                      "description": "格式: field_id (字母/数字/下划线, 首字符为字母或下划线)",
                      "markdownDescription": "格式: `field_id`.\n\n- 仅允许字母/数字/下划线\n- 首字符必须是字母或下划线"
                    },
                    {
                      "type": "object"
                    }
                  ]
                },
                "minItems": 1
              }
            ]
          },
          "minItems": 1,
          "description": "输出字段顺序(field_id/out_field_id 列表; 支持 YAML alias)",
          "markdownDescription": "输出字段顺序(`field_id/out_field_id` 列表).\n\n两类输出的语义:\n\n- 明细输出(未声明 `aggregate`): `fields` **必填**,用于选择并编排行输出字段.\n- 聚合输出(声明了 `aggregate`): `fields` **可选**,用于 derived output 的输出编排(select + order).\n  - 可引用范围: `aggregate.group_by` + `aggregate.fields` 的 key\n  - 若省略 `fields`,则使用默认 derived output layout(实现细节;不承诺顺序,强合同请显式声明)\n\n支持两种等价写法:\n\n1) 直接写 `field_id` 字符串:\n\n```yaml\nfields: [order_id, total]\n```\n\n2) 使用 YAML alias 引用字段定义对象以推导 `field_id`:\n\n```yaml\nmain_source:\n  fields:\n    quantity: &quantity {extract: quantity}\noutputs:\n  - name: detail\n    container: {type: csv, path: ./out.csv}\n    fields:\n      - *quantity\n      - \"order_id\"\n```\n\n注意: YAML merge(`<<`) 可能产生新对象并丢失 alias identity;此时建议直接使用字符串 `field_id`.\n\n可通过 `from` 继承.",
          "examples": [
            [
              "order_id",
              "user_id"
            ]
          ]
        },
        "where": {
          "type": "string",
          "description": "可选:过滤表达式(安全表达式)",
          "markdownDescription": "可选:行级过滤谓词(安全表达式),等价 SQL `WHERE`.\n\n执行阶段:\n- 明细输出: 写出前对每行执行,命中才写入该 output\n- 聚合输出: `group_by` 之前对每行执行,只有命中的行参与聚合\n\n变量来源:\n- 表达式可引用当前行的字段值,来自 demand 的 `fields.<field_id>`(包含 relation/derived 后的字段)\n- 系统会在编译期静态提取表达式依赖字段并注入 required fields\n- 只保证表达式引用到的字段会被准备;未引用字段可能为 `None`\n\n限制:\n- 仅支持受限表达式(禁止任意 import)\n- `where` 是按行过滤/路由,不是“是否启用 sheet/output”的开关;该能力应由未来独立字段提供(例如 `enabled_if`)\n- `where` 不能引用聚合后输出字段(例如 `aggregate.fields.*` 产生的指标/排名/派生字段)",
          "examples": [
            "channel == 'direct'"
          ]
        },
        "aggregate": {
          "description": "可选:派生汇总配置(声明后视为 derived output)",
          "markdownDescription": "可选:派生汇总配置(声明后视为 derived output)",
          "allOf": [
            {
              "$ref": "#/definitions/output_aggregate"
            }
          ]
        },
        "$import": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ],
          "description": "$import 引用(支持 string 或 string list)",
          "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
          "examples": [
            "common.sources",
            [
              "common.sources",
              "other.sources"
            ]
          ]
        }
      },
      "anyOf": [
        {
          "required": [
            "name"
          ]
        },
        {
          "required": [
            "$import"
          ]
        }
      ],
      "additionalProperties": false
    },
    "output_extra_sheet": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "可选:工作簿路径(缺省使用 primary workbook)",
          "markdownDescription": "可选:工作簿路径(缺省使用 primary workbook).",
          "examples": [
            "./output/report.xlsx"
          ]
        },
        "sheet": {
          "type": "string",
          "minLength": 1,
          "description": "sheet 名称",
          "markdownDescription": "sheet 名称.",
          "examples": [
            "__meta__",
            "__audit__"
          ]
        },
        "allow_formulas": {
          "type": "boolean",
          "description": "可选:允许 Excel 公式(缺省使用 primary workbook 的容器配置)",
          "markdownDescription": "可选:允许 Excel 公式(缺省使用 primary workbook 的容器配置).",
          "examples": [
            false
          ]
        },
        "write_lock": {
          "type": "boolean",
          "description": "可选:写锁(缺省使用 primary workbook 的容器配置)",
          "markdownDescription": "可选:写锁(缺省使用 primary workbook 的容器配置).",
          "examples": [
            true
          ]
        },
        "$import": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ],
          "description": "$import 引用(支持 string 或 string list)",
          "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
          "examples": [
            "common.sources",
            [
              "common.sources",
              "other.sources"
            ]
          ]
        }
      },
      "additionalProperties": false
    },
    "logging": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "启用日志观测",
          "markdownDescription": "启用日志观测.",
          "default": true
        },
        "renderer": {
          "type": "string",
          "description": "日志渲染器(pretty/logger)",
          "markdownDescription": "日志渲染器.\n\n- `pretty`: 输出到 pretty console(如 panel/table)\n- `logger`: 输出到标准 logger",
          "enum": [
            "pretty",
            "logger"
          ],
          "default": "pretty",
          "examples": [
            "pretty"
          ]
        },
        "$import": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ],
          "description": "$import 引用(支持 string 或 string list)",
          "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
          "examples": [
            "common.sources",
            [
              "common.sources",
              "other.sources"
            ]
          ]
        }
      },
      "additionalProperties": false
    },
    "performance_thresholds": {
      "type": "object",
      "properties": {
        "batch_duration_warn": {
          "description": "批次耗时告警阈值(秒)",
          "markdownDescription": "批次耗时告警阈值(秒)"
        },
        "memory_increase_warn": {
          "description": "内存增长告警阈值(MB)",
          "markdownDescription": "内存增长告警阈值(MB)"
        },
        "$import": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ],
          "description": "$import 引用(支持 string 或 string list)",
          "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
          "examples": [
            "common.sources",
            [
              "common.sources",
              "other.sources"
            ]
          ]
        }
      },
      "additionalProperties": false
    },
    "performance_report": {
      "type": "object",
      "properties": {
        "format": {
          "type": "string",
          "description": "报告输出格式",
          "markdownDescription": "报告输出格式.\n\n- `console`: 控制台输出\n- `json`: JSON 文件\n- `csv`: CSV 文件\n- `none`: 不输出报告",
          "enum": [
            "console",
            "json",
            "csv",
            "none"
          ],
          "default": "console",
          "examples": [
            "console"
          ]
        },
        "output": {
          "type": "string",
          "description": "报告输出路径",
          "markdownDescription": "报告输出路径"
        },
        "include_details": {
          "type": "boolean",
          "description": "包含详细统计",
          "markdownDescription": "包含详细统计.",
          "default": false
        },
        "$import": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ],
          "description": "$import 引用(支持 string 或 string list)",
          "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
          "examples": [
            "common.sources",
            [
              "common.sources",
              "other.sources"
            ]
          ]
        }
      },
      "additionalProperties": false
    },
    "performance": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "启用性能监控",
          "markdownDescription": "启用性能监控.",
          "default": false
        },
        "metrics": {
          "type": "array",
          "items": {
            "enum": [
              "duration",
              "memory",
              "cpu"
            ]
          },
          "description": "要收集的指标类型 (duration/memory/cpu)",
          "markdownDescription": "要收集的指标类型.\n\n- 可选: duration / memory / cpu"
        },
        "sampling_interval": {
          "type": "integer",
          "description": "资源采样间隔(批次数)",
          "markdownDescription": "资源采样间隔(按批次计).",
          "minimum": 1,
          "default": 1
        },
        "report": {
          "$ref": "#/definitions/performance_report"
        },
        "thresholds": {
          "$ref": "#/definitions/performance_thresholds"
        },
        "$import": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ],
          "description": "$import 引用(支持 string 或 string list)",
          "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
          "examples": [
            "common.sources",
            [
              "common.sources",
              "other.sources"
            ]
          ]
        }
      },
      "additionalProperties": false
    },
    "relation_report": {
      "type": "object",
      "properties": {
        "format": {
          "type": "string",
          "description": "报告输出格式",
          "markdownDescription": "报告输出格式.\n\n- `console`: 控制台输出\n- `json`: JSON 文件\n- `none`: 不输出报告",
          "enum": [
            "console",
            "json",
            "none"
          ],
          "default": "console",
          "examples": [
            "console"
          ]
        },
        "output": {
          "type": "string",
          "description": "报告输出路径",
          "markdownDescription": "报告输出路径"
        },
        "$import": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ],
          "description": "$import 引用(支持 string 或 string list)",
          "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
          "examples": [
            "common.sources",
            [
              "common.sources",
              "other.sources"
            ]
          ]
        }
      },
      "additionalProperties": false
    },
    "relations": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "启用关联可观测性",
          "markdownDescription": "启用关联可观测性.",
          "default": false
        },
        "sampling_rate": {
          "description": "采样率(0.0-1.0)",
          "markdownDescription": "采样率(0.0-1.0).",
          "minimum": 0.0,
          "maximum": 1.0,
          "default": 0.01
        },
        "log_type_mismatch": {
          "type": "boolean",
          "description": "记录类型不匹配日志",
          "markdownDescription": "记录类型不匹配日志.",
          "default": true
        },
        "max_samples": {
          "type": "integer",
          "description": "最大采样数量",
          "markdownDescription": "最大采样数量.",
          "minimum": 0,
          "default": 1000
        },
        "report": {
          "$ref": "#/definitions/relation_report"
        },
        "$import": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ],
          "description": "$import 引用(支持 string 或 string list)",
          "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
          "examples": [
            "common.sources",
            [
              "common.sources",
              "other.sources"
            ]
          ]
        }
      },
      "additionalProperties": false
    },
    "viz": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "启用 Scalim Viz 输出",
          "markdownDescription": "启用 Scalim Viz 输出.",
          "default": false
        },
        "output_dir": {
          "type": "string",
          "description": "输出目录(自动追加 scalim-viz)",
          "markdownDescription": "输出目录(自动追加 scalim-viz).\n\n- 写入路径: output_dir/scalim-viz/<run_id>/viz_*.json\n- 若 output_dir 已包含 scalim-viz,则直接使用\n- 若显式配置 output_path/snapshot_path,则忽略该规则"
        },
        "output_path": {
          "type": "string",
          "description": "事件输出文件路径(可选)",
          "markdownDescription": "事件输出文件路径(可选)."
        },
        "snapshot_path": {
          "type": "string",
          "description": "快照输出文件路径(可选)",
          "markdownDescription": "快照输出文件路径(可选)."
        },
        "append": {
          "type": "boolean",
          "description": "事件文件追加写入",
          "markdownDescription": "事件文件追加写入(可选).\n\n- 默认 `false`: 每次运行会覆盖 `output_path` 对应文件,避免跨 run 混写\n- 设为 `true` 时,将以 JSONL 追加写入(旧行为)\n- `output_dir` 写入到按 run 隔离目录时,通常不需要开启",
          "default": false,
          "examples": [
            false
          ]
        },
        "trace_enabled": {
          "type": "boolean",
          "description": "启用高频 trace 输出",
          "markdownDescription": "启用高频 trace 输出(可选).\n\n- `false`(默认): 仅输出编排级事件到 `viz_events.jsonl`\n- `true`: 额外输出高频 trace 事件到 `viz_trace.jsonl`(例如 field/row/relation lookup)\n\n建议: 默认关闭;需要深挖时在 UI 勾选加载 trace 并配合过滤/步进 lens 使用.\n\n旧字段 `observability.viz.event_mode` 已移除,请使用 `trace_enabled`.",
          "default": false,
          "examples": [
            false
          ]
        },
        "payload_policy": {
          "type": "string",
          "description": "事件 payload 策略",
          "markdownDescription": "事件 payload 策略.\n\n- `none`: 不输出 payload\n- `summary`: 仅摘要\n- `sample`: 抽样\n- `full`: 全量",
          "enum": [
            "none",
            "summary",
            "sample",
            "full"
          ],
          "default": "summary",
          "examples": [
            "summary"
          ]
        },
        "sample_size": {
          "type": "integer",
          "description": "sample 策略下的样本数量",
          "markdownDescription": "sample 策略下的样本数量.",
          "minimum": 0,
          "default": 5
        },
        "run_name": {
          "type": "string",
          "description": "运行名称",
          "markdownDescription": "运行名称(可选)."
        },
        "env": {
          "type": "string",
          "description": "运行环境标签",
          "markdownDescription": "运行环境标签(可选)."
        },
        "use_default_output_dir": {
          "type": "boolean",
          "description": "使用默认输出目录",
          "markdownDescription": "使用默认输出目录(~/.config/scalim-viz 等).",
          "default": false
        },
        "$import": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ],
          "description": "$import 引用(支持 string 或 string list)",
          "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
          "examples": [
            "common.sources",
            [
              "common.sources",
              "other.sources"
            ]
          ]
        }
      },
      "additionalProperties": false
    },
    "trace": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "启用执行追踪",
          "markdownDescription": "启用执行追踪.",
          "default": false
        },
        "$import": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ],
          "description": "$import 引用(支持 string 或 string list)",
          "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
          "examples": [
            "common.sources",
            [
              "common.sources",
              "other.sources"
            ]
          ]
        }
      },
      "additionalProperties": false
    },
    "row_gap": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "启用行缺口统计",
          "markdownDescription": "启用行缺口统计.",
          "default": false
        },
        "primary_loader_name": {
          "type": "string",
          "description": "主数据 loader 名称",
          "markdownDescription": "主数据 loader 名称.",
          "default": "primary_keys"
        },
        "data_loader_names": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "参与统计的 loader 列表",
          "markdownDescription": "参与统计的 loader 列表."
        },
        "sample_limit": {
          "type": "integer",
          "description": "缺口采样数量",
          "markdownDescription": "缺口采样数量.",
          "minimum": 0,
          "default": 5
        },
        "$import": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ],
          "description": "$import 引用(支持 string 或 string list)",
          "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
          "examples": [
            "common.sources",
            [
              "common.sources",
              "other.sources"
            ]
          ]
        }
      },
      "additionalProperties": false
    },
    "memory_opt": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "启用内存优化统计",
          "markdownDescription": "启用内存优化统计.",
          "default": false
        },
        "auto_report": {
          "type": "boolean",
          "description": "自动输出摘要",
          "markdownDescription": "自动输出摘要.",
          "default": false
        },
        "max_fields": {
          "type": "integer",
          "description": "摘要字段上限",
          "markdownDescription": "摘要字段上限(0 表示不限制).",
          "minimum": 0,
          "default": 0
        },
        "$import": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ],
          "description": "$import 引用(支持 string 或 string list)",
          "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
          "examples": [
            "common.sources",
            [
              "common.sources",
              "other.sources"
            ]
          ]
        }
      },
      "additionalProperties": false
    },
    "observability": {
      "type": "object",
      "properties": {
        "logging": {
          "description": "日志观测配置",
          "markdownDescription": "日志观测配置.\n\n- 控制日志输出开启/关闭",
          "allOf": [
            {
              "$ref": "#/definitions/logging"
            }
          ]
        },
        "performance": {
          "description": "性能可观测性配置",
          "markdownDescription": "性能可观测性配置.\n\n- 关注耗时/内存/CPU 等指标",
          "allOf": [
            {
              "$ref": "#/definitions/performance"
            }
          ]
        },
        "relations": {
          "description": "关联可观测性配置",
          "markdownDescription": "关联可观测性配置.\n\n- 关注关联步骤的采样与类型校验",
          "allOf": [
            {
              "$ref": "#/definitions/relations"
            }
          ]
        },
        "viz": {
          "description": "Scalim Viz 可视化输出配置",
          "markdownDescription": "Scalim Viz 可视化输出配置.\n\n- 输出 viz_snapshot.json + viz_events.jsonl",
          "allOf": [
            {
              "$ref": "#/definitions/viz"
            }
          ]
        },
        "trace": {
          "description": "执行追踪配置",
          "markdownDescription": "执行追踪配置.\n\n- 记录批次级执行步骤",
          "allOf": [
            {
              "$ref": "#/definitions/trace"
            }
          ]
        },
        "row_gap": {
          "description": "行缺口统计配置",
          "markdownDescription": "行缺口统计配置.\n\n- 统计 loader 期望/实际行数差异",
          "allOf": [
            {
              "$ref": "#/definitions/row_gap"
            }
          ]
        },
        "memory_opt": {
          "description": "内存优化统计配置",
          "markdownDescription": "内存优化统计配置.\n\n- 汇总字段瘦身/行释放等事件",
          "allOf": [
            {
              "$ref": "#/definitions/memory_opt"
            }
          ]
        },
        "$import": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ],
          "description": "$import 引用(支持 string 或 string list)",
          "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
          "examples": [
            "common.sources",
            [
              "common.sources",
              "other.sources"
            ]
          ]
        }
      },
      "additionalProperties": false
    },
    "guardrails_loader": {
      "type": "object",
      "properties": {
        "validate_result": {
          "type": "boolean",
          "description": "校验 loader 返回结构(契约校验)",
          "markdownDescription": "校验 loader 返回结构(契约校验).\n\n- 启用时,契约违规始终 fast_fail(即使 mode=quiet)",
          "default": false
        },
        "required_fields": {
          "type": "array",
          "items": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "object"
              }
            ]
          },
          "description": "关键字段列表(缺失/None 触发护栏;支持 field_id 字符串或 YAML alias)",
          "markdownDescription": "关键字段列表.\n\n- 缺失/None 触发护栏\n- 为空表示不启用缺失检查\n- 每项支持 `field_id` 字符串或 YAML alias(指向已定义字段对象)\n- YAML merge(`<<`) 会生成新对象并丢失 alias 身份; merge 产物请用字符串 field_id"
        },
        "on_transform_error": {
          "type": "string",
          "description": "字段转换异常策略(可选;默认继承 mode)",
          "markdownDescription": "字段转换异常策略.\n\n- 作用于 extractor/value_cast/value_formatter/transform 等异常\n- 为空则继承 guardrails.mode",
          "enum": [
            "quiet",
            "fast_fail"
          ],
          "examples": [
            "fast_fail"
          ]
        },
        "$import": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ],
          "description": "$import 引用(支持 string 或 string list)",
          "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
          "examples": [
            "common.sources",
            [
              "common.sources",
              "other.sources"
            ]
          ]
        }
      },
      "additionalProperties": false
    },
    "guardrails_relations": {
      "type": "object",
      "properties": {
        "null_key_max_rate": {
          "description": "关联 null_key 最大比例(0.0-1.0;未设置则不启用)",
          "markdownDescription": "关联 null_key 最大比例.\n\n- 未设置则不启用阈值护栏\n- 默认对全部关联 lookup step 生效",
          "minimum": 0.0,
          "maximum": 1.0
        },
        "type_error_max_rate": {
          "description": "关联 type_error 最大比例(0.0-1.0;未设置则不启用)",
          "markdownDescription": "关联 type_error 最大比例.\n\n- 未设置则不启用阈值护栏\n- 默认对全部关联 lookup step 生效",
          "minimum": 0.0,
          "maximum": 1.0
        },
        "$import": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ],
          "description": "$import 引用(支持 string 或 string list)",
          "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
          "examples": [
            "common.sources",
            [
              "common.sources",
              "other.sources"
            ]
          ]
        }
      },
      "additionalProperties": false
    },
    "guardrails_compute": {
      "type": "object",
      "properties": {
        "on_error": {
          "type": "string",
          "description": "派生字段 compute 异常策略(可选;默认继承 mode)",
          "markdownDescription": "派生字段 compute 异常策略.\n\n- 为空则继承 guardrails.mode",
          "enum": [
            "quiet",
            "fast_fail"
          ],
          "examples": [
            "fast_fail"
          ]
        },
        "$import": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ],
          "description": "$import 引用(支持 string 或 string list)",
          "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
          "examples": [
            "common.sources",
            [
              "common.sources",
              "other.sources"
            ]
          ]
        }
      },
      "additionalProperties": false
    },
    "guardrails": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "启用运行时护栏(默认关闭)",
          "markdownDescription": "启用运行时护栏.\n\n- 默认关闭(不改变现有行为)\n- 显式 enabled=true 才生效",
          "default": false
        },
        "mode": {
          "type": "string",
          "description": "护栏模式(quiet/fast_fail)",
          "markdownDescription": "护栏模式.\n\n- `quiet`: 不抛异常,但 best-effort 记录错误事件\n- `fast_fail`: 首次触发即失败并终止 pipeline",
          "enum": [
            "quiet",
            "fast_fail"
          ],
          "default": "fast_fail",
          "examples": [
            "fast_fail"
          ]
        },
        "loader": {
          "$ref": "#/definitions/guardrails_loader"
        },
        "relations": {
          "$ref": "#/definitions/guardrails_relations"
        },
        "compute": {
          "$ref": "#/definitions/guardrails_compute"
        },
        "$import": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ],
          "description": "$import 引用(支持 string 或 string list)",
          "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
          "examples": [
            "common.sources",
            [
              "common.sources",
              "other.sources"
            ]
          ]
        }
      },
      "additionalProperties": false
    },
    "loader_retry": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Loader retry 策略(可选;默认关闭)",
          "markdownDescription": "Loader retry 策略.\n\n- 默认关闭: `enabled: false`\n- 启用后会对 loader 调用的瞬态错误做有限重试\n- 需要提供 `should_retry` 回调(安全引用),用于决定是否重试\n- 受硬上限保护: max_attempts<=5, max_elapsed_seconds<=20, max_delay_seconds<=5\n\n\n注意:\n- 当 enabled=true 时需要提供 `should_retry`\n- 若未提供 `should_retry`, 仅当 driver 注入提供回调时才允许启用",
          "default": false
        },
        "should_retry": {
          "type": "string",
          "description": "重试判定回调引用(安全引用,由 allowlist 约束)",
          "markdownDescription": "重试判定回调引用.\n\n- 形式与 `loader` 引用一致:\n  - 绝对引用: `module.path.function` / `module.path:function` / `module.path:obj.method`\n  - 相对引用: 以 `.` / `..` 开头的 module path(相对 YAML 文件所在目录)\n- 相对引用会在运行期先归一化为绝对引用,并继续受 allowlist(allowed_modules/allowed_functions) 约束\n- 签名: `should_retry(exc, ctx) -> bool`",
          "examples": [
            "myapp.retry:should_retry_db",
            ".retry:should_retry_db"
          ]
        },
        "max_attempts": {
          "type": "integer",
          "description": "最大尝试次数(含首次)",
          "markdownDescription": "最大尝试次数(含首次调用).\n\n- 受硬上限保护: <= 5",
          "minimum": 1,
          "maximum": 5,
          "default": 3,
          "examples": [
            3
          ]
        },
        "max_elapsed_seconds": {
          "description": "最大累计耗时(秒,包含 sleep)",
          "markdownDescription": "最大累计耗时(秒,包含 sleep).\n\n- 受硬上限保护: <= 20",
          "minimum": 1e-06,
          "maximum": 20.0,
          "default": 10.0,
          "examples": [
            10.0
          ]
        },
        "backoff": {
          "type": "string",
          "description": "退避策略(fixed/exponential)",
          "markdownDescription": "退避策略.\n\n- `fixed`: 固定等待\n- `exponential`: 指数退避",
          "enum": [
            "fixed",
            "exponential"
          ],
          "default": "exponential",
          "examples": [
            "exponential"
          ]
        },
        "base_delay_seconds": {
          "description": "基础等待时间(秒)",
          "markdownDescription": "基础等待时间(秒).\n\n- fixed: 每次等待 base_delay\n- exponential: base_delay * 2**(attempt-1)",
          "minimum": 0.0,
          "default": 0.2,
          "examples": [
            0.2
          ]
        },
        "max_delay_seconds": {
          "description": "最大单次等待时间(秒)",
          "markdownDescription": "最大单次等待时间(秒).\n\n- 受硬上限保护: <= 5",
          "minimum": 0.0,
          "maximum": 5.0,
          "default": 2.0,
          "examples": [
            2.0
          ]
        },
        "jitter": {
          "type": "boolean",
          "description": "启用 jitter(随机扰动)",
          "markdownDescription": "启用 jitter(随机扰动),避免重试风暴.\n\n- true: 在 [0, delay] 区间内随机\n- false: 精确使用 delay",
          "default": true,
          "examples": [
            true
          ]
        },
        "$import": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ],
          "description": "$import 引用(支持 string 或 string list)",
          "markdownDescription": "$import 引用.\n\n- string: `<alias>(.<segment>)*`\n- list: 按顺序合并,后者覆盖前者,最终再被本地覆盖\n- 仅支持 mapping 片段\n- V1 仅支持同级文件导入(见顶层 `imports`)",
          "examples": [
            "common.sources",
            [
              "common.sources",
              "other.sources"
            ]
          ]
        }
      },
      "additionalProperties": false
    }
  },
  "markdownDescription": "Scalim 框架 YAML 需求配置定义 Schema.\n\n入口字段: `main_source` / `sources` / `fields`.\n不再支持 legacy 字段: relations_sql_like / relations_graph / foreign_key / target / from / via / column / pk / pk_transform / derived.",
  "additionalProperties": false
}
