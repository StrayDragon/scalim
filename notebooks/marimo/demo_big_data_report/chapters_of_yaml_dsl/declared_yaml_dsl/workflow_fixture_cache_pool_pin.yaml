# yaml-language-server: $schema=../../../../../src/scalim/dsl/yaml_dsl/schema/workflow.gen.json

workflow:
  # <!-- BEGIN AUTOGEN:workflow.runs -->
  runs:
    - id: r1
      demand: workflow_fixture_demand.yaml
    - id: r2
      demand: workflow_fixture_demand.yaml
  # <!-- END AUTOGEN:workflow.runs -->
