def load_orders():
    """相对模块 `go-to-definition` 演示用加载器.

    说明:
      - 这是 `notebooks/` 下的演示模块.
      - `YAML DSL LSP` 通过 `filesystem` + `AST` 静态解析定义位置.
      - `LSP` 不会执行该函数.
    """
    return []
