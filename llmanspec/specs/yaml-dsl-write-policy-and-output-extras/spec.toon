kind: llman.sdd.spec
name: "yaml-dsl-write-policy-and-output-extras"
purpose: "明确输出资源的四层边界：resources 声明、write_defaults 策略、outputs 内容编排、runtime output extras（meta/audit），并将 write policy 和 extras 迁出 YAML 主线。 [scope-review-2026-07-13-c25-xlsx-ir-path-presence]"
valid_scope[1]: src/scalim/
requirements[4]{req_id,title,statement}:
  r133,`resources` MUST distinguish authoring declarations from runtime overlays,"输出资源面 MUST 明确区分“authoring 声明”与“runtime overlay”: - YAML `resources.books/files` MUST 表达基础资源声明 - workflow 资源覆盖与 `RunOverrides.resources` MUST 作为 overlay / deep-merge 生效 - overlay MUST 不改变 `resources` 作为资源 identity 与目标声明面的职责"
  r375,workbook write policy MUST use Python BookWritePolicy as the single SSOT,"workbook 写策略 MUST 以 Python typed BookWritePolicy（或 ResourcesPolicy.books.*.write）为单一 SSOT: - workbook 级写入行为（mode/align_by/header_policy/on_mismatch/on_conflict）MUST 由 Python runtime policy 表达 - YAML resources.books.*.write_defaults MUST NOT 再作为 authoring SSOT（出现 MUST fail-fast） - outputs[*].write MUST 继续仅承载 output-local 的最小展示/表头 override - outputs[*].write MUST NOT 承载 workbook 级 mode/align_by/header_policy/on_mismatch/on_conflict"
  r493,`meta` and `audit` MUST be runtime output extras instead of YAML authoring field,"`meta` 与 `audit` MUST 从 YAML 主线迁出,并收敛为 runtime typed output extras: - YAML 主线 MUST 不再把 `meta` / `audit` 作为稳定 authoring 字段 - runtime output extras MUST 明确其 workbook 依赖与输出上下文"
  r572,docs and overrides contracts MUST reflect YAML declaration vs Python write policy,"输出相关文档与 typed overrides/policy 契约 MUST 反映以下四层分工: - resources.books/files = YAML 输出目标声明（id/variant/path） - Python BookWritePolicy/BookBudgetPolicy = workbook 写策略与预算 SSOT - outputs[*].to/fields/... = output 内容编排 - runtime output extras = meta/audit 等附加产物"
scenarios[6]{req_id,id,given,when,then,feature}:
  r133,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r133,"workflow-and-user-overlays-deep-merge-over-declared-resource",某个 demand YAML 声明了基础 `resources.books.report`,系统编译并运行该输出,"系统 MUST 以基础声明为底并按 overlay / deep-merge 合成最终资源配置",true
  r375,"append-via-python-policy",两个 demand outputs 绑定同一 book 同一 sheet 且需要 append 合并,调用方通过 Python BookWritePolicy(mode=append) 运行 workflow,系统 MUST 按 append 语义合并写入且 MUST NOT 要求 YAML 声明 write_defaults,true
  r493,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r493,"audit-sheet-is-configured-by-runtime-output-extras","",用户需要输出 metadata 或 audit workbook sheet,系统 MUST 通过 runtime typed output extras 完成装配,true
  r572,"docs-explain-python-write-ssot","",用户查阅 YAML DSL 输出或 workflow 资源文档,文档 MUST 说明 write_defaults/budget 已迁出 YAML 并由 Python policy 配置,true