kind: llman.sdd.spec
name: "yaml-dsl-workflow-validate"
purpose: "提供面向 CI/预发布的 workflow-level validate CLI 入口，在不执行 workflow 的前提下对 workflow YAML 及其引用的 demands 做静态/编译期校验。 [scope-review-2026-07-12-c20]"
valid_scope[1]: src/scalim/
requirements[2]{req_id,title,statement}:
  r1,"CLI MUST provide workflow-level validate that recursively validates referenced d","系统 MUST 提供一个面向 CI/预发布的 workflow-level validate CLI 入口，用于在不执行 workflow 的前提下，对 workflow YAML 及其引用的 demands 做静态/编译期校验。 该入口 MUST 支持形如： - `yaml-dsl validate --type workflow <workflow.yaml>` 校验范围 MUST 至少包含： - workflow YAML 自身结构与语义校验（解析、引用合法性、cycle detection 等） - 递归校验每个 `runs[*].demand` 引用的 demand YAML（允许 imports/$import，并在错误中提供可诊断的引用链路） - workflow ↔ demand 的交叉一致性校验（例如 demand outputs 绑定到的资源 id 必须能解析到某个有效的资源 mapping）: - `to.book` ↔ `resources.books` - `to.file` ↔ `resources.files` - 对旧 `outputs[*].container` 给出迁移诊断"
  r2,workflow validate MUST share YAML load and error envelope with demand compile,"系统 MUST 要求 workflow validate 与 demand compile/run 在以下方面保持一致： - YAML load（包括 duplicate key 检测） - imports fragments 的处理（若 workflow 支持） - location index 与 ErrorEnvelope 结构"
scenarios[6]{req_id,id,given,when,then}:
  r1,baseline,"","TODO: describe the trigger","TODO: describe the expected result"
  r1,"validate-fails-when-a-demand-binds-to-an-unknown-book-id","workflow YAML 中某个 `runs[*].demand` 引用的 demand YAML 声明 `outputs[0].to.book: \"report\"`",调用方执行 workflow validate CLI,校验 MUST 失败（非零退出码）
  r1,"validate-fails-when-a-demand-binds-to-an-unknown-file-id","workflow YAML 中某个 `runs[*].demand` 引用的 demand YAML 声明 `outputs[0].to.file: \\\"detail_csv\\\"`",调用方执行 workflow validate CLI,校验 MUST 失败（非零退出码）
  r1,"validate-fails-when-a-demand-still-uses-container","workflow YAML 中某个 `runs[*].demand` 引用的 demand YAML 仍声明 `outputs[0].container`",调用方执行 workflow validate CLI,校验 MUST 失败（非零退出码）
  r2,baseline,"","TODO: describe the trigger","TODO: describe the expected result"
  r2,"same-yaml-yields-the-same-failure-in-workflow-validate-and-c",某份 workflow YAML 包含 duplicate keys 或语法错误,维护者分别运行 workflow validate 与相同 loader 入口,两者 MUST 产生一致的错误结构与定位口径