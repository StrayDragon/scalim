kind: llman.sdd.spec
name: "yaml-dsl-lsp-server"
purpose: "定义 YAML DSL LSP server 的语义 contract：诊断（diagnostics）与 Python 引用的 definition/hover/completion，并要求 server 侧复用 shared core， 以保证跨编辑器一致、静态无副作用且可诊断降级（不 crash、不退出、不依赖 shell-out CLI）。 [scope-review-2026-07-13-c25-xlsx-ir-path-presence]"
valid_scope[1]: src/scalim/
requirements[22]{req_id,title,statement}:
  r120,YAML DSL LSP server MUST NOT shell out to CLI and MUST reuse scalim library sema,"系统 MUST 定义并支持一个 YAML DSL 语义 LSP server,其实现约束如下: - LSP server MUST 以 `scalim` 作为 library 依赖复用 validator/schema/解析逻辑 - LSP server MUST NOT 通过 shell-out 调用 `PROJECT_CLI_NAME` 或读取 CLI 文本输出再解析 - LSP server MUST 在缺失可选依赖时降级为可诊断的 warning(而不是崩溃/无响应)"
  r362,"LSP server MUST avoid polluting non-DSL YAML documents","系统 MUST 在提供 YAML DSL 语义能力前进行 DSL 探测,并满足: - 对非 DSL YAML,server MUST 发布空 diagnostics - 对非 DSL YAML,server MUST 对 go-to-definition/hover/completion 返回空结果(不得 crash) - DSL 探测 MUST 优先依据 schema 顶层 required: - 当 YAML 根 mapping 包含键 `workflow` 且其值为 mapping 时,该文件 MUST 被视为 workflow DSL - 当 YAML 根 mapping 同时包含键 `name` 与 `main_source` 时,该文件 MUST 被视为 demand DSL - 当 required 未满足时,server MAY 使用 DSL 专属语法特征作为 permissive fallback（例如 `$import/$init_var`、`loader/call_by`、schema modeline 指向 scalim schema）"
  r483,demand diagnostics MUST match CLI validate semantics (path + location),"对 demand YAML,系统 MUST 保证 LSP diagnostics 与 `PROJECT_CLI_NAME yaml-dsl validate` 的语义一致: - 逻辑路径 MUST 使用 canonical 点号口径(数组索引用数字段) - diagnostics MUST 能映射到稳定的源码位置 range(尽可能精确到字段/值) - 对同一错误形态,CLI 与 LSP 的 message SHOULD 保持一致(差异仅限展示格式)"
  r564,"workflow diagnostics v1 MUST stay schema-based and MAY emit static book alias migrations","对 workflow YAML,v1 diagnostics MUST 以 schema + unknown-fields 为基线: - MUST 基于 workflow.gen.json 与对应 unknown-fields 规则 - MUST NOT 进行 runtime compile/run 或递归 demand 执行类语义诊断 - 额外地,对 workflow.resources.books 下已硬删的 authoring 别名(xlsx_file/xlsx_memory/kind 等),系统 MUST 发出与 demand 侧一致的静态迁移诊断(可复制迁移示例),且 MUST 保持静态无副作用"
  r627,Go to Definition MUST resolve `loader`/`call_by` references statically,"系统 MUST 为 `loader`/`call_by` 等 Python 引用字段提供 go-to-definition,且解析 MUST 为静态解析(不执行用户代码): - 引用格式 MUST 支持 `module:attr` 与 `module.attr` - 引用格式 MUST 支持相对模块引用（前导 `.`），例如 `.loaders:func` / `..loaders:func` - 对 `call_by: \"pkg.mod:fn(arg=...)\"` 形态,definition 至少 MUST 能解析并处理 `pkg.mod:fn`（参数段忽略） - 对 YAML block scalar（`|`/`>` 及其变体）中的 multiline `loader/call_by`，definition MUST 仍可在光标命中 token 时工作（range 可为“当前行内 token range”） - 定义定位 MUST 基于 project discovery 的 `python_roots` 与文件系统/AST 分析 - 当引用为相对模块时，系统 MUST 基于文档 URI 推导 `yaml_path` 并交由 shared core 在 `yaml_path + python_roots` 上下文内完成规范化与解析 - 对 `module:obj.method` 形态，系统 MUST 复用 shared core 的静态推断能力，尽可能把第一个 location 定位到真实实现（例如 `Klass.method`） - definition MUST 支持返回多个 locations，且 MUST 稳定排序 + 去重： - locations MUST 稳定排序：真实实现优先；同优先级按 path → line 排序 - locations MUST 去重：同 URI + 同 range 视为同一候选 - 解析失败时 MUST 返回空结果且给出可诊断信息(不得 crash)"
  r674,Go to Definition MUST support `$import` references statically,"系统 MUST 为 demand YAML 中的 `$import` 引用提供 go-to-definition，且解析 MUST 为静态解析（不执行用户代码，不 shell-out CLI）： - `$import` 引用格式 MUST 支持 `<alias>(.<segment>)*` - 系统 MUST 基于当前文档顶层 `imports` 映射解析 `<alias>` 对应的 fragment 来源 - 系统 MUST 支持 `scalim.yaml` 中的 `import_roots` 重写 imports 路径解析（与 runtime imports 解析一致） - 若 `$import` 引用可解析为 fragment YAML 文件与目标 mapping 位置，系统 MUST 返回该位置的 `Location` - 解析失败 MUST 返回空结果且给出可诊断 warnings（不得 crash）"
  r714,Hover MUST provide docstring for resolvable Python references and degrade gracef,"系统 MUST 为 Python 引用字段提供 hover（docstring），且失败时 MUST 降级为“空结果 + warnings”（不得 crash）： - hover MUST 在可解析时返回 docstring（PlainText 即可） - hover MUST 支持相对模块引用（前导 `.`），其 base 推导与 go-to-definition 一致 - 对 YAML block scalar 中的 multiline `loader/call_by`，hover MUST 仍可在光标命中 token 时工作（range 可为“当前行内 token range”） - 解析失败 MUST 返回空 hover，并包含可诊断 warnings"
  r747,Hover MUST explain resolvable `$import` references and degrade gracefully,"系统 MUST 为 `$import` 引用提供 hover，并满足： - 若 `$import` 引用可解析，hover MUST 返回 PlainText（至少包含解析后的 fragment 来源文件路径与 ref logical path） - 若引用不可解析，hover MUST 返回空结果并提供可诊断 warnings（不得 crash）"
  r775,Completion MUST provide minimal symbol completions within Python reference strin,"系统 MUST 为 Python 引用字段提供最小 completion,并满足： - completion MUST 仅在光标位于引用字符串范围内触发（避免误触发） - completion MUST 支持相对模块引用（前导 `.`），其 base 推导与 go-to-definition 一致 - 对 YAML block scalar 中的 multiline `loader/call_by`，completion MUST 仍可在光标命中 token/value_range 时工作 - 失败 MUST 降级为“空结果 + warnings”（不得 crash）"
  r154,"LSP server MUST provide field intelligence for field-id tokens inside `call_by`","系统 MUST 在 `call_by` 字符串参数段内，为 kwargs 的 `=` **右侧** field-id token 提供 field 智能（completion/hover/definition），并满足： - definition MUST 跳转到字段声明（含跨 imports 展开的真实声明位置） - hover SHOULD 展示字段摘要（与 compute/where 的字段卡片一致），不可解析时 MUST 返回空但不得崩溃 - completion MUST 支持 Ctrl+Space 手动触发，并能在 `x=` 的空值场景返回候选列表 - `=` 左侧 kwargs 名称 MUST NOT 被当作 field-id（hover/definition 返回空） - 该能力 MUST 支持 multiline `call_by`（含 YAML block scalar）与 Python 风格 `#` 注释（不在 string literal 内） 覆盖 callsite 至少包括： - `fields.*.call_by` - `outputs[*].aggregate.fields.*.call_by` - builtin callable：`call_by: \"^<id>(...)\"`（head 为 builtin id） completion MUST 返回分层候选并稳定排序（按优先级从高到低），并以 detail/label 标注候选来源： - 在 `fields.*.call_by`：全局可见 field_id 为主集合 - 在 `outputs[*].aggregate.fields.*.call_by`：out_field_id（`aggregate.fields` key）→ group_by field_id → 全局 field_id（低优先 fallback） definition MUST 支持多 locations： - 若 token 在 aggregate.call_by 中命中 out_field_id，则该 out_field 的定义点 MUST 为第一个候选 - 其余候选（如全局 field_id 定义）MUST 作为后续候选稳定排序+去重"
  r175,LSP server MUST delegate editor semantics to shared core library,"系统 MUST 在 LSP server 内统一复用抽离后的 editor semantics core（`scalim-yaml-dsl-lsp`），作为 diagnostics/definition/completion 的语义 SSOT，避免在 server 层复制实现细节。"
  r194,LSP server MUST support codeAction and executeCommand,"系统 MUST 支持： - `textDocument/codeAction` - `workspace/executeCommand` 并且 MUST： - 通过 `WorkspaceEdit` 应用编辑 - 在执行失败时返回可诊断信息（不得 crash）"
  r212,executeCommand MUST support dumping discovery summary as JSON,"系统 MUST 通过 `workspace/executeCommand` 暴露一个可用于排障的 discovery dump command： - command id MUST 为 `scalim.dumpDiscovery` - command arguments MUST 至少包含一个 document URI（作为 discovery 的入口） - 返回值 MUST 为可 JSON 序列化的 discovery 摘要（不得回显 YAML 正文） discovery 摘要 MUST 至少包含： - project_root - scalim_yaml_path（可为空） - python_roots - allowed_yaml_roots"
  r225,"YAML entity id references MUST support definition/completion/hover (single-file)","对同一 YAML 文件内的实体 ID 引用（例如 `fields.*.source`、`fields.*.relation`、`relations.*.steps[*].from/to`）,LSP server MUST 提供： - definition：跳转到被引用实体的声明点（缺失时返回空并给出可诊断提示） - completion：补全当前文件内已声明的可用实体 ID - hover：展示被引用实体的只读摘要信息（静态，无副作用）"
  r238,LSP server MUST provide field intelligence for aggregate output field references,"系统 MUST 在 `outputs[*].aggregate` 相关结构中，为所有 field-id 引用点提供 completion/hover/definition，并满足： - completion MUST 支持 Ctrl+Space 手动触发（包含空 scalar 与空 list item 场景） - definition MUST 能跳转到字段声明位置（跨 imports 展开仍可定位） - hover SHOULD 展示字段摘要（与现有字段卡片一致），不可解析时 MUST 返回空但不得崩溃 覆盖范围至少包括： - `outputs[*].aggregate.group_by[*]` - `outputs[*].aggregate.group_by[*][*]`（复合 key 内层 token） - `outputs[*].aggregate.fields.*.*.field` - `outputs[*].aggregate.fields.*.*.fields[*]` - `outputs[*].aggregate.fields.*.(row_number|rank|dense_rank).by` - `outputs[*].aggregate.fields.*.(row_number|rank|dense_rank).partition_by[*]` - `outputs[*].aggregate.fields.*.(row_number|rank|dense_rank).order_by[*]` - `outputs[*].aggregate.fields.*.score_by_rank.rank_field` completion MUST 返回分层候选并稳定排序（按优先级从高到低）： 1) `outputs[*].aggregate.fields` 的 out_field_id（mapping key） 2) `outputs[*].aggregate.group_by` 的 field_id 3) 全局可见 field_id（低优先 fallback；MUST 以 detail/label 明确标注来源，避免误导） definition MUST 支持多 locations，并满足稳定排序： - 若 token 命中 out_field_id，则该 out_field 的定义点 MUST 为第一个候选 - 其余候选（如全局 field_id 定义）MUST 作为后续候选稳定排序+去重"
  r248,"YAML DSL LSP server MUST keep user-facing behavior stable via contract tests",YAML DSL LSP server 的用户侧行为（diagnostics、definition/hover/completion、code actions）在重构前后 MUST 保持稳定，且该稳定性 MUST 由协议级 contract tests 覆盖。
  r256,"Go to Definition MUST support `workflow.runs[*].demand` paths statically","系统 MUST 在 workflow YAML 中为 `workflow.runs[*].demand` 字段提供 go-to-definition，并且解析 MUST 为静态解析（不执行用户代码、不 shell-out CLI）： - 当光标位于 `workflow.runs[*].demand` 的字符串值范围内时，server MUST 返回指向目标 demand YAML 文件的 `Location`（至少 1 个）。 - demand path 解析规则 MUST 与 runtime 一致： - 相对路径 MUST 以 workflow YAML 文件所在目录为基准； - `@/...` 与 `ALIAS:/...` MUST 作为 path alias 语法被识别（在 alias 可解析时）。 - 解析 MUST 受 editor discovery 的 allowed roots 约束；若 demand path 解析后越界（path escapes allowed roots），server MUST 返回空结果。 - 任意解析失败（未知 alias、不存在/不可读、解析异常等）server MUST 返回空结果且不得 crash。"
  r263,Server MUST expose code actions driven by shared core diagnostics and discovery,"系统 MUST 暴露 YAML DSL 的 Quick Fix（`textDocument/codeAction`）能力，且 actions 的触发依据 MUST 来自 shared core 输出： - diagnostics（errors/warnings + range + path） - project discovery 摘要（project_root/scalim_yaml_path/python_roots/allowed_yaml_roots） - Python resolution warnings（如可获得） actions MUST 满足： - MUST 通过 `WorkspaceEdit` 应用（可撤销、可审计） - MUST 静态无副作用（不得执行用户代码） - MUST 不在 server 层复制 DSL 语义规则"
  r270,"Actions MUST be safe and workspace-scoped","系统 MUST 只对 workspace 内文件提供可写 edits： - 若目标文件不可写或不在 workspace，action MUST 降级为 explain-only（不附带 edit）"
  r380,Command ids and arguments MUST be stable for editor integration,"系统 MUST 为 v1 Quick Fix 提供稳定的 command id 与参数协议（供 VSCode/Neovim/Zed/JetBrains 等 client 复用）。 v1 command id（不区分 client）至少包含： - `scalim.yaml.createMinimal`：`[document_uri]` - `scalim.yaml.addImportRoots`：`[document_uri, mode]`，其中 `mode` 为 `minimal|wide` - `scalim.yaml.addPythonRoots`：`[document_uri, mode]`，其中 `mode` 为 `minimal|wide` - `scalim.python.explainResolutionFailure`：`[document_uri, reference]`"
  r386,Quick Fix edits MUST write `scalim.yaml yaml_dsl.lsp` (not `yaml_dsl.editor`),系统 MUST 将 `scalim.yaml` 中用于 LSP/discovery 的配置面固定为 `yaml_dsl.lsp.*`，并且 Quick Fix 在创建/修改 `scalim.yaml` 时 MUST 仅写入该新路径（不得再写入 `yaml_dsl.editor.*`）。
  r390,missing import root/alias MUST provide a Quick Fix targeting scalim.yaml,"当用户在 demand YAML 中使用 imports path alias prefix（例如 `@/x.yaml` / `ALIAS:/x.yaml`）但 workspace 的 `scalim.yaml` 未配置对应 alias/import root 时,系统 MUST 提供可诊断信息与 Quick Fix,并满足: - server MUST 提供可诊断信息（包含失败原因与建议修复点）。 - code action MUST 提供 Quick Fix，引导用户在 `scalim.yaml` 中添加缺失的 `yaml_dsl.import_roots` 条目。 - Quick Fix MUST 为 workspace-scoped，且任何文件改写 MUST 经过用户确认（WorkspaceEdit）。"
scenarios[62]{req_id,id,given,when,then,feature}:
  r120,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r120,"diagnostics-request-is-served-without-invoking-cli","",编辑器请求某个 YAML 的 diagnostics,LSP server MUST 使用 library API 直接返回结构化 diagnostics,true
  r362,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r362,"unrelated-yaml-produces-no-diagnostics",某 YAML 不满足 schema(required) 且不包含 DSL 专属语法特征,client 请求 diagnostics,server MUST 返回空 diagnostics,true
  r362,"in-progress-dsl-yaml-still-enables-editor-semantics-via-fall",某 YAML 尚未写全 required 字段,"用户触发 go-to-definition","server SHOULD 尽最大努力返回可解析的定义位置,失败时 MUST 返回空结果且包含可诊断 warnings",true
  r483,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r483,"canonical-path-and-range-are-produced",某 demand YAML 在语义 validator 下产生一条错误 issue,编辑器请求 diagnostics,返回的 diagnostic MUST 包含 canonical dot path,true
  r564,"workflow-removed-book-alias-yields-migration-diagnostic",某 workflow YAML 在 workflow.resources.books 下仍声明 xlsx_file 或 xlsx_memory,编辑器请求 diagnostics,"LSP server MUST 返回至少一条含迁移提示的 diagnostic(例如 xlsx_file was removed / xlsx_memory was removed),且 MUST NOT crash",true
  r627,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r627,"definition-resolution-locates-a-python-function","YAML 中某字段引用 `pkg.mod:func`","用户触发 go-to-definition",系统 MUST 返回 `func` 定义所在文件与范围,true
  r627,"relative-module-definition-resolution-locates-a-python-funct",YAML 文件位于某个 `python_roots` 之下,"用户触发 go-to-definition",系统 MUST 返回 `load_orders` 定义所在文件与范围,true
  r627,"go-to-definition-returns-method-location-plus-object-fallbac","YAML 中某字段引用 `pkg.mod:some_ref.a_method`","用户在引用字符串内触发 go-to-definition",系统 MUST 返回至少 2 个 locations,true
  r627,"go-to-definition-works-for-call-by-head-reference-in-a-block","YAML 包含： `call_by: |` ` pkg.mod:fn(` ` x=1,` ` )`","用户在 `pkg.mod:fn` 上触发 go-to-definition",系统 MUST 返回 `fn` 定义所在文件与范围,true
  r674,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r674,"go-to-definition-jumps-from-import-to-fragment-mapping-key","demand YAML 顶层声明 `imports: {fragments: ./ecommerce_report_fragments.yaml}`","用户在 `$import` 引用字符串内触发 go-to-definition","系统 MUST 跳转到 `ecommerce_report_fragments.yaml` 中 `report_book:` 对应的 key 位置",true
  r674,"unknown-import-alias-yields-empty-result",demand YAML 未声明 `imports.fragments`,"`$import: fragments.report_book`","go-to-definition MUST 返回空结果",true
  r674,"fragment-path-escapes-allowed-roots-yields-empty-result",`imports.fragments` 指向的 fragment 文件解析后越界（不在 allowed roots 内）,"用户触发 go-to-definition","go-to-definition MUST 返回空结果",true
  r714,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r714,"hover-returns-docstring","YAML 中某字段引用 `pkg.mod:func`",用户触发 hover,"若可解析,系统 MUST 返回 `func` 的 docstring",true
  r714,"hover-returns-docstring-for-relative-module-reference",YAML 文件位于某个 `python_roots` 之下,用户触发 hover,"若可解析,系统 MUST 返回 `load_orders` 的 docstring",true
  r714,"hover-works-for-a-block-scalar-reference","YAML 包含： `loader: |` ` pkg.mod:func`","用户在 `pkg.mod:func` 上触发 hover","若可解析,系统 MUST 返回 `func` 的 docstring",true
  r747,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r747,"hover-returns-resolved-fragment-source-path","demand YAML 顶层声明 `imports.fragments: ./ecommerce_report_fragments.yaml`",用户对 `$import` 引用触发 hover,系统 MUST 返回包含 fragment 真实文件路径的 hover 文本,true
  r775,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r775,"completion-suggests-symbols-in-module","YAML 中某字段引用 `pkg.mod:`",用户在引用字符串内触发 completion,系统 SHOULD 返回 `pkg.mod` 下的可用符号候选,true
  r775,"completion-suggests-symbols-in-module-for-relative-module-re",YAML 文件位于某个 `python_roots` 之下,用户在引用字符串内触发 completion,系统 SHOULD 返回 `.loaders` 规范化后的目标模块下的可用符号候选,true
  r775,"completion-suggests-symbols-for-a-block-scalar-reference","YAML 包含： `loader: |` ` pkg.mod:`","用户在 `pkg.mod:` 后触发 completion",系统 SHOULD 返回 `pkg.mod` 下的可用符号候选,true
  r154,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r154,"go-to-definition-resolves-a-kwargs-value-field-id","YAML 声明 `fields.order_amount: ...`","用户对 `order_amount=order_amount` 的右侧 `order_amount` 触发 go-to-definition",系统 MUST 跳转到 `fields.order_amount` 的声明位置,true
  r154,"completion-works-for-empty-kwargs-value","存在 `call_by: \"pkg.mod:fn(order_amount=)\"`",用户在 `=` 右侧触发 completion（Ctrl+Space）,"系统 MUST 返回非空 field-id 候选列表",true
  r154,"aggregate-call-by-completion-prefers-out-field-id-candidates","存在 `outputs[0].aggregate.fields.rank: {dense_rank: {by: sum_amount}}`",用户在 `rank=` 的右侧触发 completion（Ctrl+Space）,completion MUST 将 `rank`（out_field_id）作为高优先候选返回,true
  r154,"completion-works-for-empty-kwargs-value-in-a-block-scalar-ca","YAML 包含： `call_by: |` ` pkg.mod:fn(` ` order_amount=, # comment` ` )`",用户在 `=` 右侧触发 completion（Ctrl+Space）,"系统 MUST 返回非空 field-id 候选列表",true
  r175,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r175,"server-uses-shared-core-for-diagnostics","",LSP server 收到 diagnostics 请求,server MUST 调用 shared core 的 diagnostics API 产生结果,true
  r194,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r194,"codeaction-returns-an-executable-fix",当前文档存在一条可修复的 discovery/diagnostics 问题,client 请求 codeAction,server MUST 返回可执行的 fix（edit 或 executeCommand）,true
  r212,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r212,"dumpdiscovery-returns-a-json-serializable-discovery-payload",client 提供一个已打开文档的 URI,client 调用 `workspace/executeCommand` 执行 `scalim.dumpDiscovery`,server MUST 返回包含 `project_root/scalim_yaml_path/python_roots/allowed_yaml_roots` 的 JSON payload,true
  r225,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r225,"fields-source-can-go-to-definition","YAML 声明 `sources.customers: ...`",用户在 `customers` 上触发 `textDocument/definition`,server MUST 跳转到 `sources.customers` 的 key 位置,true
  r225,"unknown-entity-id-does-not-crash-and-provides-a-hint-diagnos","某字段引用 `fields.x.source: not_exist`",用户触发 definition/hover/completion,server MUST 返回空结果（无 locations / 无 hover）,true
  r238,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r238,"completion-works-for-empty-aggregate-group-by-list-item","某 demand YAML 存在 `outputs[*].aggregate.group_by` 且光标位于空 list item（例如 `- <cursor>`）",用户在该位置触发 completion（Ctrl+Space）,"系统 MUST 返回非空 field-id 候选列表",true
  r238,"definition-resolves-a-field-id-referenced-by-an-aggregate-me","某 demand YAML 中存在 `aggregate.fields.*.*.field: some_field_id`","用户对 `some_field_id` 触发 go-to-definition",系统 MUST 跳转到 `fields.some_field_id` 的声明位置（或 imports 展开后的真实声明位置）,true
  r238,"rank-by-resolves-aggregate-out-field-id-first-then-global-fi","某 demand YAML 中存在 `outputs[0].aggregate.fields.sum_amount: {sum: {field: order_amount}}`","用户对 `by: sum_amount` 的 `sum_amount` 触发 go-to-definition","系统 MUST 首选跳转到 `outputs[0].aggregate.fields.sum_amount` 的 key 位置",true
  r248,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r248,"definition-hover-completion-baseline-is-preserved",一个包含 imports 与内联 Python reference 的 YAML workspace,进行内部重构（不改变对外行为）,运行 LSP contract tests MUST 仍然通过（同一组 fixtures 与断言）,true
  r256,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r256,"relative-demand-path-jumps-to-the-referenced-demand-yaml-fil",一个 workflow YAML 文件位于 `/repo/workflows/wf.yaml`,"用户在该字符串值内触发 go-to-definition",server MUST 返回一个指向 `/repo/workflows/demands/d10_paid_orders.demand.yaml` 的 location,true
  r256,"alias-demand-path-jumps-when-alias-is-configured",project discovery 可解析到 alias `@`（例如来自 `scalim.yaml` 的 import roots alias）,"用户在该字符串值内触发 go-to-definition",server MUST 返回一个指向 alias 解析后目标文件的 location,true
  r256,"invalid-or-out-of-roots-demand-path-yields-empty-definition-",一个 workflow YAML 的某个 run 的 `demand` 路径无法解析或解析后越界,"用户触发 go-to-definition",server MUST 返回空结果（不返回任何 location）,true
  r263,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r263,"missing-scalim-yaml-yields-a-create-action",shared core discovery 表明 `scalim_yaml_path` 为空,client 请求 codeAction,"server MUST 返回至少一个 \"Create minimal scalim.yaml\" action",true
  r270,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r270,"non-workspace-target-yields-no-edit",某 action 需要写入 workspace 外文件,server 构造该 action,server MUST 不返回 `WorkspaceEdit`,true
  r380,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r380,"user-can-invoke-a-documented-quick-fix-command-id-from-any-c",某 LSP client 支持 `workspace/executeCommand`,client 使用文档中列出的 command id 与参数协议调用 Quick Fix,"server MUST 接受该请求并返回可诊断的结果（成功时应用 edit；失败时返回 explain-only payload）",true
  r386,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r386,"addpythonroots-writes-to-yaml-dsl-lsp-python-roots","",用户触发 Quick Fix `scalim.yaml.addPythonRoots`,server 应用的 edits MUST 写入 `scalim.yaml yaml_dsl.lsp.python_roots`,true
  r386,"createminimal-uses-yaml-dsl-lsp-keys","",用户触发 Quick Fix `scalim.yaml.createMinimal`,生成的 `scalim.yaml` MUST 使用 `yaml_dsl.lsp.*` 键名（不得包含 `yaml_dsl.editor.*`）,true
  r390,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r390,"quick-fix-suggests-adding-import-root-for","demand YAML 配置 `imports: {common: \"@/fragments/common.yaml\"}`",LSP 生成 diagnostics 并请求 code actions,"MUST 提供一个 Quick Fix（例如 \"Add import root alias '@' to scalim.yaml\"）",true