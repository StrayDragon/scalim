kind: llman.sdd.spec
name: "ir-key-normalization"
purpose: "`key_normalization` 提供一个运行期可控的”稳定字符串口径”键匹配策略,用于解决 relations/derived outputs 中跨来源类型不一致导致的 miss/分组拆分问题(例如 `1` 与 `”1”`). 该能力为 `EXPERIMENTAL`,默认关闭(`raw`). [scope-review-2026-07-13-c25-xlsx-ir-path-presence]"
valid_scope[1]: src/scalim/
requirements[10]{req_id,title,statement}:
  r58,"`key_normalization` MUST be a validated opt-in runtime option (default `raw`)","系统 MUST 提供一个运行期开关 `key_normalization`,取值域为: - `raw`: 保持现有 key 口径(默认) - `auto_str`: 使用稳定字符串口径做 key 匹配(仅在未显式配置 cast 时作为缺省策略生效) - `force_str`: 强制使用稳定字符串口径做 key 匹配(即使显式配置 cast 也会在最终匹配边界做字符串规范化) 系统 MUST 在对外入口对该字段进行校验;未知取值 MUST fail-fast(编译期/启动期均可). 该开关 MUST 可在所有公开入口启用并最终落到执行核心的运行期上下文."
  r302,`key_normalization` EXPERIMENTAL warning MUST be visible by default,"当用户启用 `key_normalization` 的非 `raw` 模式时,系统 MUST 在一次运行内至少发出一次包含 `EXPERIMENTAL` 的提示,且该提示在默认配置下 MUST 可见(不要求用户额外挂载 observer/hook,也不要求显式开启 fallback logger)。 该提示仍需满足原有约束: - MUST 包含当前启用的 `key_normalization` 值 - MUST NOT 包含任何明细 key 值 - SHOULD 在一次运行内去重(避免刷屏)"
  r425,"explicit cast precedence and normalization mode MUST be well-defined","当同时存在显式 cast 与 `key_normalization` 时,系统 MUST 按优先级确定最终 match key: **候选 key 计算优先级**: 1. Step 级 cast 配置 2. Source 级 cast 配置 3. Raw key(缺省) **规范化模式行为**: - `raw`: 直接使用 candidate key - `auto_str`: 仅当 candidate key 来自 raw key(未配置显式 cast)时,对 candidate key 应用字符串规范化;否则保持不变 - `force_str`: 无论 candidate key 来源,都 MUST 应用字符串规范化 并且: - Step 级 cast MUST 优先于 source 级 cast - `auto_str` MUST 仅作为 fallback 策略生效(存在显式 cast 时不得额外做字符串规范化) - `force_str` MUST 对 candidate key 强制执行字符串规范化(显式 cast 先执行)"
  r519,`key_normalization` string modes MUST normalize match keys,"系统 MUST 提供基于字符串的 key 规范化策略 `key_normalization=auto_str` 与 `key_normalization=force_str`. 当启用任一字符串策略时,框架内部所有”需要匹配”的 key 在进入匹配边界时 MUST 逐字段应用字符串规范化后再参与匹配. 并且字符串规范化的启用时机 MUST 遵循 cast precedence 规则(即 `auto_str` 为 fallback,`force_str` 为强制)."
  r595,relations mapping collisions after normalization MUST be handled safely by defau,"当 relations 在构建”规范化后的 mapping 视图”时,若多个不同 raw key 规范化到同一个稳定字符串 key(即发生 collision),系统 MUST 按以下规则安全处理: - 若 collision 对应的 value 全部深度相等,系统 MUST 保留任一值并继续(并发出一次 redacted 告警) - 若 collision 对应的 value 存在差异,系统 MUST fail-fast(避免 silent 选择导致隐性错误) 并且: - 告警/错误文案 MUST NOT 包含任何明细 key 值 - 告警/错误文案 SHOULD 包含 source 标识、`key_normalization` 模式、collision 计数等上下文信息"
  r649,relations MUST use the normalized key space consistently (including cached sourc,"当 relations 实际采用字符串规范化口径进行匹配时,relations 查找 MUST 使用规范化后的 key 口径,且该口径 MUST 在下列边界保持一致: - Loader 调用入参(key 绑定) - Loader 返回 mapping 的命中 - 针对缓存源,系统 MUST 在”匹配点”使用与 lookup key 相同的 key 口径进行命中 注意: - 当 `key_normalization=auto_str` 且存在显式 cast 时,系统 MUST 使用显式 cast 的 key 口径,不得额外做字符串规范化 - 当 `key_normalization=force_str` 时,系统 MUST 在最终匹配边界对 candidate key 强制做字符串规范化(显式 cast 先执行)"
  r693,"loader mapping key-space mismatches MUST be diagnosable and redacted","当 `key_normalization` 与显式 cast 组合导致”预期 key 口径”与 loader mapping 的实际 key 口径不一致时,系统 MUST 提供可诊断的告警/错误,并满足: - MUST NOT 泄露明细 key 值 - SHOULD 提供可操作的修复建议(例如调整 cast、改用 `force_str`、统一 loader key 口径)"
  r731,derived outputs MUST normalize `group_by`/`dedup_by` keys and output key fields,"当 `key_normalization=auto_str` 或 `key_normalization=force_str` 时,系统 MUST 在 derived outputs 的 `group_by`/`dedup_by` 中使用规范化后的 key 做合并,并在输出行中回填规范化后的 key 字段值(保证内部口径与输出表现一致). - derived outputs 的 key MUST 逐字段应用字符串规范化后再参与分组/去重 - 输出行中对应字段值 MUST 使用规范化后的值 - 若 raw 非 `None` 但规范化失败,系统 MUST fail-fast,且错误信息 MUST 不包含 raw 明细值"
  r763,"key_normalization MUST propagate into adaptive per-task runtimes","当系统为 `parallel_mode=adaptive` 创建 per-task 子运行时时,子运行时 MUST 继承本次运行的 `key_normalization` 值,并使用相同的规范化规则参与匹配与诊断."
  r144,"ordered-unique helper MUST be centralized","系统 MUST 提供一个公共的\"去重保序\"工具函数作为 SSOT,并要求相关模块复用该函数. 该工具函数 MUST: - 对输入序列按出现顺序去重 - 输出结果 MUST 可预测且稳定"
scenarios[24]{req_id,id,given,when,then,feature}:
  r58,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r58,"unknown-key-normalization-is-rejected","用户传入 `key_normalization=\"whatever\"`",系统开始编译或运行,"系统 MUST fail-fast 并提示仅支持 `raw|auto_str|force_str`",true
  r302,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r302,"enabling-key-normalization-emits-a-visible-experimental-warn","调用方启用 `key_normalization=\"auto_str\"`(或 `\"force_str\"`)",系统开始运行,"系统 MUST 发出一次包含 `EXPERIMENTAL` 的提示,且该提示在默认配置下可见",true
  r425,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r425,"step-lookup-cast-overrides-key-normalization","raw lookup key 为 `\"1\"`(str)",用户启用 `key_normalization=auto_str`,"系统 MUST 使用显式 `lookup_cast` 的结果作为 match key(例如 `1`),而不是 `\"1\"`",true
  r425,"force-str-normalizes-even-when-lookup-cast-is-set","raw lookup key 为 `\"1\"`(str)",用户启用 `key_normalization=force_str`,"系统 MUST 在最终匹配边界将 candidate key 规范化为稳定字符串(例如 `\"1\"`)",true
  r519,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r519,"auto-str-normalizes-raw-int-key-into-a-stable-string",raw match key 为 `1`(int),用户启用 `key_normalization=auto_str`,"系统 MUST 将该 key 规范化为稳定字符串 `”1”` 后参与匹配 #### Semantics: `None` vs normalize failure raw 为 `None` 表示”空值”: 在 relations 中 MUST 计入 null key;在 derived `group_by`/`dedup_by` 中 MUST 允许作为 key 的组成部分 raw 非 `None` 但规范化返回 `None` 表示”无法规范化”: 在 relations 中 MUST 计入类型错误;在 derived `group_by`/`dedup_by` 中 MUST fail-fast 当需要输出错误/诊断 message 时,系统 MUST NOT 在 message 中包含 raw key 的明细值;message SHOULD 仅包含类型/字段/来源/原因等上下文信息. #### Semantics: float keys 当 raw key 为 `float` 时: `NaN/inf` MUST 视为规范化失败 其余 float MUST 按 `auto_str_normalize` 规则规范化为稳定字符串(整数 float 去除小数部分;非整数 float 使用稳定格式化) 注意: 该行为与 relations 的 `lookup_cast: {auto: {}}`(会拒绝 float) 不同;启用 `key_normalization=auto_str` 或 `key_normalization=force_str` 表示用户接受按字符串语义做 key 匹配的风险. #### Semantics: multi-field keys 当 key 为多字段复合键时(例如 `Tuple[object, ...]`): 系统 MUST 对每个组成字段逐字段应用 `auto_str_normalize` 系统 MUST 将规范化后的结果组成 `Tuple[str, ...]` 作为 match key 任一组成字段 raw 为 `None` → relations MUST 视为 `null_key` 任一组成字段 raw 非 `None` 且规范化失败 → relations MUST 视为 `type_error`(derived MUST fail-fast) #### Semantics: key collisions after normalization 当多个不同 raw key 规范化到同一个稳定字符串 key 时: derived outputs 的 `group_by`/`dedup_by` 语义为“合并”: 这是该能力的预期行为(按规范化 key 合并) relations 在构建“需要匹配的 mapping 的规范化视图”时的 collision 处理,见下方 requirement(`relations mapping collisions after normalization...`),并保持 redacted",true
  r595,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r595,"collision-with-identical-values-continues-with-a-redacted-wa",启用 `key_normalization=”force_str”`(或满足进入字符串规范化 key space 的条件),系统构建规范化 mapping 视图并执行 relations lookup,系统 MUST 继续执行并命中该 mapping,true
  r595,"collision-with-different-values-fails-fast",启用 `key_normalization=”force_str”`(或满足进入字符串规范化 key space 的条件),系统构建规范化 mapping 视图,"系统 MUST fail-fast",true
  r649,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r649,"cached-mapping-with-int-keys-still-hits-when-enabled","某个目标 source 为缓存源,其 loader 返回 mapping 的 key 为 `1`(int)",用户启用 `key_normalization=auto_str` 或 `key_normalization=force_str`,系统 MUST 将两侧 key 统一到相同的规范化口径并命中该 mapping,true
  r693,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r693,"auto-str-with-explicit-cast-hits-only-after-normalization-em",`key_normalization=”auto_str”`,"系统发现 cast 后候选 key 命中失败,但对候选 key 做字符串规范化后可以命中",系统 SHOULD 发出 redacted 告警提示存在 key 口径错配,true
  r731,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r731,"derived-outputs-group-by-merges-semantically-equal-keys-when","derived outputs 的 `group_by` 字段在不同输入行中分别为 `1` 与 `\"1\"`",用户启用 `key_normalization=auto_str` 或 `key_normalization=force_str`,"系统 MUST 将两者规范化为相同的分组 key,从而落入同一分组",true
  r731,"derived-outputs-dedup-by-merges-semantically-equal-keys-when","derived outputs 的 `dedup_by` 字段在不同输入行中分别为 `1` 与 `\"1\"`",用户启用 `key_normalization=auto_str` 或 `key_normalization=force_str`,"系统 MUST 将两者规范化为相同的去重 key,从而视为同一条记录",true
  r731,"relations-lookup-key-1-and-1-are-treated-as-the-same-key-whe",relations 需要查找的 raw key 值为 `1`,用户启用 `key_normalization=auto_str` 或 `key_normalization=force_str`,"系统 MUST 将 `1` 规范化为稳定字符串并命中 `\"1\"` 的映射",true
  r763,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r763,"adaptive-per-task-runtime-uses-the-same-normalization-mode-a","本次运行 `key_normalization=\"auto_str\"`(或 `\"force_str\"`)","系统在 adaptive 下创建 per-task 子运行时并执行任务",子运行时 MUST 使用与父运行时相同的 `key_normalization` 值,true
  r144,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r144,"duplicates-are-removed-while-preserving-order","","输入为 `[\"a\", \"a\", \"b\"]`","输出 MUST 为 `[\"a\", \"b\"]`(或等价的 tuple 形态)",true