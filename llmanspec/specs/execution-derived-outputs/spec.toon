kind: llman.sdd.spec
name: "execution-derived-outputs"
purpose: "支持在同一次运行中基于详情行流生成派生输出(增量聚合 + finalize 阶段输出),并定义 IR/Python-only 配置入口、资源护栏与 `adaptive` 并发边界. [scope-review-2026-07-13-c25-xlsx-ir-path-presence]"
valid_scope[1]: src/scalim/
requirements[16]{req_id,title,statement}:
  r36,派生输出定义(同步聚合),"系统 SHALL 支持在同一次运行中定义派生输出,派生输出可基于详情数据进行聚合/计算,并在运行结束时输出结果."
  r280,增量聚合与批次累计,"系统 SHALL 支持派生输出以增量方式累计(按批次或流式),以避免加载全部详情数据."
  r404,派生输出的数据来源选择,"系统 SHALL 允许派生输出明确选择其数据来源(例如直接消费运行期事件/批次结果,或基于已落盘的详情输出),以平衡内存与 I/O 成本."
  r499,非增量指标的处理方式,"系统 SHALL 支持对不可增量的统计指标提供可行路径,包括可合并近似算法或二阶段后置聚合."
  r577,聚合状态资源控制,"系统 SHALL 不在进程内为派生输出聚合状态提供基数护栏(如 max_groups/max_distinct);聚合状态规模不受限,内存过载风险交由宿主系统层(如 OOM killer)兜底."
  r637,IR/Python 配置入口,"系统 SHALL 仅提供 IR/Python 方式配置派生输出,并保持 YAML DSL 不变."
  r683,`adaptive` 一致性边界,"系统 SHALL 明确 `adaptive` 并发下派生聚合的一致性边界,并在不满足条件时 fail-fast."
  r755,内置 set 口径聚合原语,"系统 SHALL 在派生输出的内置聚合能力中新增 streaming-friendly 的 set 口径原语,用于减少业务侧 Python state 并提升复用性. 系统 MUST 至少支持: - `count_distinct` (支持复合 key) - `dedup_by` - `two_stage_group_by`"
  r137,`count_distinct` 复合 key 与缺失值语义,"系统 MUST 支持 `count_distinct` 同时接受单字段与复合 key. 系统 MUST 明确缺失值语义: - 当 distinct key 的任一组成字段为 `None` 时,该行 MUST 被忽略 - 空字符串 MUST 作为普通值参与 distinct."
  r160,`dedup_by` 冲突策略,"系统 MUST 对 `dedup_by` 在同一 key 命中多行时的冲突策略提供显式配置,并保证在相同输入下结果确定性."
  r181,`adaptive` 下的确定性边界,"系统 MUST 明确 `parallel_mode=\"adaptive\"` 下的确定性边界: 任一阶段包含顺序依赖语义时,系统 MUST fail-fast."
  r199,两阶段聚合确定性,"系统 MUST 为 `two_stage_group_by` 定义确定性的 tie-break 与输出顺序规则,以避免对拍误报."
  r230,最小条件计数原语,"系统 MUST 提供至少一种\"可对拍且可指纹化\"的条件计数能力,用于覆盖常见口径. 系统 MUST 至少支持: - `count_true_gte(field_id, threshold)`"
  r242,meta/audit 的稳定指纹与结构化审计,"系统 MUST 为每个 derived target 生成稳定聚合指纹,并写入 meta sheet. 系统 MUST 满足: - meta 中 MUST 写入聚合指纹 - 当聚合发生错误时,系统 MUST 写入结构化 audit 行(仅脱敏信息) - 指纹 MUST 使用 `sha256` 计算,不得用于签名/认证等安全目的"
  r251,aggregate producer key enums MUST be centralized,"系统 MUST 为 aggregate 输出相关的 producer keys 提供一个 SSOT 模块,并要求 YAML 解析、运行时装配、工具自省、JSON Schema、前端 editor 共享同一份枚举集合. 该 SSOT MUST 包含并可被引用: - metric producer keys - rank producer keys - post producer keys"
  r800,派生输出不再提供基数护栏配置,"系统 MUST NOT 为派生输出提供基数护栏配置(max_groups / max_distinct / distinct_on_overflow / dedup_by.on_overflow);相关的 IR 字段、运行期构造参数、YAML 字段与警告 MUST 被移除. 高基数聚合的内存风险由宿主系统层兜底,框架不再在进程内做基数限界."
scenarios[38]{req_id,id,given,when,then,feature}:
  r36,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r36,"订单明细-供应商利润率","",详情输出包含订单ID/订单金额/订单利润/供应商ID,系统应生成包含供应商ID与利润率的汇总输出,true
  r280,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r280,分批累计,"",详情数据按批次处理,派生聚合应跨批次累计并在结束时输出完整结果,true
  r280,"可合并统计-均值-比率","",派生输出需要计算均值或比率,系统应仅累计必要的分子/分母状态并在结束时输出结果,true
  r280,"top-k-近似聚合","","派生输出需要 Top-K 排名且数据量巨大",系统应允许使用可合并的近似算法并以有限内存完成累计,true
  r404,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r404,列式输出的后置聚合,"",详情输出采用列式或落盘模式,派生输出可选择在详情落盘后进行聚合,true
  r404,事件流增量聚合,"",派生输出选择消费运行期事件或批次结果,系统应允许在详情写入后立即更新聚合状态而不保留明细,true
  r499,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r499,需要全量数据的指标,"",指标无法在单批次内增量计算,系统应允许使用近似算法或选择后置聚合模式完成输出,true
  r499,精确分位数后置聚合,"",需要精确分位数且不允许近似,系统应允许将该指标标记为后置聚合并在详情落盘后计算,true
  r577,无护栏默认运行,派生输出未配置任何基数护栏,运行执行派生聚合,"系统 MUST 不发出基数相关 warn,且 MUST 不对聚合键数量做进程内限界检查",true
  r577,高基数场景不受限,派生聚合键数量巨大,运行执行派生聚合,系统 MUST 不抛出基数超限错误;内存压力由宿主系统层处理,true
  r637,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r637,仅使用编程方式配置,"",用户通过编程方式定义派生输出,系统应无需修改 YAML DSL 即可运行,true
  r683,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r683,"内置可交换聚合在-adaptive-下可用","",派生输出仅使用内置可交换/可结合的增量指标,系统应允许派生聚合并确保结果确定性,true
  r683,"自定义聚合器在-adaptive-下默认拒绝","",派生输出使用自定义聚合器且未声明支持 `adaptive`,"系统应 fail-fast 并提示切换到 `parallel_mode=\"seq\"`",true
  r755,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r755,"count-distinct-统计-distinct-用户数",详情流包含 `cs_id` 与 `user_id`,派生输出按 `cs_id` 分组并对 `user_id` 执行 `count_distinct`,系统 MUST 输出每个 `cs_id` 的 distinct 用户数且结果确定性,true
  r137,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r137,"复合-key-的缺失值忽略",distinct key 由多个字段组成,某行的任一字段为 `None`,系统 MUST 忽略该行且不计入 distinct 计数,true
  r160,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r160,"dedup-by-on-conflict-first",两行数据具有相同的 dedup key,"配置 `dedup_by(..., on_conflict=first)`","系统 MUST 选择稳定的\"第一条\"并用于后续指标计算",true
  r181,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r181,"adaptive-下拒绝顺序依赖去重策略","","`parallel_mode=\"adaptive\"` 且使用顺序依赖的去重策略","系统 MUST fail-fast 并提示切换到 `parallel_mode=\"seq\"`",true
  r199,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r199,两阶段聚合产生相同输出,"",执行两阶段聚合,系统 MUST 在相同输入下产生相同输出(含顺序),true
  r230,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r230,"count-true-gte-阈值计数",某行满足阈值条件,指标配置为 `count_true_gte`,系统 MUST 对该行计数 +1,true
  r242,聚合错误仍写审计,派生聚合过程中发生非基数类错误,运行结束,"系统 MUST 在 audit 写入结构化错误行,且 meta 中 MUST 写入聚合指纹",true
  r251,baseline,"","TODO: describe the trigger","TODO: describe the expected result",true
  r251,"parser-runtime-introspection-共享枚举","",系统提供 SSOT 模块,"各层 MUST 共享同一份枚举集合,不得维护本地副本",true
  r251,默认配置一致性,YAML primary output 包含 compute 字段且未显式指定输出字段,调用 introspection 加载输出配置,返回的默认输出字段 MUST 与 runtime 默认一致,true
  r251,"json-schema-editor-bundles-同步","",系统生成 canonical JSON schema,schema 中 producer keys 的可选集合 MUST 与 SSOT 一致,true
  r800,残留护栏字段被拒绝,YAML 配置中残留 max_groups / max_distinct / distinct_on_overflow / dedup_by.on_overflow 任一字段,解析该配置,"系统 MUST fail-fast 报错并给出迁移提示(建议移除该字段)",true
  r800,"IR-构造不再接受护栏参数",代码构造 DerivedGroupBySpec 或 DedupBySpec 时传入 max_groups/max_distinct/distinct_on_overflow/on_overflow,调用构造器,系统 MUST 报 TypeError(参数不存在),true