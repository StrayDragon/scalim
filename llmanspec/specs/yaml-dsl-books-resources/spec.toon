kind: llman.sdd.spec
name: "yaml-dsl-books-resources"
purpose: "定义 demand/workflow 统一的 `resources.books` Excel IO 资源入口,并约束 `outputs[*].to` / `outputs[*].write` 的 book 绑定与导出语义."
valid_scope[1]: src/scalim/
requirements[11]{req_id,title,statement}:
  r1,demand/workflow YAML MUST support resources.books as unified Excel IO identity declaration,"系统 MUST 在 demand 与 workflow YAML 中提供一致的 resources.books 入口作为 Excel book 的 identity/variant/path 声明面: - demand: resources.books.<book_id> - workflow: workflow.resources.books.<book_id> - <book_id> MUST 非空且在 mapping 内唯一 - 每个 book MUST 选择且仅选择一个实现分支 xlsx_file 或 xlsx_memory - YAML books MUST NOT 再把 write_defaults 或 xlsx_memory.budget 作为稳定 authoring 字段（出现 MUST fail-fast 并迁移到 Python policy） - legacy kind discriminator 仍 MUST fail-fast - demand 侧 imports/ 规则保持既有 scope；workflow MUST NOT 支持 imports/"
  r2,`books.kind=xlsx_file` MUST define file export semantics and path resolution bas,"系统 MUST 定义 `resources.books.<id>.xlsx_file` 的文件导出语义与路径解析基准。 当 `resources.books.<id>.xlsx_file` 分支被选择时,该 book 表示一个版本化输出 root 下的 `.xlsx` 导出,并满足: - `resources.books.<id>.xlsx_file.path` MUST 为非空字符串或 `{$init_var: <name>}` 指令节点 - `resources.books.<id>.xlsx_file.path` 语义 MUST 为 **输出 root 目录**（版本化输出 D-2），而不是最终 `.xlsx` 文件路径 - 相对路径 MUST 以**声明该 book 的 YAML 文件所在目录**为基准解析(不是进程 CWD) - 系统 MUST 在实际写入前创建父目录(`mkdir(parents=True, exist_ok=True)`) - 系统 MUST 基于 `book_id` 与 `version_id` 推导最终输出路径： - final path MUST 等价于 `<root>/versions/<version_id>/books/<book_id>.xlsx` 可选字段: - `resources.books.<id>.xlsx_file.allow_formulas` MUST 为 bool(默认 `true`) legacy `write_lock` 配置面 MUST 被移除；若用户仍提供 `resources.books.<id>.write_lock`，系统 MUST fail-fast 并给出迁移提示。"
  r3,xlsx_memory MUST define typed internal semantics and optional export; budget is Python policy,"系统 MUST 定义 resources.books.<id>.xlsx_memory 的 typed internal semantics 与可选 export_xlsx: - export_xlsx.path 等导出声明可留在 YAML - 内存预算 MUST 由 Python BookBudgetPolicy（或等价）表达，YAML MUST NOT 再 authoring xlsx_memory.budget - 当未提供 Python budget policy 时系统 MUST 视为 unlimited - internal rows MUST 保持 FieldValue SSOT（含 Decimal 不降级为 float） - allow_formulas 仍可按既有 YAML/variant 规则存在（本 change 不翻转默认值）"
  r4,"demand MUST bind outputs to books via `outputs[*].to.book` and `outputs[*].to.sh","系统 MUST 支持在 demand YAML 中将 Excel outputs 绑定到 book 资源,且绑定入口仅允许位于 output 局部: - 对于绑定到 book 的 output,`outputs[*].to` MUST 存在且 MUST 为 mapping: - `outputs[*].to.book` MUST 为非空字符串 - `outputs[*].to.sheet` MAY 为非空字符串 默认/继承规则: - 若 `outputs[*].to.sheet` 缺省,则 `sheet` MUST 默认等于 `outputs[*].name` - `sheet` MUST 通过 Excel sheet 名校验(非空、长度 `<=31`、且不得包含 `\\\\ / ? * [ ] :`) - 若某 output 的 effective `to.book` 缺失,系统 MUST fail-fast 并给出可复制迁移提示(例如提示设置 `outputs[*].to.book`)"
  r5,"standalone demand MUST fail-fast when a referenced book resource is missing","系统 MUST 在 standalone `compile/run` 执行 demand 时,对所有 outputs 的 effective `to.book`(来自 `outputs[*].to.book`)执行资源存在性校验,并在缺失时 fail-fast: - 系统 MUST 确保该 `book_id` 在 effective `resources.books` 中存在 - 若缺失,系统 MUST fail-fast(不得静默降级为“无输出”或“写到临时路径”) - 错误信息 MUST 同时包含: - 缺失的 `book_id` - 发生位置(例如 `outputs[0].to.book`) - 可复制迁移提示(例如: “在 demand 中声明 `resources.books.<id>` 或在 Python overrides 的 `overrides.resources.books` 提供该资源”)"
  r6,workflow MUST merge books from demand/workflow with deterministic precedence and,"系统 MUST 定义 `books` 的合并/覆盖优先级(从低到高): 1) demand YAML 的 `resources.books` 2) workflow YAML 的 `workflow.resources.books`(同名 `<book_id>` 覆盖) 3) Python overrides(同名 `<book_id>` 最终覆盖) 约束: - 覆盖同名 `<book_id>` 时,系统 MUST 校验 effective book 仍满足其所选 variant 的必填字段约束 - 系统 MUST 在 variant 不兼容时 fail-fast(例如未知分支 key,或 YAML 结构与分支约束不匹配)"
  r7,workflow book patches MUST be applied with strict contracts and consistent diagn,"当 workflow compile 对 `workflow.resources.books.<book_id>`（以及其嵌套字段如 `write_defaults` / `budget` / `export_xlsx`）应用 patch/overlay 时，系统 MUST 提供严格且可预测的契约校验： - 对任意 patch mapping，系统 MUST 检测 unknown keys 并 fail-fast - 对任意字段类型不匹配（例如期望 bool 但得到 list），系统 MUST fail-fast 且诊断信息 MUST 指向准确逻辑 path - 对 `write_defaults` 等枚举字段，系统 MUST 以一致口径校验并提供可行动错误提示 - 上述校验 SHOULD 由集中实现的 helper 承载，避免同类规则在不同入口漂移"
  r8,"book write behavior MUST come from Python policy; outputs.write stays header-local","系统 MUST 以 Python BookWritePolicy 配置 book 级写入行为，并允许 outputs 仅覆盖展示/表头局部字段: - mode/align_by/header_policy/on_mismatch/on_conflict MUST 来自 Python policy（缺省用 builtin defaults） - YAML resources.books.<id>.write_defaults MUST NOT 再作为 authoring SSOT - outputs[*].write MAY 提供 include_header/header_fields_output_by 等 output-local header 行为 - outputs[*].write MUST NOT 承载 workbook 级 mode/align_by/header_policy/on_mismatch/on_conflict - xlsx_memory 在 append+align_by=header 时仍 MUST fail-fast"
  r9,"Excel exports MUST escape formula-like strings by default (opt-out via allow_for","当系统导出 `.xlsx`(包括 `xlsx_file` book 与 `xlsx_memory.export_xlsx`)时,系统 MUST 默认保留所有字符串 cell 值原样写出,不得执行公式前缀转义。 防护模式（不可信输入显式收紧）： - 若 effective book 配置 `allow_formulas=false`,系统 MUST 对所有字符串 cell 值执行公式前缀转义,以避免 `Excel` 将其解析为公式。 转义规则 MUST 满足： - 仅对 `str` 生效（其它类型保持原样）。 - 若原始字符串以 `'` 开头,MUST 保持不变（避免重复转义）。 - 对 `value.lstrip()` 的首字符,若属于 `{ '=', '+', '-', '@' }`,MUST 在**原始值**前追加 `'`。 - 其它字符串 MUST 保持不变。 - 该规则 MUST 同时作用于表头行与数据行。"
  r10,"downstream demands MUST be able to load xlsx book sheet rows via a built-in loader","系统 MUST 提供一个 workflow scope 的内置 loader,允许下游 demand 将上游 `xlsx_file` 或 `xlsx_memory` book 的某个 sheet 作为 rows 输入使用: - loader 稳定导入路径: `scalim.workflow.loaders:book_sheet_rows` - YAML builtin callable id: `^workflow/book_sheet_rows`(由 `yaml-dsl-builtin-callables` 约束) - `book_sheet_rows` MUST 以 canonical field key 作为返回 row 的键 SSOT - `book_sheet_rows` MUST 返回保留 `FieldValue` 值域的 row values,而不是 `CSV` 等价字符串 values loader MUST 接收 `params.ref` 映射对象,并满足以下结构: - `ref.node`（上游 node id） - `ref.book`（book 资源 id） - `ref.sheet`（sheet 名） 约束: - `xlsx_file` 与 `xlsx_memory` MUST 均被支持 - 下游 node 仅允许读取其依赖闭包内上游 nodes 的 book(闭包可见性) - 当引用越界或目标 sheet 不存在时,系统 MUST fail-fast 并提供可诊断摘要 - display header MUST NOT 泄漏到 loader 返回键空间 - loader MUST NOT 依赖猜测性字符串恢复来满足 typed row 语义"
  r11,`.xlsx` outputs MUST use books binding; legacy workbook container surface MUST b,"系统 MUST 将输出 authoring surface 收敛到 `resources + outputs[*].to + outputs[*].write`,并移除 `outputs[*].container` 这条并行路径。 约束: - `.xlsx` 输出 MUST 使用 `resources.books` + `outputs[*].to.book` - CSV 输出 MUST 使用 `resources.files` + `outputs[*].to.file` - `outputs[*].container` MUST 在 schema-only 与 runtime semantic 校验阶段被拒绝 - `outputs[*].to` MUST 成为唯一目标绑定入口 - `outputs[*].write` MUST 成为唯一写入策略入口"
scenarios[26]{req_id,id,given,when,then}:
  r1,"yaml-write-defaults-rejected",YAML 声明 resources.books.report.xlsx_file.path 且同时声明 write_defaults,执行 schema 或 semantic validate,"系统 MUST fail-fast 并提示改用 Python BookWritePolicy"
  r2,baseline,"","TODO: describe the trigger","TODO: describe the expected result"
  r2,"xlsx-file-book-requires-a-path",`resources.books.report.xlsx_file` 被选择,`resources.books.report.xlsx_file.path` 缺失或为空,"编译/校验 MUST fail-fast"
  r2,"xlsx-file-relative-path-is-resolved-against-the-yaml-directo",demand YAML 位于 `/a/b/report.demand.yaml`,系统解析该 book 的输出 root,解析结果 MUST 等价于 `/a/b/out`(经 `resolve` 归一化)
  r3,"yaml-budget-rejected",YAML 声明 resources.books.report.xlsx_memory.budget,执行 validate 或 compile,"系统 MUST fail-fast 并提示改用 Python BookBudgetPolicy"
  r4,baseline,"","TODO: describe the trigger","TODO: describe the expected result"
  r4,"sheet-defaults-to-output-name-and-is-validated","`outputs[0].name: metrics`",系统计算 effective IO binding,"`outputs[0]` MUST 绑定到 book=`report`, sheet=`metrics`"
  r4,"invalid-default-sheet-name-fails-fast","`outputs[0].to.book: report`",系统计算 effective IO binding,"MUST fail-fast"
  r4,"books-output-can-override-header-source-at-output-local-writ","",output 声明 `to.book=report`,该 output 的表头来源 MUST 使用 `field_id`
  r4,"include-header-is-rejected-for-append-mode-books-output",output 绑定到某个 book,用户显式声明 `write.include_header`,"系统 MUST fail-fast"
  r5,baseline,"","TODO: describe the trigger","TODO: describe the expected result"
  r5,"missing-book-id-fails-fast-with-actionable-hint","demand 声明 `outputs[0].to.book: report`",调用方执行 standalone `compile/run` 且未提供 `overrides.resources.books.report`,"MUST fail-fast"
  r6,baseline,"","TODO: describe the trigger","TODO: describe the expected result"
  r6,"workflow-overrides-a-demand-book-path","demand 声明 `resources.books.report: {xlsx_file: {path: ./out/a}}`",workflow 运行该 demand,effective `report.path` MUST 等于 `./out/b`(以 workflow YAML 目录为基准解析)
  r7,baseline,"","TODO: describe the trigger","TODO: describe the expected result"
  r7,"unknown-book-patch-key-fails-fast-with-a-precise-path",用户在 `workflow.resources.books.report` 中提供未知字段 `unknown_key`,workflow compile 应用 book patch,"系统 MUST fail-fast"
  r7,"nested-write-defaults-enum-validation-is-consistent",用户提供 `write_defaults.on_mismatch=not_a_policy`,workflow compile 校验该配置,"系统 MUST fail-fast"
  r8,"python-policy-drives-mode",调用方设置 BookWritePolicy(mode=append) 且 YAML 无 write_defaults,两个 outputs 写入同一 sheet,系统 MUST 按 append 语义合并
  r9,baseline,"","TODO: describe the trigger","TODO: describe the expected result"
  r9,"allow-formulas-is-true-by-default-and-preserves-raw-strings",`resources.books.report.xlsx_file.path=./out`,"写入字符串 `\\\"=1+1\\\"` 到任一 sheet cell","导出的 `.xlsx` 中对应单元格值 MUST 为 `\\\"=1+1\\\"`"
  r9,"allow-formulas-false-escapes-formula-like-strings",`resources.books.report.xlsx_file.path=./out`,"写入字符串 `\\\"=1+1\\\"` 到任一 sheet cell","导出的 `.xlsx` 中对应单元格值 MUST 为 `\\\"'=1+1\\\"`"
  r10,"book-sheet-rows-supports-xlsx-file",上游 node 向 xlsx_file book 的某 sheet 写入 typed rows,下游 node 通过 book_sheet_rows 读取 ref.node/book/sheet,返回 row 的值 MUST 保留 FieldValue 类型且键为 canonical field id
  r10,"book-sheet-rows-xlsx-file-respects-visibility",下游 node 的依赖闭包不包含某 producer,下游调用 book_sheet_rows 引用该 producer 写入的 xlsx_file sheet,"系统 MUST fail-fast 或按既有可见性契约拒绝越界读取"
  r10,"book-sheet-rows-xlsx-memory-unchanged",上游 node 向 xlsx_memory book 写入 typed rows,下游通过 book_sheet_rows 读取,既有 xlsx_memory 行为 MUST 保持（typed FieldValue rows）
  r11,baseline,"","TODO: describe the trigger","TODO: describe the expected result"
  r11,"legacy-container-output-is-rejected-with-migration-hint","","demand YAML 仍声明 `outputs[*].container`","schema-only 与 runtime 校验 MUST fail-fast"