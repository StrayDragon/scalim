kind: llman.sdd.spec
name: "execution-concurrency-safety"
purpose: "为执行层的并发安全定义护栏：任何可能触发用户回调（hooks/observers）或外部回调的操作不得在内部互斥锁临界区内执行，避免重入/锁顺序反转导致的死锁。 [scope-review-2026-07-13-c25-xlsx-ir-path-presence]"
valid_scope[1]: src/scalim/
requirements[2]{req_id,title,statement}:
  r1,instrumentation emit MUST NOT run under internal locks,"当组件在内部使用互斥锁保护状态时（例如 cache pools、workflow resources 等），系统 MUST 禁止在持锁期间执行： - `instrumentation.emit(...)` - 任何会触发 hooks/observers 的回调路径 系统 MUST 将这类外部回调移动到锁外执行（锁内仅采集必要字段与完成状态更新）。"
  r2,"lock-safe emit MUST also be thread-safe for observers by default","系统 MUST 同时满足两条约束： - 回调必须在锁外（避免死锁） - 锁外回调在并发下必须默认安全（避免并发调用 observer 导致竞态） 系统 MUST 通过 capture+replay 或等价的序列化策略实现上述双约束."
scenarios[4]{req_id,id,given,when,then}:
  r1,baseline,"","TODO: describe the trigger","TODO: describe the expected result"
  r1,"hook-reentrancy-does-not-deadlock",调用方注册了一个 hook/observer，其回调会重入触发同一组件的 API,组件触发某个可观测事件（例如 diagnostic warning / resource write）,系统 MUST 不发生死锁
  r2,baseline,"","TODO: describe the trigger","TODO: describe the expected result"
  r2,"lock-safe-emit-does-not-introduce-observer-races",workflow 并发执行且 emit 在锁外,同一个 observer 可能被多个线程触发,系统 MUST 通过序列化/回放避免并发回调