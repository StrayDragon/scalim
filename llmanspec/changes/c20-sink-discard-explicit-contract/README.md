# c20-sink-discard-explicit-contract

把失败清理从 duck-typed `getattr(discard)` 提升为 `ISink.discard()` 显式合约。

- `proposal.md` / `design.md` / `tasks.md`
- deltas：`output-sink-contracts`、`execution-structure`、`execution-output-composition`

下一步：`llman-sdd-apply`。
