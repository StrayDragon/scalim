kind: llman.sdd.delta
ops[1]{op,req_id,title,statement,from,to,name}:
  add_requirement,r10,ColumnExcelSink column residency MUST be documented without default flush,"系统 MUST 明确内建 `ColumnExcelSink` 的列驻留契约: - 调用方在 `write_column`/`write_columns` 成功后 MAY 丢弃其持有的源列数据 - sink 在 `close()` 完成前 MUST 继续持有写入副本以完成原子写出 - 默认路径 MUST NOT 提供隐式 mid-close 列释放/flush 语义(证据显示对 peak RSS ROI≈0) - MUST NOT 将 streaming/flush knobs 引入 YAML 主线 - 可选流式 sibling sink(若未来提出) MUST 显式 opt-in 且不得改变默认 `ColumnExcelSink` 行为",null,null,null
op_scenarios[2]{req_id,id,given,when,then}:
  r10,caller-may-drop-source-columns,"调用方已向 ColumnExcelSink 写入列",调用方释放本地源列引用后继续写入其他列并 close,"系统 MUST 仍能正确写出已接收列且输出可读"
  r10,default-path-has-no-implicit-flush,"维护者审阅 ColumnExcelSink 公共 API 与 YAML 主线",检查是否存在默认 flush/streaming knobs,"默认路径 MUST NOT 暴露隐式列释放或 YAML streaming 配置"
