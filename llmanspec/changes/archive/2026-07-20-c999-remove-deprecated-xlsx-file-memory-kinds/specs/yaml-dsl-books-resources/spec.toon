kind: llman.sdd.delta
ops[11]{op,req_id,title,statement,from,to,name}:
  modify_requirement,r105,books MUST declare exactly one xlsx identity branch,"系统 MUST 在 demand 与 workflow YAML 中提供一致的 resources.books 入口作为 Excel book 的 identity 声明面: - demand: resources.books.<book_id> - workflow: workflow.resources.books.<book_id> - <book_id> MUST 非空且在 mapping 内唯一 - 每个 book MUST 选择且仅选择一个实现分支：稳定 SSOT 分支 xlsx（有 path=pathful；无 path=pathless） - YAML MUST NOT 再接受 deprecated 别名分支 xlsx_file 或 xlsx_memory（出现 MUST fail-fast 并给出迁移到 xlsx 的提示） - YAML books MUST NOT 再把 write_defaults 或 budget 作为稳定 authoring 字段（出现 MUST fail-fast 并迁移到 Python policy） - legacy kind discriminator（含 flat kind 字段）仍 MUST fail-fast - demand 侧 imports/ 规则保持既有 scope；workflow MUST NOT 支持 imports/",null,null,null
  modify_requirement,r209,books MUST support unified xlsx variant with optional path,"系统 MUST 在 resources.books.<id> 提供统一实现分支 xlsx 作为唯一 authoring SSOT： - 当选择 xlsx 且 path 缺省或显式为空语义时，该 book MUST 表示内存共享容器（可被 book_sheet_rows 读取），MUST NOT 强制落盘 - 当选择 xlsx 且 path 为非空字符串或 init_var 指令时，path MUST 表示版本化输出 root，commit 成功后 MUST 发布 .xlsx - xlsx 分支 MUST NOT 再提供并行的 export_xlsx 嵌套字段作为稳定 authoring（导出意图仅由 path 表达） - write_defaults 与 budget MUST 继续禁止出现在 YAML books（Python policy SSOT） - allow_formulas MAY 保留在会导出的 xlsx（有 path）上，规则对齐既有导出转义合约",null,null,null
  modify_requirement,r237,book runtime identity MUST be pathful vs pathless after normalize,在 parse/normalize 之后，book 的运行时身份 MUST 由是否存在版本化导出 path（pathful vs pathless）定义。实现 MUST NOT 把 xlsx_file / xlsx_memory 字符串作为 IR 或 runtime 业务身份 SSOT；MUST NOT 再通过 legacy kind wire shim（例如 options.kind 或 get_book_kind 返回旧 kind）作为对外契约。materialize MUST 按 path 有无绑定既有 workbook（有 path）与 sheetbook（无 path）后端。本需求 MUST NOT 要求合并 workbook/sheetbook 源文件。,null,null,null
  modify_requirement,r347,pathful xlsx.path MUST define file export semantics and path resolution,"系统 MUST 定义 resources.books.<id>.xlsx 在提供非空 path 时的文件导出语义与路径解析基准。当 xlsx.path 存在时，该 book 表示一个版本化输出 root 下的 .xlsx 导出，并满足: - path MUST 为非空字符串或 {$init_var: <name>} 指令节点 - path 语义 MUST 为输出 root 目录（版本化输出 D-2），而不是最终 .xlsx 文件路径 - 相对路径 MUST 以声明该 book 的 YAML 文件所在目录为基准解析(不是进程 CWD) - 系统 MUST 在实际写入前创建父目录 - 系统 MUST 基于 book_id 与 version_id 推导最终输出路径：final path MUST 等价于 <root>/versions/<version_id>/books/<book_id>.xlsx - allow_formulas MUST 为 bool(默认 true) - legacy write_lock 配置面 MUST 被移除；若用户仍提供 write_lock，系统 MUST fail-fast 并给出迁移提示。",null,null,null
  modify_requirement,r151,book_sheet_rows MUST support unified xlsx pathful and pathless books,"系统 MUST 提供 workflow scope 内置 loader scalim.workflow.loaders:book_sheet_rows（builtin ^workflow/book_sheet_rows），允许下游将上游 book 的某 sheet 作为 typed rows 输入。loader MUST 支持统一分支 xlsx 的 pathful 与 pathless 形态。返回键 MUST 为 canonical field id；值 MUST 保留 FieldValue。params.ref MUST 含 node/book/sheet。可见性闭包与 fail-fast 合约保持既有要求。",null,null,null
  modify_requirement,r191,"pathless xlsx MUST remain a supported in-memory shared-book pattern",系统 MUST 将 resources.books.<id>.xlsx 在无 path 时视为合法 book identity：该 book 可作为 workflow 内多 demand 写入、并由下游通过 book_sheet_rows（或等价内置 loader）读取的内存共享容器，且 MUST NOT 强制要求落盘路径。有 path 的 xlsx MUST 继续表示以版本化导出 root 为终点的文件书。系统 MUST NOT 仅因 pathful book 也可承载多 sheet 而移除或静默折叠 pathless 语义。,null,null,null
  modify_requirement,r772,Excel exports MUST escape formulas for pathful xlsx books,"当系统导出 .xlsx（pathful xlsx）时，系统 MUST 默认保留所有字符串 cell 值原样写出，不得执行公式前缀转义。防护模式：若 effective book 配置 allow_formulas=false，系统 MUST 对所有字符串 cell 值执行公式前缀转义。转义规则 MUST 满足：仅对 str 生效；若原始字符串以 ' 开头则保持不变；对 value.lstrip() 首字符属于 { '=', '+', '-', '@' } 时 MUST 在原始值前追加 '；其它字符串保持不变；表头行与数据行均适用。",null,null,null
  modify_requirement,r744,"book write behavior MUST come from Python policy; outputs.write stays header-local","系统 MUST 以 Python BookWritePolicy 配置 book 级写入行为，并允许 outputs 仅覆盖展示/表头局部字段: - mode/align_by/header_policy/on_mismatch/on_conflict MUST 来自 Python policy（缺省用 builtin defaults） - YAML resources.books.<id>.write_defaults MUST NOT 再作为 authoring SSOT - outputs[*].write MAY 提供 include_header/header_fields_output_by 等 output-local header 行为 - outputs[*].write MUST NOT 承载 workbook 级 mode/align_by/header_policy/on_mismatch/on_conflict - pathless xlsx 在 append+align_by=header 时仍 MUST fail-fast",null,null,null
  remove_requirement,r224,null,null,null,null,xlsx_file and xlsx_memory MUST be deprecated aliases during transition
  remove_requirement,r469,null,null,null,null,xlsx_memory remains a deprecated alias for pathless xlsx semantics
  add_requirement,r901,"YAML MUST fail-fast on removed book kind aliases","系统 MUST 拒绝 resources.books.<id> 下的 xlsx_file 与 xlsx_memory 分支（含历史 xlsx_memory.export_xlsx 挂载）。出现时 MUST fail-fast（不得再以 warning 接受），错误信息 MUST 含可复制迁移示例：xlsx_file.path → xlsx.path；xlsx_memory{} → xlsx{}；xlsx_memory.export_xlsx.path → xlsx.path。",null,null,null
op_scenarios[12]{req_id,id,given,when,then,feature}:
  r105,"accepts-only-unified-xlsx-branch",workflow 声明 books.report.xlsx.path,schema 或 semantic validate,系统 MUST 接受该 book identity,true
  r901,"xlsx-file-alias-is-rejected",workflow 仍声明 books.report.xlsx_file.path,validate 或 parse,"系统 MUST fail-fast 并提示迁移到 xlsx.path",true
  r901,"xlsx-memory-alias-is-rejected",workflow 仍声明 books.scratch.xlsx_memory,validate 或 parse,"系统 MUST fail-fast 并提示迁移到无 path 的 xlsx",true
  r901,"xlsx-memory-export-alias-is-rejected",workflow 仍声明 books.report.xlsx_memory.export_xlsx.path,validate 或 parse,"系统 MUST fail-fast 并提示迁移到 xlsx.path",true
  r237,"pathful-xlsx-normalizes-without-kind-shim",books.report.xlsx 声明非空 path,parse 与 normalize,IR/runtime 身份 MUST 为 pathful；MUST NOT 依赖 xlsx_file 字符串作为业务身份,true
  r237,"pathless-xlsx-normalizes-without-kind-shim",books.scratch.xlsx 不提供 path,parse 与 normalize,IR/runtime 身份 MUST 为 pathless；MUST NOT 依赖 xlsx_memory 字符串作为业务身份,true
  r191,"pathless-memory-bus-is-valid",workflow 声明 books.scratch.xlsx 且不提供 path，另有 books.report.xlsx.path,上游写入 scratch 下游 book_sheet_rows 读取再写入 report,校验与执行 MUST 接受 pathless xlsx 总线,true
  r151,"book-sheet-rows-supports-pathful-and-pathless-xlsx",上游向 pathless xlsx book 写入 typed rows,下游 book_sheet_rows 读取该 sheet,返回行 MUST 保留 FieldValue 且键为 field id,true
  r347,"xlsx-path-book-requires-a-path-when-exporting",resources.books.report.xlsx 需要导出但 path 缺失或为空,compile 或 validate,"系统 MUST fail-fast",true
  r209,"unified-xlsx-remains-only-ssot",workflow 声明 books.scratch.xlsx 与 books.report.xlsx.path,validate 与执行,系统 MUST 接受唯一 xlsx 分支的 pathless 与 pathful 语义,true
  r772,"pathful-xlsx-respects-allow-formulas",books.report.xlsx.path 且 allow_formulas=false,导出含公式前缀的字符串 cell,导出值 MUST 按既有转义规则加前导单引号,true
  r744,"pathless-rejects-append-align-by-header","pathless xlsx book 与 BookWritePolicy(mode=append, align_by=header)",compile 或写入校验,"系统 MUST fail-fast",true