# c15-tabular-bus-object-sink-accept-precheck

表格总线（`InMemoryRows`）细胞放宽为 `object`；按 sink 声明 accept set；Python opt-in 预检（默认关）；写出失败清理最终半成品。

- `proposal.md` / `design.md` / `tasks.md`
- `evidence/`：可复现 MVP（共享）；运行 JSON 默认写仓库 `.tmp/`
- delta specs：`specs/`
