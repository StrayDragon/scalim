kind: llman.sdd.delta
ops[6]{op,req_id,title,statement,from,to,name}:
  add_requirement,r13,books MUST support unified xlsx variant with optional path,"系统 MUST 在 resources.books.<id> 提供统一实现分支 xlsx 作为新的 authoring SSOT： - 当选择 xlsx 且 path 缺省或显式为空语义时，该 book MUST 表示内存共享容器（可被 book_sheet_rows 读取），MUST NOT 强制落盘 - 当选择 xlsx 且 path 为非空字符串或 init_var 指令时，path MUST 表示版本化输出 root（与既有 xlsx_file.path 语义一致），commit 成功后 MUST 发布 .xlsx - xlsx 分支 MUST NOT 再提供并行的 export_xlsx 嵌套字段作为稳定 authoring（导出意图仅由 path 表达） - write_defaults 与 budget MUST 继续禁止出现在 YAML books（Python policy SSOT） - allow_formulas MAY 保留在会导出的 xlsx（有 path）上，规则对齐既有导出转义合约",null,null,null
  add_requirement,r14,xlsx_file and xlsx_memory MUST be deprecated aliases during transition,在统一 xlsx 落地后的过渡期，系统 MUST 继续接受既有分支 xlsx_file 与 xlsx_memory（含历史 xlsx_memory.export_xlsx.path 映射为导出 path），其运行语义 MUST 分别等价于 xlsx+path 与 xlsx无path。每当 authoring 仍使用旧分支时，系统 MUST 发出 warning 级 deprecated 诊断（不得仅 debug），文案 MUST 含可复制的迁移到 xlsx 的示例。旧分支 MUST NOT 仅因 deprecated 而失败。移除旧分支或将 warning 升级为 error MUST 作为后续显式 BREAKING change，不得在本需求内静默删除。新文档与示例 MUST 优先展示 xlsx。write/budget MUST 继续仅由 Python ResourcesPolicy 表达，MUST NOT 因本需求回流 YAML。,null,null,null
  modify_requirement,r1,books MUST declare exactly one xlsx identity branch,"系统 MUST 在 demand 与 workflow YAML 中提供一致的 resources.books 入口作为 Excel book 的 identity 声明面: - demand: resources.books.<book_id> - workflow: workflow.resources.books.<book_id> - <book_id> MUST 非空且在 mapping 内唯一 - 每个 book MUST 选择且仅选择一个实现分支：优先稳定 SSOT 分支 xlsx；过渡期 MUST 仍接受 deprecated 别名分支 xlsx_file 与 xlsx_memory（语义见统一 xlsx 与 deprecated 别名要求） - YAML books MUST NOT 再把 write_defaults 或 budget 作为稳定 authoring 字段（出现 MUST fail-fast 并迁移到 Python policy） - legacy kind discriminator（非 xlsx/xlsx_file/xlsx_memory）仍 MUST fail-fast - demand 侧 imports/ 规则保持既有 scope；workflow MUST NOT 支持 imports/",null,null,null
  modify_requirement,r3,xlsx_memory remains a deprecated alias for pathless xlsx semantics,在过渡期，resources.books.<id>.xlsx_memory MUST 仍可解析，并 MUST 正规化为无 path 的 xlsx 内存总线语义（可被 book_sheet_rows 读取；MUST NOT 强制落盘）。若旧 authoring 仍提供 xlsx_memory.export_xlsx.path，系统 MUST 将其正规化为有 path 的导出意图，并发出 warning 级 deprecated 诊断。内存预算 MUST 仍仅由 Python BookBudgetPolicy 表达，YAML MUST NOT authoring budget。新文档 MUST 引导使用 xlsx 而非 xlsx_memory。,null,null,null
  modify_requirement,r9,"Excel exports MUST escape formulas for path-bearing xlsx books","当系统导出 .xlsx（包括有 path 的 xlsx、过渡期 xlsx_file、以及由旧 xlsx_memory.export_xlsx 正规化而来的导出）时，系统 MUST 默认保留所有字符串 cell 值原样写出，不得执行公式前缀转义。防护模式：若 effective book 配置 allow_formulas=false，系统 MUST 对所有字符串 cell 值执行公式前缀转义。转义规则 MUST 满足：仅对 str 生效；若原始字符串以 ' 开头则保持不变；对 value.lstrip() 首字符属于 { '=', '+', '-', '@' } 时 MUST 在原始值前追加 '；其它字符串保持不变；表头行与数据行均适用。",null,null,null
  modify_requirement,r10,book_sheet_rows MUST support unified xlsx and deprecated aliases,"系统 MUST 提供 workflow scope 内置 loader scalim.workflow.loaders:book_sheet_rows（builtin ^workflow/book_sheet_rows），允许下游将上游 book 的某 sheet 作为 typed rows 输入。loader MUST 支持：统一分支 xlsx（有 path 与无 path）、以及过渡期 deprecated 别名 xlsx_file 与 xlsx_memory。返回键 MUST 为 canonical field id；值 MUST 保留 FieldValue。params.ref MUST 含 node/book/sheet。可见性闭包与 fail-fast 合约保持既有要求。",null,null,null
op_scenarios[8]{req_id,id,given,when,then}:
  r13,"unified-xlsx-memory-bus-without-path",workflow 声明 books.scratch.xlsx 且不提供 path，另有 books.report.xlsx.path,上游 runs 写入 scratch 的 sheet，下游用 book_sheet_rows 读 scratch，最终 run 写入 report,系统 MUST 接受无 path 的 xlsx 作为内存总线；scratch MUST 不因缺 path 失败；report MUST 按版本化 root 导出
  r13,"unified-xlsx-export-uses-path-only",YAML 在 books.report.xlsx 下同时试图声明 export_xlsx,schema 或 semantic validate,"系统 MUST fail-fast 或拒绝该字段，并提示导出意图只使用 xlsx.path"
  r14,"deprecated-xlsx-file-still-runs",workflow 仍声明 books.report.xlsx_file.path,validate 与执行,系统 MUST 仍接受并按文件书语义导出，且 MUST 发出 warning 级迁移到 xlsx.path 的 deprecated 诊断
  r14,"deprecated-xlsx-memory-bus-still-runs",workflow 仍声明 books.scratch.xlsx_memory 且无 export_xlsx,下游 book_sheet_rows 读取 scratch,系统 MUST 保持内存总线语义，且 MUST 发出 warning 级迁移到无 path 的 xlsx 的 deprecated 诊断
  r1,"accepts-unified-xlsx-branch",workflow 声明 books.report.xlsx.path,schema 或 semantic validate,系统 MUST 接受该 book identity，不得因缺少 xlsx_file/xlsx_memory 键而失败
  r3,"memory-export-xlsx-aliases-to-path",workflow 声明 books.report.xlsx_memory.export_xlsx.path,validate 或 compile,系统 MUST 正规化为有 path 的导出语义，MUST 发出 warning，MUST NOT 仅因 deprecated 失败
  r9,"unified-xlsx-path-respects-allow-formulas",books.report.xlsx.path 且 allow_formulas=false,导出含公式前缀的字符串 cell,导出值 MUST 按既有转义规则加前导单引号
  r10,"book-sheet-rows-supports-unified-xlsx",上游向无 path 的 xlsx book 写入 typed rows,下游 book_sheet_rows 读取该 sheet,返回行 MUST 保留 FieldValue 且键为 field id