# c0-add-field-value-datetime（active）

转正自 notplan。指针：旧目录见 `llmanspec/notplan/c0-add-field-value-datetime/README.md`。

关键决策见 `proposal.md` / `design.md`：扩 `FieldValue` 含 openpyxl 全部时间类型；撤 `str()`；**不去 tz**。
