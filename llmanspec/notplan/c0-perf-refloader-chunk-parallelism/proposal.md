# SUPERSEDED（已转正）

> 一句话描述: 为 ref loader 分片加载引入默认串行、opt-in 并行的加速能力；已转正至 `c30-refloader-chunk-parallelism` 并实现。

本 notplan 草案 **不再作为讨论/实现 SSOT**。

| 项 | 值 |
|----|-----|
| 状态 | superseded |
| 替代 active change | `c30-refloader-chunk-parallelism`（`llmanspec/changes/c30-refloader-chunk-parallelism/`） |
| 转正日期 | 2026-08-01 |
| 要点保留 | 默认串行分片；opt-in 并行；不新增 YAML 键；合并语义 ≡ 串行。 |

请只读并修改：`llmanspec/changes/c30-refloader-chunk-parallelism/{proposal,design,tasks}.md`。

历史长文已移除，避免与 active change 双源漂移。
