# SUPERSEDED（已转正）

本 notplan 草案 **不再作为讨论/实现 SSOT**。

| 项 | 值 |
|----|-----|
| 状态 | superseded |
| 替代 active change | `c10-write-precompute-derived-fields`（`llmanspec/changes/c10-write-precompute-derived-fields/`） |
| 转正日期 | 2026-08-01 |
| 范围修订 | Active change 第一期 late 字段含 **无 `$ctx` 的 `call_by`**（用户确认）；含 `$ctx` 禁止 late。 |

请只读并修改：`llmanspec/changes/c10-write-precompute-derived-fields/{proposal,design,tasks}.md`。

历史长文已移除，避免与 active change 双源漂移。
